/**
 * 이 페이지의 **자** — 글자 크기와 간격. 값을 손으로 적지 말고 여기서 고른다.
 *
 * ## ⚠️ 왜 자가 필요한가
 *
 * 자가 없을 때 이 페이지의 글자 크기가 **열셋**이었다(10 · 10.5 · 11 · 11.5 · 12 ·
 * 12.5 · 13 · 14 · 15 · 18 · 20 · 21 · 28). 값 하나하나는 그럴듯한데 모아 놓으면
 * 규칙이 안 보여서 카드가 정돈돼 보이지 않는다("폰트도 들쭉날쭉이라").
 *
 *   big   22  카드에 **하나뿐인** 큰 숫자
 *   lead  15  결론 문장 · 요약 타일의 값
 *   body  12  라벨 · 목록 · 문장 · 각주
 *   small 11  조밀한 표의 이름과 값 · 범례 · 단위 · 보조
 *
 * ⭐ **네 단이면 충분하다.** 자에 눈금이 있으면 언젠가 누가 쓴다 — 달력 카드가 먼저
 * 이 교훈을 겪어 다섯에서 셋으로 줄였다. 이제 그 카드도 이 자를 쓴다.
 *
 * ## ⚠️⚠️ 바닥이 10 이었다. **이 사이트에서 그렇게 작은 글자는 여기뿐이었다**
 *
 * 시장 브리핑·MDD·카더라의 작은 보조 글자는 전부 **11~11.5px** 이다. 10px 짜리 `tiny`
 * 를 따로 두고 44곳에 쓰고 있었는데, 크기만 작은 게 아니라 색까지 `--c-faint` 라
 * 명암비가 **2.69** 였다(기준 4.5). "텍스트들이 너무 연하다"는 말이 그 자리다.
 * small 로 합쳤다.
 *
 * ⚠️ 아이콘 크기는 이 자가 아니다. 글자가 아니라 그림이라 광학 크기가 따로 논다.
 *
 * ⚠️ 시트 머리(18 · 14 · 12.5)는 `SectionHead` 의 자다. 다섯 화면이 공유하는 값이라
 * 이 페이지 혼자 바꾸면 안 된다.
 */
export const T = { big: 22, lead: 15, body: 12, small: 11 } as const;

/** 간격도 4의 배수 넷으로만. */
export const S = { xs: 4, sm: 8, md: 12, lg: 16 } as const;
