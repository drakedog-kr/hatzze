"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { createContext, useContext, useEffect, useRef, useState, useSyncExternalStore } from "react";
import { INSIDER_LISTS, INSIDER_LIST_SLUGS, insiderListHref } from "./insider/lists";
import { PageJsonLd } from "./JsonLd";
import { NOTE_PAGE } from "./daily/copy";
import { DIVIDEND_PAGE } from "./dividend/copy";
import { KR_THEME_SHORT, THEME_PAGE, US_THEME_PAGE } from "./theme/copy";
import { DAILY_PUBLIC, DIVIDEND_PUBLIC, SEOHAK_PUBLIC, THEME_PUBLIC } from "./screen-flags";
import { THEME_NAMES, US_THEME_NAMES, themeHref, usThemeHref } from "@/lib/theme-href";

import { track } from "@/lib/ga";
import { SLOGAN } from "./brand";
import { C, Icon, R } from "./ui";
import { BetaBadge, LogoLockup } from "./Logo";
import Footer from "./Footer";
import GaEvents from "./GaEvents";
import { TipTap } from "./TipTap";

// sub 는 본문 헤더의 페이지 부제다(목업이 사이드바 로고 밑에 있던 문장을 여기로 옮겼다).
// 사이드바 항목에는 안 쓰이고 PageHeader 만 읽는다.
// badge 는 제목 옆 회색 알약이다(콘솔 리디자인). "이 화면이 몇 개를 다루나"를 제목 줄에서
// 바로 말해 준다 — 부제 문장으로 적으면 읽어야 알 수 있다. 없는 화면엔 안 그린다.
type Glyph = (props: { size?: number; dimmed?: boolean }) => React.ReactElement;

/**
 * 카더라 서브 항목의 표식 둘 — 국장은 **손으로 그린 태극**, 미장은 **시안을 벡터로 뜬
 * 자유의 여신상**이다.
 *
 * ⭐⭐ 시안이 있으면 눈대중으로 옮기지 말고 벡터로 뜰 것. 처음엔 맨손으로 좌표를 찍었는데
 * 하나를 열 번 넘게 고쳐도 매번 근사치였다("삐죽삐죽 이상하다"). potrace 로 원본 윤곽을
 * 그대로 뜨니 한 번에 끝났다. 반대로 태극처럼 **원·호 몇 개로 떨어지는 도형**은 손으로
 * 그리는 편이 낫다 — 좌표가 뜻을 갖고(아래 33.69°), 파일도 100배 작다.
 * 다만 뜻을 갖는 만큼 **틀려도 그럴듯해 보인다**. 태극은 국기 원본 경로와 대조할 것.
 *
 * 재현 방법(스크래치패드에 도구가 있다):
 *   node vectorize/trace.mjs <이미지> [--flip] [--threshold 128]
 *   → traced.jsx(붙여넣을 JSX) · preview.png(18/20/32/96px 를 라이트·다크로)
 *   ⚠️ 밝은 그림 / 어두운 바탕 시안은 **먼저 반전**해야 한다(안 그러면 배경이 떠진다).
 *
 * transform 은 원본 좌표를 24 박스로 옮기는 것이다(여백을 잘라 가운데 맞춤).
 * 경로 좌표를 직접 고치지 말고 **시안을 고쳐 다시 뜰 것** — 손으로 만지면 원본과 갈린다.
 *
 * ⭐⭐ **고르는 기준은 18px(서브 항목의 실제 크기)이다.** 96px 에서 좋아 보이는 그림이
 * 여기서도 좋다는 보장이 전혀 없다. 실제로 호랑이 시안 셋을 이 크기로 찍어 보고 전부
 * 접었다 — 특히 **가로로 긴 그림(전신 옆모습 2.07:1)은 정사각 칸에서 자리를 못 쓴다**
 * (폭을 맞추면 잉크 높이가 7.8px 로, 옆자리 아이콘의 절반이 된다).
 * vectorize/compare2.mjs 가 후보들을 18·20·32px 로 라이트·다크·활성 알약에 나란히 찍는다.
 *
 * 이모지(🇰🇷 🇺🇸)를 그대로 안 쓰는 이유: currentColor 를 안 따라 활성 알약(파랑) 위에서
 * 아이콘 열이 깨지고, 플랫폼마다 그림이 달라진다(🇰🇷 는 윈도우에서 글자 "KR").
 *
 * ⚠️ 여신상 시안의 출처가 확인되지 않았다. 저장소가 public 이라 라이선스를 확인하거나,
 *    비율만 참고하고 세부를 다르게 다듬어야 할 수 있다.
 */

/** 태극 — 🇰🇷 에서 태극만 남겼다(건곤감리는 이 크기에서 회색 얼룩이 된다). */
function KrTaegukIcon({ size = 20 }: { size?: number }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden="true"
      style={{ display: "block", flexShrink: 0 }}
    >
      {/*
        기울기 33.69° = atan(2/3) — 깃면이 3:2 라 태극의 축이 그 대각선과 나란하다.
        이 기울기가 태극기의 태극을 일반적인 음양 문양과 가른다.

        ⚠️ 2026-08-14 까지 좌우가 뒤집힌 채였다("물결이 반대로 친다"). 기울기 부호와
        작은 호 둘의 sweep 이 함께 뒤집혀 있어서 얼핏 그럴듯해 보였다 — 국기의 태극을
        거울에 비친 꼴이다. 눈으로 맞추지 말고 **국기 원본 경로와 대조할 것**:
          빨강 M12 0 a18 18 0 1 1 -36 0  24 24 0 1 1 48 0   (rotate 33.69)
          파랑 M-24 0 a24 24 0 1 0 48 0  A12 12 0 1 0 0 0  a12 12 0 1 1 -24 0
        여기서 읽어야 할 결론은 하나다. 축을 수평으로 눕혔을 때 경계선은
        **왼쪽에서 아래로 파이고 오른쪽에서 위로 솟는다**. 작은 호의 sweep 0·1 이 그 순서다.

        색이 하나뿐이라 반쪽만 채운다. 채우는 쪽은 **국기의 파랑(아래)** 이다 — 큰 호의
        sweep 1 이 그것이고, 0 이면 빨강(위)이 채워진다. 경계선은 어느 쪽을 채우든 같다.
      */}
      <g transform="rotate(33.69 12 12)">
        <path
          d="M3.6 12 A4.2 4.2 0 0 0 12 12 A4.2 4.2 0 0 1 20.4 12 A8.4 8.4 0 0 1 3.6 12 Z"
          fill="currentColor"
        />
      </g>
      <circle cx="12" cy="12" r="8.4" stroke="currentColor" strokeWidth="1.7" />
    </svg>
  );
}

/**
 * 자유의 여신상 — 미장. 앞서 쓰던 독수리(같은 방법으로 뜬 시안)를 갈아 끼웠다.
 *
 * 시안을 한 번 갈았다(2026-08-12). 처음엔 **얼굴만** 있는 시안이었는데, 뿔이 상자를
 * 넓게 차지해서 정작 얼굴이 18px 에서 안 읽혔다("얼굴이 너무 안 보인다").
 *
 * ⭐⭐ **키우는 방법을 바꿨다. 상자 안에서 확대하지 않고, 상자째 크게 그린다.**
 * 이 시안은 세로로 길어서(282×425) 상자에 맞추면 세로가 먼저 꽉 찬다. 거기서 배율만
 * 올리면 남는 건 가로 여백인데 확대는 가로세로를 같이 키우므로, **세로가 먼저 넘쳐
 * 횃불 꼭대기와 옷자락이 잘렸다**(120% 면 0.96씩). 경로는 100% 로 두고 SVG 자체를
 * 크게 그리면 잘리는 데가 전혀 없다.
 *
 * BLEED 가 그 장치다. 그림은 size×1.22 로 그리고 음수 마진으로 그만큼 되당겨,
 * **줄에서 차지하는 자리는 size 그대로**다. 그래야 옆 항목(태극)과 라벨 왼쪽 선이
 * 어긋나지 않는다. 18px 자리에서 23px 로 그려 잉크 세로가 20.7px 이 된다.
 *
 * ⭐ 세로로 긴 글리프를 옆 아이콘과 **같은 크기로 그리면 더 작아 보인다**(가로를 못
 * 써서 잉크가 얇다). 광학적으로 맞추려고 일부러 키운 것이지 실수가 아니다.
 * 비율을 바꾸려면 BLEED 만 만질 것 — 경로 좌표와 transform 은 손대지 말 것.
 */
/** 그림을 자리보다 몇 배 크게 그릴지. 넘친 만큼은 음수 마진으로 되당긴다. */
const LIBERTY_BLEED = 1.25;

function LibertyIcon({ size = 20 }: { size?: number }) {
  const draw = Math.round(size * LIBERTY_BLEED);
  const pull = (draw - size) / 2;
  return (
    <svg
      width={draw}
      height={draw}
      viewBox="0 0 24 24"
      fill="currentColor"
      aria-hidden="true"
      style={{ display: "block", flexShrink: 0, margin: -pull }}
    >
      <g transform="translate(4.839 1.200) scale(0.05080) translate(-349.000 -280.000)">
        <path fillRule="evenodd" d="M 389.154 283.451 C 387.746 287.345, 385.009 289.821, 376.054 295.303 C 370.795 298.522, 369.451 299.957, 367.304 304.647 C 365.269 309.093, 364.858 311.273, 365.224 315.688 C 365.629 320.570, 365.445 321.278, 363.590 321.985 C 352.225 326.316, 349 328.988, 349 334.076 C 349 337.684, 350.354 338.469, 353.275 336.555 C 357.250 333.951, 366.633 331.157, 373.767 330.454 C 383.358 329.509, 394.133 331.353, 402.250 335.329 L 409 338.635 409 334.752 C 409 327.969, 406.383 325.491, 395.950 322.392 L 391.156 320.969 394.545 315.932 C 400.064 307.732, 401.181 301.242, 398.569 292.562 C 397.099 287.679, 392.587 280, 391.187 280 C 390.755 280, 389.840 281.553, 389.154 283.451 M 367.004 339.099 C 361.316 340.213, 355.014 342.706, 352.750 344.739 C 351.788 345.603, 351 347.580, 351 349.132 C 351 352.379, 352.852 353.836, 361 356.998 C 364.025 358.172, 367.123 359.742, 367.885 360.487 C 368.964 361.542, 369.295 366.215, 369.385 381.670 C 369.498 401.111, 369.552 401.600, 372.166 406.572 C 374.199 410.438, 377 420.354, 377 423.684 C 377 423.858, 377.900 424, 379 424 C 380.100 424, 381 424.587, 381 425.305 C 381 427.570, 388.859 477.097, 391.520 491.605 C 394.554 508.144, 398.934 527.182, 399.962 528.298 C 400.366 528.737, 403.337 525.812, 406.563 521.798 C 415.616 510.537, 416 510.258, 416 514.946 C 416 518.779, 409.522 531.238, 403.960 538.102 C 398.937 544.301, 388.308 553.881, 381.641 558.218 L 377.078 561.186 383.153 573.343 C 386.494 580.029, 389.445 585.725, 389.711 586 C 390.830 587.156, 408.839 563.506, 420 546.224 C 424.005 540.023, 427.490 535.157, 427.744 535.410 C 428.521 536.188, 423.922 549.093, 420.228 556.500 C 416.227 564.521, 414.026 567.884, 401.866 584.558 L 393.072 596.616 398.678 611.058 C 401.762 619.001, 404.608 625.865, 405.004 626.311 C 406.338 627.817, 427.332 593.319, 440.186 568.500 C 448.449 552.547, 449.553 552.156, 446.392 566.303 C 444.312 575.609, 438.320 590.275, 431.376 603.051 C 428.343 608.631, 426.017 613.351, 426.206 613.540 C 426.860 614.193, 472.533 581.727, 484.056 572.418 C 497.215 561.788, 500.176 560.015, 504.785 560.006 C 507.749 560.001, 507.967 560.194, 507.012 561.978 C 504.236 567.165, 488.499 585.680, 478.094 596.001 C 470.848 603.189, 450.467 620.358, 423.750 641.781 C 400.238 660.634, 381 676.267, 381 676.521 C 381 677.541, 403.312 688.094, 404.503 687.637 C 405.207 687.367, 413.699 678.144, 423.375 667.142 C 439.491 648.816, 443 645.092, 443 646.315 C 443 646.553, 437.150 656.796, 430 669.077 C 422.850 681.357, 417 691.743, 417 692.155 C 417 693.262, 434.578 697.810, 435.376 696.911 C 435.576 696.685, 439.454 689.525, 443.994 681 C 459.286 652.282, 474.628 626.460, 490.338 603 C 499.498 589.320, 515.961 567, 516.891 567 C 517.291 567, 518.441 567.602, 519.448 568.338 C 521.166 569.595, 520.792 570.552, 513.280 584.088 C 508.882 592.015, 503.574 601.650, 501.486 605.500 C 499.397 609.350, 495.076 617.287, 491.882 623.137 C 480.873 643.306, 451.994 699.328, 452.398 699.732 C 452.621 699.955, 458.683 700.398, 465.869 700.718 C 475.444 701.144, 479.329 700.972, 480.410 700.075 C 481.222 699.401, 483.098 695.171, 484.579 690.675 C 492.413 666.900, 509.301 624.967, 510.542 626.209 C 510.794 626.461, 508.802 641.029, 506.115 658.583 C 498.975 705.237, 499.462 700.173, 502.183 699.491 C 503.458 699.172, 509.124 697.870, 514.776 696.597 C 520.427 695.324, 525.246 694.087, 525.484 693.849 C 525.722 693.611, 525.438 687.586, 524.854 680.458 C 524.237 672.932, 524.002 654.504, 524.294 636.500 C 524.737 609.125, 525.096 603.777, 527.365 590.765 C 529.684 577.469, 531.853 571, 533.994 571 C 534.414 571, 534.989 589.337, 535.270 611.750 C 535.687 644.926, 536.183 655.840, 537.942 670.467 C 539.130 680.349, 540.272 688.606, 540.481 688.814 C 541.157 689.490, 559.968 680.174, 568.533 674.922 C 587.617 663.219, 609.155 643.210, 623.689 623.681 C 627.755 618.218, 630.951 613.644, 630.791 613.516 C 630.450 613.245, 612.358 603.238, 603.102 598.201 L 596.705 594.720 593.348 598.610 C 575.585 619.198, 571.368 623.818, 570.535 623.604 C 570.004 623.468, 565.925 613.873, 561.470 602.282 C 556.489 589.323, 551.712 578.662, 549.066 574.601 C 543.910 566.688, 534.663 557.876, 527.937 554.468 C 521.036 550.971, 519.692 547.952, 520.568 537.910 C 521.259 529.987, 521.250 529.945, 517.553 523.955 L 513.844 517.946 516.570 510.999 L 519.296 504.051 516.711 498.275 C 515.289 495.099, 513.397 489.286, 512.507 485.358 C 510.506 476.528, 506.571 470.160, 500.302 465.609 C 494.001 461.034, 488.651 459.629, 479.746 460.211 C 465.239 461.158, 454.947 470.404, 451.910 485.217 C 451.314 488.123, 449.687 493.410, 448.295 496.967 C 445.620 503.799, 445.740 506.482, 449.092 514.693 C 450.258 517.549, 450.146 518.419, 448.027 522.943 C 446.724 525.724, 445.440 528, 445.173 528 C 444.907 528, 443.183 526.852, 441.343 525.448 C 437.265 522.338, 435.005 518.196, 434.986 513.800 C 434.976 511.386, 432.962 507.452, 427.486 499.147 C 423.369 492.902, 420 487.351, 420 486.811 C 420 485.884, 427.989 486.209, 440.250 487.635 C 444.181 488.092, 445 487.910, 445 486.575 C 445 483.767, 449.656 471.505, 452.332 467.267 C 460.084 454.986, 473.300 449.334, 488.022 452.004 C 504.153 454.929, 515.026 465.591, 518.224 481.617 C 518.869 484.853, 519.511 487.665, 519.649 487.867 C 520.115 488.547, 557 483.679, 557 482.938 C 557 482.535, 553.962 481.687, 550.250 481.053 C 525.960 476.907, 526.013 476.922, 525.394 474.120 C 525.075 472.679, 524.625 470.788, 524.393 469.918 C 523.832 467.810, 525.476 466.517, 539 458.426 C 546.269 454.078, 549.852 451.418, 548.738 451.198 C 547.769 451.006, 540.603 452.074, 532.814 453.571 L 518.651 456.293 515.769 453.067 C 513.856 450.927, 513.195 449.448, 513.803 448.671 C 514.308 448.027, 520.421 439.741, 527.388 430.259 C 540.104 412.952, 542.687 408.881, 539.920 410.506 C 538.104 411.572, 522.077 424.311, 510.728 433.709 C 505.904 437.704, 500.865 441.247, 499.532 441.581 C 497.004 442.216, 490.431 440.506, 489.479 438.966 C 489.177 438.478, 487.416 428.581, 485.565 416.973 C 483.714 405.364, 481.941 395.607, 481.623 395.290 C 480.708 394.375, 480.310 396.397, 477.552 416 C 473.960 441.527, 474.478 439.773, 470.176 440.968 C 463.437 442.839, 466.113 444.350, 441 424.500 C 424.469 411.433, 422 409.646, 422 410.744 C 422 411.283, 428.518 420.374, 436.484 430.945 C 450.916 450.095, 450.962 450.174, 449.234 452.558 C 448.280 453.873, 446.799 455.189, 445.943 455.481 C 445.087 455.773, 437.887 454.663, 429.943 453.014 C 421.999 451.365, 414.938 450.013, 414.250 450.008 C 411.598 449.991, 413.294 451.648, 419.769 455.401 C 436.321 464.995, 440 467.794, 440 470.789 C 440 475.461, 438.105 476.849, 429.500 478.482 C 418.799 480.512, 415.307 480.441, 414.091 478.170 C 413.552 477.163, 412.186 470.826, 411.055 464.088 C 409.924 457.349, 407.221 444.110, 405.049 434.668 C 399.951 412.512, 399.187 407.406, 398.441 390.500 C 397.759 375.072, 396.866 370.887, 393.355 366.673 C 389.379 361.901, 390.140 360.342, 398.250 356.640 C 406.728 352.770, 408.479 351.043, 407.574 347.441 C 405.876 340.673, 382.993 335.969, 367.004 339.099 M 476.408 473.754 C 471.374 477.131, 466 481.651, 466 482.507 C 466 482.857, 468.469 482.835, 471.488 482.458 C 476.040 481.889, 477.082 482.049, 477.598 483.394 C 478.966 486.959, 472.638 492.482, 465.500 493.953 C 462.956 494.477, 462.449 495.097, 462.167 498.035 C 461.168 508.444, 469.266 519, 478.250 519 C 484.389 519, 485.936 515.053, 480.832 512.413 L 477.664 510.775 480.332 509.156 C 483.652 507.141, 483.660 506.622, 480.393 505.133 L 477.786 503.945 481.143 501.653 C 484.430 499.407, 484.494 499.226, 484.223 492.930 C 484.071 489.393, 484.206 485.432, 484.523 484.128 C 485.054 481.941, 485.488 481.792, 490.050 482.231 C 492.772 482.492, 495 482.586, 495 482.438 C 495 481.402, 483.194 471.037, 482 471.024 C 481.175 471.016, 478.658 472.244, 476.408 473.754 M 380.767 512.706 C 379.368 515.893, 377.657 520.949, 376.965 523.942 C 375.640 529.679, 375.050 548.383, 376.159 549.492 C 376.517 549.850, 380.041 547.994, 383.990 545.368 C 390.648 540.941, 391.140 540.371, 390.743 537.546 C 389.854 531.213, 384.748 508.466, 384.046 507.706 C 383.642 507.270, 382.167 509.520, 380.767 512.706 M 469 531.875 C 469 540.441, 469.918 543.765, 473.190 547.036 C 476.149 549.995, 482.571 552.372, 486.087 551.809 C 487.799 551.535, 487.336 550.288, 482.001 540.813 C 476.897 531.746, 472.085 526, 469.596 526 C 469.268 526, 469 528.644, 469 531.875" />
      </g>
    </svg>
  );
}

/**
 * 테마 리포트의 국장·미장 서브 아이콘 — **태극기의 건곤감리**(마름모 배치)와 **엠파이어 스테이트 빌딩 상층부**.
 * 2026-09-22 에 받은 참고 이미지 둘을 **potrace 로 그대로 떴다**(카더라 여신상과 같은 방법 — 손으로 옮긴 두 시안이 "완전 다르다"였다).
 * 카더라 서브(태극·여신상)와 같은 '나라 상징' 결이지만 그림이 다르다.
 *
 * 건곤감리: 참고는 빨강(위 둘 · 건 왼쪽 위 · 감 오른쪽 위)·파랑(아래 둘 · 리 왼쪽 아래 · 곤 오른쪽 아래) 두 색이다. 색이 하나뿐이라
 * **빨강은 테두리만, 파랑은 채운다** — 국장 카더라의 태극이 파랑(아래)만 채우는 규칙 그대로. 두 색을 따로 마스크로 떠서 경로가 둘이다.
 * 테두리 굵기는 0.45(24 박스 기준) — 막대 굵기가 1.3 이라 그 이상이면 속이 찬다. 18px 에선 테두리가 옅은 막대로 보이고 36px 부터 속이 보인다.
 *
 * 엠파이어 스테이트: 참고는 그림 그린 선(잡티가 있는 연필 선)이라, 흐림 2 · 문턱 170 · 잡티 120 으로 떠서 굵은 선만 남겼다("선만 깔끔하게").
 * 창의 세로줄은 남아 있어 36px 부터 보이고 18px 에선 첨탑 실루엣이다.
 *
 * 재현: 저장소 밖 hatzze-icons 폴더의 trace.mjs(potrace + jimp) — 색 마스크를 회색 점수로 적고 potrace 가 문턱을 나눈다(불리언으로
 * 자르면 경계가 톱니가 되어 경로가 두 배로 길어진다). transform 은 원본 좌표를 24 박스로 옮기는 것(여백 1, 가운데 맞춤).
 * 둘 다 여신상과 같은 BLEED 장치로 크게 그린다(LibertyIcon 주석의 이유).
 */
const THEME_GLYPH_BLEED = 1.2;

function KrTrigramsIcon({ size = 20 }: { size?: number }) {
  const draw = Math.round(size * THEME_GLYPH_BLEED);
  const pull = (draw - size) / 2;
  return (
    <svg width={draw} height={draw} viewBox="0 0 24 24" aria-hidden="true" style={{ display: "block", flexShrink: 0, margin: -pull }}>
      <g transform="translate(-0.018 0.255) scale(0.05992)">
        {/* 건·감(참고의 빨강) — 테두리만. 굵기는 원본 좌표계라 24 박스의 0.45 에 해당한다. */}
        <path d="M 192.1 20.5 C 188.2 24.6 185 28.5 185 29.2 C 185 31 230.3 76 232.1 76 C 233 76 237 72.9 240.9 69 L 248 62.1 248 59.8 L 248 57.6 225.7 35.3 L 203.3 13 201.3 13 L 199.3 13 192.1 20.5 M 166.4 46.2 C 162.3 50.3 159 54.5 159 55.6 L 159 57.5 209.8 108.3 C 238.4 137 261.5 159.3 262.9 159.7 L 265.3 160.3 272.6 152.8 C 276.7 148.6 280 144.7 280 144 C 280 142.3 180.3 42.3 176.7 40.4 L 173.8 38.8 166.4 46.2 M 139.5 73.5 C 135.4 77.6 132 81.5 132 82.2 C 132 84.2 177.3 129 179.3 129 C 181.5 129 196 115.1 196 113 C 196 111 150.7 66 148.6 66 C 147.7 66 143.6 69.4 139.5 73.5 M 250.3 77.3 C 246.3 81.4 243 85.5 243 86.5 L 243 88.4 265.9 111.2 C 278.5 123.8 289.3 134 290.1 134 C 292.1 134 306 119.6 306 117.4 L 306 115.5 283.2 92.8 C 270.7 80.2 259.8 70 259 70 C 258.3 70 254.4 73.3 250.3 77.3 M 68.2 144.3 L 17 195.5 17 197.6 L 17 199.7 24 206.4 L 31 213.1 33 212.8 C 35.1 212.5 138 110.5 138 108.7 C 138 108.2 134.8 104.5 130.8 100.4 L 123.7 93 121.6 93 L 119.5 93 68.2 144.3 M 94.8 170.7 C 66.3 199.2 43 223.1 43 223.8 C 43 225.8 57 240 58.9 240 C 61.1 240 164 137.5 164 135.4 C 164 133.4 149.9 119 147.9 119 C 147.2 119 123.2 142.3 94.8 170.7 M 198.2 129.6 C 194.2 133.2 190.7 137.3 190.4 138.7 L 189.7 141.2 212.6 164.1 C 225.2 176.7 236.2 187 237.1 187 C 239.2 187 253 172.7 253 170.5 C 253 168.7 208.2 123 206.4 123 C 205.9 123 202.2 126 198.2 129.6 M 122.4 196.1 C 94 224.4 70.4 248.3 69.8 249.3 L 68.8 251.2 76.7 259.1 C 81.1 263.4 85.1 267 85.8 267 C 87.5 267 191 163.7 191 161.9 C 191 160.6 180.3 149.5 175.7 146 L 173.9 144.7 122.4 196.1" fill="none" stroke="currentColor" strokeWidth="7.5" strokeLinejoin="round" />
        {/* 리·곤(참고의 파랑) — 채움 */}
        <path d="M 314 132.8 C 312.6 133.7 307.7 138.5 303 143.4 C 291.7 155.2 291.7 155.2 281.2 165.4 C 269.7 176.5 269.7 175.7 280.3 184.5 L 286.1 189.3 294.2 181.4 C 334.6 142.2 332.6 145.1 323.9 136.8 C 317 130.2 317.4 130.4 314 132.8 M 334.4 165.1 C 330.1 169.6 325.7 174 324.8 174.9 C 321.5 178 320.7 178.8 313.6 185.8 C 301.5 197.8 300 199.4 299.4 201.3 L 298.8 203.2 302.2 206.5 C 304 208.3 306.9 211.2 308.5 212.9 C 312.6 217.1 312.3 217.3 323.1 206.3 C 328.4 200.9 333.5 195.8 334.4 195 C 335.3 194.2 340 189.3 344.9 184.3 C 349.9 179.2 354.3 175 354.9 175 C 358.7 175 356 166.4 351.5 164 C 350 163.2 347.9 161.3 346.8 159.8 C 344 155.8 343.2 156.1 334.4 165.1 M 364.2 188.3 C 361.4 191.1 358.2 194.7 357 196.3 C 355.8 197.8 354.4 199 353.8 199 C 352.2 199 342 209.8 342 211.5 C 342 212.3 340.8 213.6 339.2 214.2 C 335.8 215.8 325 227.1 325 229.2 C 325 230 328 233.4 331.6 236.9 L 338.2 243.1 340.4 241.3 C 341.5 240.3 351.9 230.1 363.3 218.7 L 384.1 197.9 383.8 196.2 C 383.6 195.3 382.5 194.1 381.4 193.5 C 380.2 192.9 377.2 190.3 374.7 187.7 C 369.1 182 370.2 181.9 364.2 188.3 M 249 197.3 C 244.9 201.7 239.3 207.5 236.6 210 C 233.9 212.5 230.1 216.2 228.1 218.4 C 226.1 220.5 222.4 224.3 219.8 226.8 C 217.1 229.3 215 232.1 215 233 C 215 234.8 226.2 247 227.9 247 C 228.5 247 239.2 236.9 251.6 224.5 L 274.2 201.9 272.4 199.7 C 271.3 198.5 269.8 197.1 269 196.6 C 268.2 196 266.2 194.1 264.6 192.3 L 261.6 189 259.1 189.1 L 256.5 189.1 249 197.3 M 160.7 215.6 C 157.6 218.1 154.4 221.3 153.5 222.7 L 151.8 225.2 171.7 245.2 C 182.6 256.2 192.1 265.7 192.8 266.3 C 193.5 267 197.8 271.1 202.4 275.5 C 206.9 279.9 211.2 284 211.7 284.5 C 212.3 285.1 215.6 288.5 219.1 292.3 C 222.5 296 225.9 299 226.6 299 C 227.4 299 228.6 300.2 229.4 301.8 C 231 304.8 237.8 311.5 242.5 314.8 C 244.2 315.9 246.2 317.9 246.9 319.2 C 247.6 320.5 249.7 322.7 251.4 324.2 L 254.6 326.9 259.1 323 C 261.5 320.8 264.5 318.2 265.8 317.1 C 268.4 314.7 268.7 311.6 266.4 309.8 C 264.1 307.9 260 304 255.8 299.5 C 249.8 293 248.8 292.1 242.5 286 C 239.2 282.8 235.4 279.1 234 277.7 C 232.6 276.3 221.6 265.3 209.6 253.3 C 197.5 241.3 184.7 228.5 181.2 224.8 C 177.7 221.1 172.9 216.5 170.5 214.5 L 166.2 211 160.7 215.6 M 281.3 218.2 C 279.7 219.9 277.7 222.1 276.8 222.9 C 273.4 226.1 245.6 254 245.1 254.8 C 244.7 255.3 243.7 255.9 242.8 256.3 C 239.8 257.4 240.8 259.6 247.8 267 L 254.5 274.2 277.3 251.7 C 289.8 239.4 300 228.9 300 228.4 C 300 227 286.8 215 285.3 215 C 284.6 215 282.8 216.4 281.3 218.2 M 131.7 244 C 123.7 252.1 123.3 250.9 137.2 264 C 143.3 269.8 150.9 277.2 154.1 280.5 C 157.3 283.8 160.6 287.2 161.5 288 C 162.4 288.8 164.4 290.7 166 292.3 C 167.6 293.8 169.9 295 171.4 295 L 174 295 175.2 292.5 C 175.8 291.1 176.7 290 177.1 290 C 177.5 290 179.5 288 181.6 285.5 L 185.3 280.9 179.4 275.6 C 168.2 265.3 145.3 242.5 143.6 239.8 C 141 235.8 139.3 236.4 131.7 244 M 309.4 243 C 308.4 244.1 304.8 247.6 301.4 250.8 C 298 253.9 294.5 258 293.6 259.8 C 292.7 261.5 291.4 263 290.8 263 C 290.2 263 284.8 267.9 278.9 273.9 L 268.1 284.8 268.6 286.8 C 268.9 287.8 271.6 291.1 274.5 294 L 279.8 299.3 282.4 298.6 C 283.9 298.2 286.3 296.5 287.8 294.8 C 289.3 293.1 298.5 283.7 308.3 273.9 C 328.8 253.3 327.7 255.8 319.6 247.5 C 312.6 240.3 312.2 240.1 309.4 243 M 108.9 267.6 C 107 269.5 104.5 271.5 103.3 272 C 102.1 272.5 100.8 274.1 100.4 275.5 L 99.8 278.1 102.4 280.9 C 103.8 282.4 105 284.1 105 284.6 C 105 285.2 106.9 287.2 109.1 289 C 111.4 290.8 119.4 298.6 126.9 306.3 C 134.4 314 143.2 322.8 146.4 325.9 C 149.6 329 162.9 342.2 176 355.3 C 189.9 369.2 200.4 379 201.5 379 C 203.2 379 215 367.6 215 366 C 215 365.4 198.7 347.8 194.4 343.7 C 193.4 342.8 174.8 324.4 153.1 303 C 131.5 281.5 113.4 264 113 264 C 112.6 264 110.8 265.6 108.9 267.6 M 189.3 301.1 C 182.1 309.4 181.4 308 205.5 332 C 228.8 355.3 228.9 355.4 231.5 351.3 C 232.5 349.8 235.3 346.5 237.6 344.2 C 242.9 338.9 243 338.2 238.3 333.8 C 236.2 331.9 233.7 329.5 232.8 328.4 C 231.8 327.4 229 324.7 226.5 322.5 C 220.3 316.9 210.5 307.4 204.2 300.7 L 198.8 295 196.7 295 L 194.5 295 189.3 301.1" fill="currentColor" />
      </g>
    </svg>
  );
}

function UsEmpireIcon({ size = 20 }: { size?: number }) {
  const draw = Math.round(size * LIBERTY_BLEED);
  const pull = (draw - size) / 2;
  return (
    <svg width={draw} height={draw} viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" style={{ display: "block", flexShrink: 0, margin: -pull }}>
      <g transform="translate(-7.587 -7.557) scale(0.03834)">
        <path fillRule="evenodd" d="M 509.8 226.3 C 508.6 228.5 507.5 269.6 506.7 342.2 C 506.5 358 506.1 371.2 505.8 371.6 C 505.4 371.9 503.7 372.9 501.8 373.8 C 497.7 375.9 495.8 379.6 496.9 383.6 C 497.5 385.7 497.2 387.5 495.8 390.3 C 494.8 392.3 494 395.5 494.2 397.4 C 494.3 399.2 493.6 402.4 492.5 404.5 C 491 407.9 490.9 409.1 491.9 412.7 C 494 420.7 493.4 465.2 491.1 469.7 C 489.8 472.3 489 476.7 488.6 484 C 487.7 498.7 486.1 506.1 483.5 508.6 C 482 510 481 512.5 480.5 515.9 C 479.8 521 478 524 475.6 524 C 473.3 524 471.2 527.4 470.8 531.5 C 470.5 535.2 470.1 535.7 465.8 537.7 C 460.8 540.1 460.3 541.1 459.8 548.5 C 459.5 552 459.1 552.7 455.8 554.2 C 451 556.5 448.7 559.9 449.6 563.4 C 450.4 566.6 448.6 569 445.5 569 C 440.3 569 438.5 572.1 438.4 581.2 C 438.3 592.2 438.4 591.9 435.6 593 C 430.6 594.9 430 597.2 429.9 616 C 429.8 636.4 429.5 637.5 424.3 638.3 C 416.3 639.6 416.9 636.1 416.3 687.6 C 415.8 735.8 415.7 737 411.4 737 C 406.8 737 404.6 790.2 409 793 C 414.7 796.6 415.5 794.3 415.8 770.6 C 416.2 743.3 413.3 746 442.7 745.3 C 471.8 744.5 469 742 469 768.6 C 469 793.8 470.2 796.9 477.6 790.5 C 479.2 789.1 481 787.5 481.7 787 C 482.6 786.3 482.9 762.6 482.9 690.7 C 483 595.3 483 595.3 480 592.1 C 474.5 586.4 476.1 578 482.6 578 C 486.8 578 487.4 579 488 587.5 C 488.5 593.9 488.7 594.5 490.9 594.8 C 494.2 595.3 495 593.6 495 585.7 C 495 579.4 495.2 578.9 497.6 578 C 503.1 575.9 505 578 505 586.4 C 505 593.7 506.5 595.9 509.9 594 C 511.7 593.1 512 592 512 586.1 C 512 578.6 512.6 577.6 517.7 576.6 C 522.7 575.5 525 578.5 525 585.9 C 525 593.4 525.6 595 528.5 595 C 531.3 595 532 593.3 532 587.2 C 532 579.2 535.3 575.3 539.4 578.3 C 542.7 580.7 542.1 587.4 538.2 591 C 532.2 596.5 534.6 598 549.3 598 C 564.3 598 564 597.8 564 609 C 564 625.7 563 633.1 560.5 634.2 C 555.9 636.3 554.3 634 553.6 624.3 C 552.8 614.1 552.2 612.7 548.9 613.2 C 546.6 613.5 546.5 613.9 546.2 622.5 C 545.8 636.4 542 637.2 542 623.4 C 542 607.6 534.9 608.1 534.2 623.9 C 533.8 632.3 533.6 633 530.8 635.5 C 524.4 641.2 528.8 642.8 551.4 642.9 C 566.2 643 569.7 643.3 571 644.5 C 572.7 646.3 572.7 728.6 571 730.4 C 567.4 734.2 567 731.1 567.1 693 C 567.1 660.3 566.9 655.6 565.5 653.4 C 560.5 645.8 559.5 655 559.1 708.5 C 559 729.7 558.5 732.8 555.6 731.7 C 554.2 731.1 554 727 554 693.1 C 554 652.5 553.6 649.7 548.9 651.5 C 546.3 652.5 546 657.1 546 695.1 C 546 733 545.7 735.5 541.5 731.4 C 540 729.8 539.9 726.4 540.2 693.1 C 540.5 653.6 540.3 652 535.5 652 C 533.8 652 533 652.6 532.9 653.8 C 532.9 654.7 532.9 671.3 532.9 690.5 C 533 727.3 532.6 732.3 529.6 732.8 C 525.7 733.6 525.6 732.6 525.9 649.8 C 526.1 609.9 525.9 608 521.6 608 C 517.6 608 517.5 609.2 517.2 698.7 C 516.9 788.8 517 790 520.9 790 C 524.3 790 524.7 787.6 525.3 766.6 C 525.7 753.6 526.3 745.3 527 744.6 C 528.3 743.3 572.7 742 575.1 743.2 C 576.6 743.9 576.8 746.8 577.2 766.2 C 577.6 790.3 578.1 793 582.6 793 C 584.5 793 585.4 792.3 586.2 790 C 587.4 786.5 587.7 786.4 590.1 788.6 C 591.8 790.1 592.1 790.1 594.5 788.6 C 596.6 787.2 597.4 787.1 598.6 788 C 599.4 788.8 601.3 789 603.4 788.6 C 606.3 788 606.9 788.3 607.5 790 C 608.9 794.5 614.4 794.8 616 790.4 C 616.5 789.2 616.9 778.2 617 766 C 617 741.8 617.1 742.2 610.4 738.4 C 608.6 737.3 608.5 736 608.9 692.9 C 609.3 642.6 609.8 645.9 600.9 641.6 C 596.5 639.5 596.5 639.5 596.2 619.8 C 596 600.1 596 600.1 593.4 598.3 C 587.4 594.1 587.3 594 587.2 583 C 587 571.7 586.9 571.5 580.7 568.5 C 576.9 566.7 576.4 566.1 575.9 562.1 C 575.3 557.5 572.3 554 569 554 C 566.3 554 564.7 551.4 565.4 548 C 566.2 543.8 564.2 540.2 559.8 537.9 C 556.9 536.4 555.8 535.1 555 532 C 554.2 529.2 553.1 527.6 551 526.5 C 547.3 524.6 546 522.2 546 517.2 C 546 512.4 544.3 507.3 542.5 506.6 C 540.3 505.8 539.1 499.5 538.4 485.5 C 537.9 475 537.4 472 535.9 469.8 C 533.8 466.8 532.7 421.5 534.5 410.8 C 535.1 407.2 534.9 405.7 533.6 403.9 C 532.7 402.5 532 399.6 532 396.7 C 532 394 531.5 390.9 530.8 389.7 C 530.2 388.5 529.6 385.2 529.5 382.3 C 529.4 376 527.5 373.5 522 372.6 C 519.8 372.3 518 371.4 518 370.7 C 518.2 332.9 516.8 228.5 516 226.6 C 514.8 223.4 511.5 223.2 509.8 226.3 M 512.7 459.5 C 511.1 468.6 512.3 518 514.2 518 C 515.1 518 515.1 470.5 514.1 461.5 C 513.5 455.5 513.5 455.5 512.7 459.5 M 523.5 477.7 C 522.3 482.8 524.1 521 525.7 522.6 C 527.6 524.4 531.5 524.4 533.4 522.5 C 535.2 520.6 535.1 519.8 530.5 500.5 C 528.7 492.8 526.6 483.9 526 480.8 C 524.7 474.2 524.5 473.9 523.5 477.7 M 499.7 484.3 C 498.6 488.8 496.2 497.2 494.3 503 C 488.5 520.4 489.9 527 498.6 522.9 C 502.2 521.1 502.8 517.6 502.9 493.3 C 503 474 502.3 472.1 499.7 484.3 M 463.7 563.7 C 462.7 564 462 564.9 462.2 565.8 C 462.5 567.4 466.1 567.5 504.9 567.4 C 546.9 567.2 552.4 566.8 548 564 C 546.5 563 467.3 562.8 463.7 563.7 M 463 579.5 C 461.7 581 462 591.9 463.4 595 C 465 598.7 461.9 600 451 600.7 C 445.2 601 439.9 601.7 439.3 602.3 C 437 604 437.5 635.7 439.8 636.9 C 443.5 638.8 444.5 636.7 445 625.8 C 445.5 614.7 446.8 612.7 450.9 616.4 C 452.5 617.9 452.8 617.9 454.5 616 C 458.8 611.3 462 614.8 462 624 C 462 632.5 463.1 635.5 466.9 637.5 C 469.3 638.7 470.1 639.9 470.6 643.1 C 471.9 651.2 472.9 645 473.6 623.9 C 474.3 602.2 474.3 602.2 470.1 599.6 C 465.8 596.8 465.1 594.8 467.5 591.6 C 471.8 586 467.4 574.3 463 579.5 M 488.6 610.7 C 487.4 613.6 487.9 782.9 489 785 C 490.3 787.4 492.3 787.6 494.4 785.4 C 496.1 783.7 496 789.5 496.4 662.9 C 496.5 608.6 496.5 609 491.5 609 C 490.2 609 488.9 609.7 488.6 610.7 M 504.1 610.4 C 502.8 611.9 501.7 767.3 502.8 779 C 503.5 785.6 506.2 788.2 509.2 785.2 C 511.2 783.2 512.1 613 510.1 610.6 C 508.5 608.6 505.6 608.5 504.1 610.4 M 433.5 646.7 C 424.3 647.3 425.3 641.7 425.1 695.9 C 425 735 425 734.6 427.5 733.7 C 429.1 733 429.2 729 429.4 676.5 C 429.5 654.5 429.5 654.5 431.7 654.2 C 435.4 653.7 435.7 656.3 436.3 695.5 C 436.7 715.9 437 732.6 437 732.8 C 437 732.9 437.5 733 438 733 C 438.6 733 439 719.5 439 694.6 C 439 660.5 439.2 656 440.6 654.5 C 445.9 649.2 446.9 655.4 446.9 696.2 C 447 718.8 447.4 732.1 448 732.5 C 448.7 732.9 449 720.3 449 695.3 C 449 651.2 449.3 649.5 454.7 653.5 C 456.7 655 456.6 653.5 456.9 710.3 C 457 731.5 457.1 733.1 458.8 732.8 C 460.9 732.4 461.9 649.8 459.8 647.3 C 458.8 646 448.8 645.8 433.5 646.7 M 564.6 748.6 C 564.3 749.5 564 758.2 564 768 C 564 787.8 564.2 789 567.5 789 C 571.3 789 572 784.5 571.6 765.9 C 571.4 756.7 571 748.8 570.6 748.1 C 569.5 746.4 565.3 746.8 564.6 748.6 M 533.2 749.2 C 532.4 750 532 755.9 532 769 C 532 788.3 532.3 790 536.2 790 C 539.5 790 540.2 785.4 539.8 767.9 C 539.4 749.2 537.8 744.6 533.2 749.2 M 549 750.4 C 547.8 753.6 548 786.2 549.3 787.8 C 550.7 789.6 553.8 789.3 555 787.1 C 556.5 784.2 556.3 750.7 554.8 749.2 C 552.8 747.2 550 747.8 549 750.4 M 455.6 750.9 C 454.7 752.2 453.6 784.4 454.4 787.3 C 455.2 790.7 458.9 791.9 460.7 789.4 C 462.3 787.2 462.6 754.9 461 751.9 C 459.9 750 456.6 749.3 455.6 750.9 M 424 752 C 420.3 754.3 421.7 789.4 425.6 790.7 C 429.3 791.9 430.1 788.2 429.9 771.6 C 429.8 753 428.6 749.1 424 752 M 440.1 752.4 C 438.1 754.8 439.1 789 441.2 790.4 C 445.6 793.1 446.5 789.4 446.1 770.7 C 445.8 751.8 444.3 747.3 440.1 752.4" />
      </g>
    </svg>
  );
}

// 서브 항목. href 가 없으면 아직 페이지가 없는 예고 항목이라 링크가 아니라 <div> 로 그린다.
// sub 는 그 서브 페이지의 본문 머리(PageHeader)가 쓸 한 줄이다. 부모와 주소가 같은
// 서브(국장 → /kadera)에는 두지 않는다 — 부모가 이미 자기 문장을 갖고 있다.
type NavChild = { label: string; href?: string; Glyph: Glyph; badge?: string; tip?: string; sub?: string };

// icon 은 Material Symbols 이름, Glyph 는 직접 그린 SVG 다(폰트에 없는 것 — 태극·성조·개미).
// 둘 중 하나만 있으면 된다. NavGlyph 가 Glyph 를 우선한다.
type NavItem = {
  href: string;
  label: string;
  icon?: string;
  Glyph?: Glyph;
  sub: string;
  badge?: string;
  children?: NavChild[];
};

/**
 * 테마 리포트를 사이드바에 **링크로** 낼 것인가. 열면(THEME_PUBLIC) 늘 그렇고, 안 연 동안은 로컬(배포 아님)에서만 —
 * 만드는 중에 사이드바에서 눌러 봐야 해서다(2026-09-22). 배포에서는 COMING_SOON 의 눌리지 않는 '준비 중' 줄로 남는다
 * (2026-08-30 에 안 연 화면이 사이드바에서 눌려 404 가 났다 — COMING_SOON 머리 주석).
 *
 * 값은 서버가 정한다 — 이 파일은 클라이언트 컴포넌트라 `process.env.VERCEL_ENV` 를 직접 읽으면 서버 렌더와 브라우저가
 * 다른 값을 봐 hydration 이 어긋난다. layout.tsx 가 AppShell 의 prop 으로 넘기고 ShellEnv 컨텍스트로 내려온다.
 */
type ShellEnv = { themeNav: boolean };
const ShellEnvContext = createContext<ShellEnv>({ themeNav: THEME_PUBLIC });
const useShellEnv = () => useContext(ShellEnvContext);

function buildNav(themeNav: boolean): NavItem[] {
  return [
  { href: "/", label: "시장 브리핑", icon: "monitoring", sub: "지표 25개로 잰 오늘의 시장 온도" },
  // 카더라가 국장·미장 둘로 갈린다. **부모는 그대로 두고 밑에 서브 항목을 단다** —
  // 부모를 국장으로 바꿔 버리면 카더라라는 이름이 사이드바에서 사라지고, 나중에 시장을
  // 하나 더 붙일 자리도 없어진다.
  //
  // 서브의 국장이 부모와 같은 /kadera 를 가리키는 건 의도다. 부모는 구역의 대문이고
  // 그 대문을 열면 나오는 게 국장이다(카테고리는 라우트가 아니다). 주소를 안 옮기는 이유는
  // 구글 색인이 이미 그 주소로 끝나 있어서다.
  {
    href: "/kadera",
    label: "카더라 리포트",
    icon: "forum",
    sub: "주식 텔레그램에서 무엇이 회자되는지",
    children: [
      { label: "국장 카더라", href: "/kadera", Glyph: KrTaegukIcon },
      {
        label: "미장 카더라",
        href: "/kadera/us",
        Glyph: LibertyIcon,
        sub: "미장에선 무엇이 화제인지",
      },
    ],
  },
  // 테마 리포트(/theme) — 카더라 바로 아래. 카더라 재료를 테마 단위로 다시 읽는 화면이라 그 옆이다.
  // ⛔ 여는 것은 `app/screen-flags.ts` 의 THEME_PUBLIC 한 줄이다. 안 연 동안 배포에서는 COMING_SOON 에 선다(themeNav 주석).
  // 카더라처럼 국장·미장 서브 항목이 달린다(국장은 부모와 같은 /theme, 미장은 /theme/us — 카더라와 같은 이유).
  ...(themeNav
    ? [
        {
          href: THEME_PAGE.href,
          label: THEME_PAGE.label,
          icon: THEME_PAGE.icon,
          sub: THEME_PAGE.sub,
          children: [
            { label: KR_THEME_SHORT, href: THEME_PAGE.href, Glyph: KrTrigramsIcon },
            { label: US_THEME_PAGE.short, href: US_THEME_PAGE.href, Glyph: UsEmpireIcon, sub: US_THEME_PAGE.sub },
          ],
        },
      ]
    : []),
  // ⚠️ COMING_SOON 에 두지 말 것 — 본문 헤더(PageHeader)가 NAV 에서 경로를 못 찾아
  // **제목 칸을 통째로 비운다.** 예고 시절에도 배지만 달아 NAV 에 뒀던 이유다.
  {
    href: "/insider",
    label: "내부자 리포트",
    // 아이콘은 첫 판이던 `contact_page` 로 돌아왔다. 한때 중절모·선글라스 실루엣을 직접 그려 넣었지만
    // (얼굴을 감춘 쪽), 옆의 폰트 아이콘들과 나란히 놓고 보면 이 그림이 낫다는 판단이다.
    icon: "contact_page",
    // ⚠️ "채팅방에 오른 미국 종목에 남은 공시 기록" 이었다. 그 부제는 이 화면의
    //    모집단을 **카더라 종목으로** 못 박는데, 실제로는 의원 축이 650종목을 보고
    //    거물 축은 보유 전부(migration_051)를 본다. 부제가 화면보다 좁으면 독자가
    //    카드 숫자를 통째로 다르게 읽는다(서학개미에서 같은 실수를 했다).
    sub: "임원과 의원, 월가 거물이 무엇을 사고팔았나",
    // ⚠️ `badge: "준비 중"` 이 여기 있었다. 이 줄을 지우는 것이 곧 **화면을 여는 것**이다
    //    (사이드바에 예고로 걸어 두고 만들었다). 2026-08-26 에 뗐다.
  },
  { href: "/mdd", label: "MDD 정밀분석", icon: "trending_down", sub: "고점에서 얼마나 내려왔고 언제 회복했을까" },
  // ⚠️ **'준비 중' 배지를 단 채로 NAV 에 있다.** 화면은 다 만들어졌지만 아직 안 열었다.
  // COMING_SOON 에 두면 본문 헤더(PageHeader)가 NAV 에서 경로를 못 찾아 **제목 칸을
  // 통째로 비운다** — 그래서 이 화면만 제목도 부제도 없이 카드부터 시작했다.
  // 열 때는 이 줄의 badge 만 지우면 된다.
  // 서학개미 장부 — 2026-09-22 부터 꺼 두었다(폐기 예정, app/screen-flags.ts SEOHAK_PUBLIC). 항목은 남긴다.
  ...(SEOHAK_PUBLIC
    ? [
      {
        href: "/seohak",
        // ⚠️ "서학개미 해부도" 였다(2026-08-27 개명). '해부'는 갈라서 안을 보여 준다는 약속인데,
        //    정작 가르던 카드(종류별 구성 · 개인과 기관 · 시작 연도별 성과)는 근거가 틀려 다 뺐다.
        //    남은 여섯 장은 언제 얼마가 나갔고 얼마나 머물렀고 지금 얼마가 됐나 하는 **기록**이라,
        //    이름을 그쪽으로 옮겼다. 주소(/seohak)는 색인이 이미 끝나 있어 그대로 둔다.
        label: "서학개미 장부",
        Glyph: AntIcon,
        // ⚠️ "한국인이…" 였다. 이 화면은 **개인**을 재는데 그 부제는 국민연금까지 포함한
        // 전 국민을 가리켰다. 화면의 모집단이 부제와 갈리면 카드 숫자를 통째로 다르게 읽는다.
        // ⚠️ 그다음엔 "얼마를 넣었고 지금 얼마가 됐나" 였는데, 그건 **카드 한 장**('원화로
        // 보면')만 가리켰다. 화면이 자라면서 매매 습관·보유기간·가계 자리·ETF 까지 들어왔다.
        // 두 축(어떻게 사고파나 · 그래서 얼마가 됐나)으로 줄여 적는다.
        sub: "개인이 미국 주식을 어떻게 사고팔고 얼마가 됐나",
        // ⚠️ `badge: "준비 중"` 이 여기 있었다. 이 줄을 지우는 것이 곧 **화면을 여는 것**이다
        //    (사이드바에 예고로 걸어 두고 만들었다). 2026-08-19 에 뗐다.
      },
      ]
    : []),
  // 2026-09-04 에 열었다. COMING_SOON 에서 옮겨 온 항목이다.
  //
  // 맨 아래에 두는 이유는 이 화면이 하루 중 07~09시에만 쓸모가 있어서다 — 종일 보는
  // 브리핑·카더라를 밀어낼 자리가 아니다.
  //
  // 아이콘 `preview` 는 창 안에 눈이 든 그림이라 이름 그대로 '미리보기' 를 가리킨다.
  // ⚠️ 후보를 **20px 실물로 띄워 놓고** 골랐다(96px 로 보면 다 그럴듯하다). 이 그림은
  // 안쪽 눈이 작아 20px 에서 점처럼 뭉치는 편이다 — 나중에 흐려 보이면 그게 이유이고,
  // 그때 볼 대안은 `pageview`(문서에 돋보기)·`double_arrow`(겹화살표)·`next_plan` 이다.
  // ⚠️ 위 내부자 리포트의 `contact_page` 와 **둘 다 네모 안에 뭐가 든 모양**이라 20px
  // 사이드바에서 제일 닮은 한 쌍이다(세로로 두 칸 떨어져 있어 지금은 견딘다).
  {
    href: "/preview",
    label: "국장 미리보기",
    icon: "preview",
    // ⚠️ **"어디에 닿나" 로 되돌리지 말 것.** 처음 문구가 "밤사이 미국에서 움직인 것이
    // 오늘 아침 어디에 닿나" 였는데, '움직인 것'·'닿나' 둘 다 두루뭉술해서 이 화면이
    // 무엇을 내주는지 안 잡혔다(2026-09-03). 이 화면이 내는 것은 **과거 기록**이다 —
    // 그런 밤 뒤에 국내가 실제로 몇 %에 열렸는지. 그래서 과거형으로 적는다.
    // ⛔ "얼마에 열릴까" 같은 미래형으로 바꾸지 말 것. 그 순간 해설이 아니라 예보가 된다.
    sub: "밤사이 미장이 크게 움직인 날 국장은 보통 얼마에 열렸나",
  },
  // 배당으로 살기(/dividend) — ⛔ 여는 것은 `app/screen-flags.ts` 의 DIVIDEND_PUBLIC 한 줄이다. 안 연 동안은
  // 아래 COMING_SOON 에 눌리지 않는 줄로 서고, 열면 여기(국장 미리보기 다음)에 선다. 데일리 노트보다
  // 위에 두는 이유는 종일 열어 두고 담는 화면이라서다 — 노트는 저녁에 한 번 읽는 글이라 맨 아래(2026-09-16).
  ...(DIVIDEND_PUBLIC ? [{ href: DIVIDEND_PAGE.href, label: DIVIDEND_PAGE.label, icon: DIVIDEND_PAGE.icon, sub: DIVIDEND_PAGE.sub }] : []),
  // 데일리 노트 — 매일 저녁 한 편의 시장 정리 글. ⛔ 여는 것은 `app/screen-flags.ts` 의
  // DAILY_PUBLIC 한 줄이다. 배당으로 살기와 같은 방식으로 안 연 동안은 COMING_SOON 에, 열면
  // 여기 맨 아래에 선다. 이름·부제·아이콘은 app/daily/copy.ts 한 곳에서 온다.
  ...(DAILY_PUBLIC ? [{ href: NOTE_PAGE.href, label: NOTE_PAGE.label, icon: NOTE_PAGE.icon, sub: NOTE_PAGE.sub }] : []),
  ];
}

// 두 값뿐이라 미리 만들어 둔다 — 렌더마다 새 배열을 만들면 useMemo 없이 참조가 흔들린다.
const NAV_ON = buildNav(true);
const NAV_OFF = buildNav(false);
const navFor = (env: ShellEnv) => (env.themeNav ? NAV_ON : NAV_OFF);

// 외부(텔레그램) 링크라 NAV 배열이 아니라 따로 둔다 — pathname 기반 active 판정 대상이
// 아니고, 새 탭으로 열려야 해서 next/link 가 아닌 <a> 를 쓴다. 사이드바와 모바일 탭바가
// 같은 값을 참조해야 두 내비게이션의 항목이 어긋나지 않는다.
//
// 라벨이 "커뮤니티 합류"가 아닌 이유: 채널이 파는 건 공간이 아니라 "오늘 장에서 무슨
// 말이 오갔나"라서, 들어가는 행위가 아니라 받는 내용을 이름으로 삼는다. 지수·브리핑
// 계열 이름은 전부 피했다 — 사이트에 이미 있는 것들이라 같은 메뉴가 둘로 보인다.
//
// aria-label 은 라벨과 따로 둔다. "오늘 뭐래?"만 읽히면 스크린리더 사용자는 이게 외부
// 텔레그램으로 나가는 링크라는 걸 알 수 없다.
const TELEGRAM = {
  href: "https://t.me/hatzze69",
  label: "오늘 뭐래?",
  aria: "오늘 뭐래? 텔레그램 채널 열기(새 탭)",
  icon: "send",
};

// 아직 페이지가 없는 예고 항목. NAV 에 넣지 않는 이유가 둘이다.
//  1. NAV 항목은 전부 <Link> 로 렌더되고 pathname 기반 active 판정을 받는다 — 갈 곳이
//     없는 항목이 그 배열에 섞이면 "링크인데 href 가 가짜"인 상태를 만들어야 한다.
//  2. NAV 는 모바일 하단 탭바와 공유한다. 탭바는 한 줄에 항목을 나눠 갖는 구조라
//     (지금 4개) 5번째가 들어가면 나머지 라벨이 눌린다. 게다가 탭바가 나오는 폭은
//     터치 화면이라 hover 가 없어 툴팁이 뜨지 않는다 — 눌러도 아무 일도 일어나지 않는
//     칸만 남는다. 그래서 사이드바에만 둔다. 페이지가 생기면 NAV 로 옮긴다.
// `after` 는 이 항목이 사이드바에서 **어느 NAV 항목 뒤에** 붙는지다. 배열 순서가 아니라
// 자리를 데이터로 적는 이유는, 미장 카더라가 국장 카더라 바로 밑에 있어야 하기 때문이다 —
// 예고 항목을 전부 목록 끝에 몰면 짝인 둘이 MDD 를 사이에 두고 떨어진다.
// 아이콘은 NAV 항목과 같은 규칙이다 — 직접 그린 Glyph 든 Material Symbols 이름(icon)이든
// 하나만 있으면 되고, NavGlyph 가 골라 그린다.
type SoonItem = { label: string; badge: string; tip: string; after: string; icon?: string; Glyph?: Glyph };
const COMING_SOON: SoonItem[] = [
  // ⭐ 국장 미리보기가 2026-09-04 에 NAV 로 옮겨 가 비었다가, 2026-09-06 에 데일리 노트가
  // 들어왔다. 그 항목은 손으로 옮기지 않는다 — DAILY_PUBLIC 하나가 여기서 빼고 NAV 에 넣는다.
  //
  // ⚠️ 다시 쓸 때 기억할 것: **배지는 표시일 뿐 아무것도 막지 않는다.** 한때 NAV 에
  // href 를 달고 배지만 '준비 중' 으로 뒀는데, 2026-08-30 에 프로덕션 사이드바에서
  // 그냥 눌려 들어가졌다. 여기(COMING_SOON)에 두면 href 필드 자체가 없어 링크가 안 생긴다.
  // 대신 본문 헤더가 NAV 에서 경로를 못 찾으므로 DEEP_PAGES 에 제목을 따로 둬야 한다.
  //
  // 테마 리포트(2026-09-19 만듦)는 여기 없다 — themeNav 가 꺼진 곳(배포)에서만 서므로 comingSoonFor 가 끼운다.
  // 배당으로 살기(2026-09-11 만듦, 09-16 열었다). 국장 미리보기 다음, 데일리 노트 앞 — NAV 와 같은 자리.
  ...(DIVIDEND_PUBLIC
    ? []
    : [{ label: DIVIDEND_PAGE.label, badge: "준비 중", tip: DIVIDEND_PAGE.tip, after: "/preview", icon: DIVIDEND_PAGE.icon }]),
  // 데일리 노트(2026-09-06 만듦, 09-07 열었다). 사이드바 맨 아래에 서는 이유는 저녁에 한 번 읽는
  // 글이라 종일 보는 브리핑·카더라 위에 둘 자리가 아니어서다.
  ...(DAILY_PUBLIC
    ? []
    : [{ label: NOTE_PAGE.label, badge: "준비 중", tip: NOTE_PAGE.tip, after: DIVIDEND_PUBLIC ? DIVIDEND_PAGE.href : "/preview", icon: NOTE_PAGE.icon }]),
];

/** 테마 리포트의 예고 줄. 카더라 리포트(와 그 서브) 바로 다음 — NAV 와 같은 자리. 링크로 낼 수 있는 곳(themeNav)에선 안 선다. */
const THEME_SOON: SoonItem = { label: THEME_PAGE.label, badge: "준비 중", tip: THEME_PAGE.tip, after: "/kadera", icon: THEME_PAGE.icon };
const comingSoonFor = (env: ShellEnv): SoonItem[] => (env.themeNav ? COMING_SOON : [THEME_SOON, ...COMING_SOON]);

/**
 * 사이드바·모바일 메뉴가 그리는 순서. NAV 항목 사이사이에 예고 항목을 끼운다.
 *
 * NAV 에 예고 항목을 넣지 않는 이유는 COMING_SOON 위 주석 참고(모바일 하단 탭바가
 * NAV 를 공유하는데 한 줄에 4개가 한계다). 그래서 목록을 하나 더 만들지 않고
 * 그릴 때 합친다 — 두 배열을 손으로 맞춰 두면 한쪽만 고쳤을 때 조용히 어긋난다.
 *
 * `after` 가 어느 NAV 항목과도 안 맞으면 조용히 사라지지 않도록 끝에 붙인다.
 */
function sidebarItems(env: ShellEnv) {
  const placed = new Set<string>();
  const rows: (
    | { kind: "nav"; item: NavItem }
    | { kind: "child"; item: NavChild; siblings: NavChild[] }
    | { kind: "soon"; item: SoonItem }
  )[] = [];
  const soons = comingSoonFor(env);
  for (const item of navFor(env)) {
    rows.push({ kind: "nav", item });
    for (const child of item.children ?? []) rows.push({ kind: "child", item: child, siblings: item.children ?? [] });
    for (const soon of soons) {
      if (soon.after === item.href) {
        rows.push({ kind: "soon", item: soon });
        placed.add(soon.label);
      }
    }
  }
  for (const soon of soons) {
    if (!placed.has(soon.label)) rows.push({ kind: "soon", item: soon });
  }
  return rows;
}

/** NAV 아이콘. 직접 그린 SVG(Glyph)가 있으면 그걸, 없으면 Material Symbols 이름을 쓴다. */
function NavGlyph({ item, size }: { item: { icon?: string; Glyph?: (p: { size?: number }) => React.ReactElement }; size: number }) {
  if (item.Glyph) return <item.Glyph size={size} />;
  return <Icon name={item.icon ?? ""} style={{ fontSize: size }} />;
}

/**
 * 서학개미 아이콘. Material Symbols 에 개미가 없어서 직접 그렸다.
 *
 * 폰트를 먼저 뒤졌다: ant · insects · termite · bug 는 글리프 자체가 없어서 리거처가
 * 안 잡히고 "ANT" 같은 대문자 텍스트로 찍힌다. 실제로 있는 벌레는 pest_control 과
 * bug_report 둘뿐인데, 둘 다 몸통이 한 덩어리인 딱정벌레라 개미로 안 읽힌다.
 *
 * 어법은 브랜드 유령(Logo.tsx 의 GhostSymbol)을 따랐다 — 외곽선이 아니라 통으로 채운
 * 덩어리에 눈을 파낸다. 해부학적으로 정확한 개미(머리·가슴·배 3마디 + 다리 6개)를
 * 외곽선으로 그려도 봤는데, 이 아이콘이 실제로 쓰이는 16px 에서는 다리가 뭉개져
 * 그냥 얼룩이 된다. 덩어리는 작아져도 형태가 남는다.
 *
 * 눈은 흰 원이 아니라 fillRule="evenodd" 로 뚫은 구멍이다. 흰색으로 칠하면 다크모드
 * 카드(어두운 배경) 위에서 흰 점이 떠 버린다. 구멍이라야 배경을 안 가린다.
 *
 * 좌표는 받은 시안을 1200px 기준으로 재서 24 박스로 옮긴 값이다(배율 0.0505).
 * 그래서 눈 지름 대 몸통 높이(32%), 더듬이 굵기(1.52) 같은 비율이 시안 그대로다.
 * 별도 transform 없이 그림이 이미 박스에 맞다 — bbox 는 x 3.8~20.0, y 1.6~22.5 다.
 *
 * 더듬이 두 가닥은 몸통 '안'(y 12.5·12.2)에서 끝난다. 몸통 테두리에 딱 맞춰 끝내면
 * 둥근 캡이 실루엣 위로 튀어나와 이음매에 혹 두 개가 생긴다. 채움 안쪽으로 밀어 넣으면
 * 캡이 덮여서 시안처럼 한 덩어리로 이어진다. 몸통 오른쪽 위 모서리(19.73)는 더듬이
 * 바깥선(18.97 + 굵기 절반 0.76)과 같은 값이라 둘이 단차 없이 만난다.
 */
/**
 * 자리보다 몇 배 크게/작게 그릴지. 여신상(LIBERTY_BLEED)과 같은 장치인데 **방향이 반대**다 —
 * 개미는 통으로 채운 덩어리라 같은 크기로 그리면 옆의 선 아이콘들보다 무겁게 보인다.
 * 넘거나 모자란 만큼은 마진으로 되메워 **줄에서 차지하는 자리는 size 그대로**다.
 */
const ANT_SHRINK = 0.85;

function AntIcon({ size = 16 }: { size?: number }) {
  const draw = Math.round(size * ANT_SHRINK);
  const pull = (draw - size) / 2;
  return (
    <svg
      width={draw}
      height={draw}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.52}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      style={{ display: "block", flexShrink: 0, margin: -pull }}
    >
      {/* 몸통 + 눈구멍(한 path 에 evenodd) */}
      <path
        fill="currentColor"
        stroke="none"
        fillRule="evenodd"
        d="M19.73 10.4 C19.9 12 19.98 13.5 19.98 15 C19.98 19.2 16.3 22.46 11.9 22.46 C7.5 22.46 3.82 19.2 3.82 15.6 C3.82 12.2 7 9.84 11.2 9.84 C14.6 9.84 18.2 9.9 19.73 10.4 Z M10.03 13.52 a2.02 2.02 0 1 0 4.04 0 a2.02 2.02 0 1 0 -4.04 0 Z"
      />
      {/* 뒤로 쓸리는 더듬이 두 가닥 */}
      <path d="M5.64 2.41 C9.8 1.55 14.8 2 17.2 4.3 C18.5 5.55 18.97 7.4 18.97 12.5" />
      <path d="M6.09 5.14 C9.8 4.4 14 4.8 16 6.6 C16.9 7.4 17.15 8.6 17.15 12.2" />
    </svg>
  );
}

/**
 * 마우스가 닿은(또는 손가락이 닿은·포커스가 온) 링크만 **전체 프리페치**로 올린다.
 *
 * Next 의 기본값은 동적 라우트에서 "가장 가까운 loading 경계까지"만 미리 받는다.
 * /kadera 는 데이터 가지가 12개라 클릭 시점에야 시작되는 서버 왕복이 0.7~1.5초다
 * (프로덕션 실측). prefetch 를 켜면 그 왕복이 클릭 **전에** 끝나 있어 즉시 열린다.
 *
 * 그런데 `prefetch` 를 항상 켜면 **카더라를 누를 생각이 없는 방문자까지 전원이**
 * 5분마다 /kadera 풀 렌더(왕복 60회)를 서버에 시킨다. Supabase 지연이 이미 불안정하고
 * 부하에 민감한 걸 확인한 터라, 사이트 전체를 느리게 만들 수 있는 거래다.
 * 그래서 **의도를 보인 사람만 비용을 낸다** — 커서가 링크에 닿는 순간 시작한다.
 *
 * 한번 켜면 끄지 않는다(목록에 쌓아 둔다). 마우스가 들락날락할 때마다 프리페치가
 * 껐다 켜지면 그게 더 낭비다.
 *
 * **쉬는 값은 `false` 다 — `undefined`(=Next 기본값) 가 아니다.** 기본값으로 두면
 * 링크가 화면에 들어오는 순간 자동으로 프리페치가 나간다. 사이드바와 모바일 메뉴가
 * 같은 목록을 두 벌 그리는 탓에 홈 한 번 여는 데 그 요청이 **9건** 붙었다.
 * 이 사이트는 전 라우트가 force-dynamic 이라(그전에는 루트 레이아웃의 cookies() 가
 * 원인이었다), 9건이 전부 CDN 을 못 타고 함수를 깨운다(x-vercel-cache: MISS). 방문
 * 1회에 함수 10회였다.
 *
 * 그 9건이 사 오던 건 loading 껍데기뿐이고(3.7~3.9KB), `/seohak` 과 `/` 는 loading.tsx
 * 가 없어 본문이 `[null,null]` 인 빈 응답이었다. 끄면서 잃는 건 hover 없이 곧바로 누른
 * 첫 탭에서 스켈레톤이 0.06~0.1초 늦게 뜨는 것뿐이다 — 뒤이어 기다릴 데이터 왕복이
 * 0.6~1.3초라 그 10분의 1 미만이다. (전부 2026-08-27 프로덕션 실측)
 *
 * ⚠️ `false` 는 뷰포트 진입뿐 아니라 **hover 프리페치까지 끈다**(Next 문서 Link#prefetch).
 * 그래서 아래 arm 이 선택이 아니라 필수다. 이 훅을 걷어내고 `prefetch={false}` 만
 * 남기면 위에 적은 클릭 왕복 0.7~1.5초가 그대로 돌아온다.
 *
 * prefetch 는 **프로덕션 빌드에서만 동작한다** — `npm run dev` 로는 확인할 수 없고
 * `npm run build:local` + `start:local`(hatzze-prod) 로 봐야 한다.
 */
function useIntentPrefetch() {
  const [armed, setArmed] = useState<string[]>([]);
  const arm = (href: string) => setArmed((a) => (a.includes(href) ? a : [...a, href]));
  return (href: string) => ({
    // true = 데이터까지 전부(클라이언트 캐시 5분). false = 자동 프리페치 없음.
    prefetch: armed.includes(href),
    onMouseEnter: () => arm(href),
    onFocus: () => arm(href),
    onTouchStart: () => arm(href),
  });
}

/**
 * 서브 항목의 현재 페이지 판정. 주소가 같거나 **그 아래**(테마 리포트의 테마 26장 · 미장 16장)면 켜진다 — 형제 중 가장 긴
 * 접두가 이기므로 /theme/us/memory 는 국장 테마(/theme)가 아니라 미장 테마(/theme/us)다. 예전엔 같은 주소일 때만 켜져서
 * 테마 한 장에 들어가면 어느 시장인지 사이드바가 말하지 않았다(2026-09-22).
 */
function childActive(siblings: NavChild[], child: NavChild, pathname: string): boolean {
  if (!child.href) return false;
  const covers = (href: string) => pathname === href || pathname.startsWith(`${href}/`);
  const best = siblings.filter((c) => c.href && covers(c.href)).sort((a, b) => b.href!.length - a.href!.length)[0];
  return best === child;
}

/** NAV 항목의 현재 페이지 판정. 사이드바와 모바일 탭바가 같은 규칙을 써야 한다. */
/**
 * **사이드바에 안 나오지만 자기 제목이 있어야 하는** 하위 페이지.
 *
 * 내부자 리포트의 전체보기 여섯 장이 그렇다. NAV 의 children 으로 넣으면 사이드바에
 * 여섯 줄이 더 생기고, 아예 안 넣으면 PageHeader 가 부모(`/insider`)를 집어
 * **두 페이지가 같은 h1** 을 갖는다(그 문제로 /kadera 와 /kadera/us 가 한 번 겹쳤다).
 *
 * 명단·문구의 원본은 `app/insider/lists.ts` 다 — 여기서 베끼지 말고 그걸 읽는다.
 * 카드와 전체보기가 같은 말을 해야 독자가 같은 자료로 읽는다.
 */
const DEEP_PAGES: Record<string, { label: string; sub: string; badge?: string }> = {
  // 데일리 노트 — 안 연 동안만. NAV 에 없으니(COMING_SOON 은 href 가 없다) 본문 헤더가 제목을
  // 못 찾아 여기서 채운다. 배지가 있어 PageJsonLd 도 안 나간다(noindex 와 맞는다).
  // 열면 DAILY_PUBLIC 이 이 항목을 빼고, NAV 의 /daily 항목이 제목을 준다.
  ...(DAILY_PUBLIC ? {} : { [NOTE_PAGE.href]: { label: NOTE_PAGE.label, sub: NOTE_PAGE.sub, badge: "준비 중" } }),
  // 배당으로 살기 — 같은 이유로 안 연 동안만.
  ...(DIVIDEND_PUBLIC ? {} : { [DIVIDEND_PAGE.href]: { label: DIVIDEND_PAGE.label, sub: DIVIDEND_PAGE.sub, badge: "준비 중" } }),
  // 테마 리포트 목록(국장·미장) — 안 연 동안만. 제목은 열린 뒤 NAV 서브 항목이 줄 것과 같게(국장 테마 · 미장 테마).
  ...(THEME_PUBLIC
    ? {}
    : {
        [THEME_PAGE.href]: { label: KR_THEME_SHORT, sub: THEME_PAGE.sub, badge: "준비 중" },
        [US_THEME_PAGE.href]: { label: US_THEME_PAGE.short, sub: US_THEME_PAGE.sub, badge: "준비 중" },
      }),
  // 테마 26장(/theme/반도체 …). 테마 이름이 곧 제목이라 셸이 h1 을 그린다 — 종목 화면(464장)은
  // 셸이 이름을 알 길이 없어 자기 h1 을 그리지만, 테마는 사전(lib/stock-themes.ts)이 정적이라
  // 여기서 안다. 안 연 동안은 '준비 중' 배지가 붙어 구조화 데이터도 안 나간다(noindex 와 맞는다).
  ...Object.fromEntries(
    THEME_NAMES.map((t) => [
      themeHref(t),
      { label: t, sub: `${t} 테마를 두고 채널에서 요즘 무슨 얘기가 도는지 봅니다`, ...(THEME_PUBLIC ? {} : { badge: "준비 중" }) },
    ]),
  ),
  // 미장 테마 16장(/theme/us/ai-semiconductor …). 국장과 이름이 겹치는 테마(금융)가 있어 부제에 시장을 적는다.
  ...Object.fromEntries(
    US_THEME_NAMES.map((t) => [
      usThemeHref(t),
      { label: t, sub: `미장 ${t} 테마를 두고 채널에서 요즘 무슨 얘기가 도는지 봅니다`, ...(THEME_PUBLIC ? {} : { badge: "준비 중" }) },
    ]),
  ),
  ...Object.fromEntries(
    INSIDER_LIST_SLUGS.map((slug) => [
      insiderListHref(slug),
      { label: INSIDER_LISTS[slug].title, sub: INSIDER_LISTS[slug].sub },
    ]),
  ),
};

function isActive(href: string, pathname: string) {
  return href === "/" ? pathname === "/" : pathname.startsWith(href);
}

function Sidebar() {
  const intentPrefetch = useIntentPrefetch();
  const env = useShellEnv();
  // `?? "/"` 는 타입 때문이다 — pages/500.tsx 가 있어 usePathname 의 타입이 string | null 이
  // 되는데(그 파일 주석 참고), app 트리에서는 null 이 안 온다. 아래 여섯 자리 모두 같다.
  const pathname = usePathname() ?? "/";
  // 로고를 감싸는 태그는 어느 페이지에서도 h1이 아니다. 사이드바는 모든 페이지가
  // 공유하는데, 검색엔진은 h1을 그 페이지의 주제로 읽는다 — 늘 h1이면 카더라·MDD가
  // 자기 제목이 아니라 "로고"를 주제로 선언하는 셈이다.
  // 예전엔 홈에서만 h1이었다("홈은 페이지 대표 제목이 따로 없다"는 이유였다). 그 전제는
  // 본문 헤더(PageHeader)가 생기면서 깨졌다 — 홈도 이제 "시장 브리핑"이라는 자기 제목을
  // 갖고, 로고까지 h1이면 홈만 h1이 둘이 된다.
  // div로 바뀌어도 보이는 건 그대로다. globals.css에 h1~h6 규칙이 없고 Tailwind
  // preflight가 font-size·font-weight를 inherit으로 되돌려서, 태그 기본값 중 남는 게 없다.
  const LogoTag = "div";
  return (
    <aside
      className="hz-sidebar"
      style={{
        width: 210,
        flexShrink: 0,
        background: C.card,
        borderRight: `1px solid var(--c-divider)`,
        padding: "28px 14px 20px",
        gap: 34,
      }}
    >
      <div style={{ padding: "0 6px" }}>
        {/* 베타 배지는 로고 우측 상단에 붙인다 — 서비스 전체가 베타라는 표시라서,
            페이지마다(예전엔 카더라 제목 옆) 다는 것보다 여기 한 곳이 맞다.
            alignItems:flex-start 로 로고 윗선에 맞춰 위첨자처럼 올린다. */}
        <LogoTag style={{ margin: 0, display: "flex", alignItems: "flex-start", gap: 5 }}>
          {/* 로고는 메인(시장 브리핑)으로 가는 링크 — 어느 페이지에서든 홈으로 돌아올 수 있게. */}
          <Link href="/" aria-label="hatzze 홈" className="hz-logo-link" style={{ display: "inline-flex" }}>
            <LogoLockup symbolSize={29} wordmarkSize={30} gap={7} />
          </Link>
          {/* 배지 크기는 '준비 중' 배지와 맞춘다(10px / padding 3-7). 서로 다른 크기면
              같은 사이드바 안에서 배지가 두 종류로 보인다. */}
          <BetaBadge logoSize={30} style={{ height: "auto", fontSize: "var(--fs-10)", padding: "3px 7px", lineHeight: 1.4 }} />
        </LogoTag>
        <p style={{ margin: "8px 0 0", fontSize: "var(--fs-11)", fontWeight: 600, color: C.sub, letterSpacing: "0.02em", lineHeight: 1.5 }}>
          {SLOGAN}
        </p>
      </div>
      <nav style={{ display: "flex", flexDirection: "column", gap: 6 }}>
        {sidebarItems(env).map((row) => {
          if (row.kind === "soon") {
            const soon = row.item;
            return (
              <div
                key={soon.label}
                className="hz-tip hz-nav-item"
                data-tip={soon.tip}
                style={{
                  display: "flex",
                  alignItems: "center",
                  gap: 12,
                  padding: "11px 12px",
                  color: C.disabled,
                  fontWeight: 600,
                  borderRadius: R.nav,
                }}
              >
                <NavGlyph item={soon} size={20} />
                {/* 배지를 라벨 '우측 상단'에 위첨자로 띄운다(로고 옆 베타 배지와 같은 어법).
                    absolute 라 배지가 행 폭 계산에서 빠져 라벨이 눌리지도, 항목이 넘치지도 않는다. */}
                <span style={{ position: "relative", display: "inline-flex" }}>
                  <span style={{ fontSize: "var(--fs-14)", whiteSpace: "nowrap" }}>{soon.label}</span>
                  <span
                    style={{
                      position: "absolute",
                      left: "100%",
                      top: -6,
                      marginLeft: 3,
                      fontSize: "var(--fs-10)",
                      fontWeight: 700,
                      lineHeight: 1.4,
                      whiteSpace: "nowrap",
                      color: C.muted,
                      background: C.chip,
                      padding: "3px 7px",
                      borderRadius: R.pill,
                    }}
                  >
                    {soon.badge}
                  </span>
                </span>
              </div>
            );
          }
          if (row.kind === "child") {
            const child = row.item;
            // 서브 행은 알약을 주지 않는다. 부모가 이미 알약을 쓰고 있어서 둘 다 칠하면
            // 한 구역에 강조가 둘이 된다 — 부모는 "여기 구역", 서브는 "이 페이지"다.
            const on = childActive(row.siblings, child, pathname);
            const rowStyle = {
              display: "flex",
              alignItems: "center",
              gap: 10,
              // 좌패딩(12) + 아이콘(20) + 간격(12) = 44 → 서브 아이콘이 **부모 라벨의 x** 에서
              // 시작한다. 예전 34 는 부모 아이콘과 라벨 사이 어중간한 자리였다(2026-09-04).
              padding: "8px 12px 8px 44px",
              borderRadius: R.nav,
            } as const;
            if (!child.href) {
              return (
                <div
                  key={child.label}
                  className="hz-tip hz-nav-item"
                  data-tip={child.tip}
                  style={{ ...rowStyle, color: C.disabled, fontWeight: 600 }}
                >
                  <child.Glyph size={18} dimmed />
                  <span style={{ position: "relative", display: "inline-flex" }}>
                    <span style={{ fontSize: "var(--fs-13)", whiteSpace: "nowrap" }}>{child.label}</span>
                    <span
                      style={{
                        position: "absolute",
                        left: "100%",
                        top: -6,
                        marginLeft: 3,
                        fontSize: "var(--fs-10)",
                        fontWeight: 700,
                        lineHeight: 1.4,
                        whiteSpace: "nowrap",
                        color: C.muted,
                        background: C.chip,
                        padding: "3px 7px",
                        borderRadius: R.pill,
                      }}
                    >
                      {child.badge}
                    </span>
                  </span>
                </div>
              );
            }
            return (
              <Link
                key={child.label}
                href={child.href}
                {...intentPrefetch(child.href)}
                className="hz-nav-item"
                style={{
                  ...rowStyle,
                  color: on ? C.blueInk : C.sub,
                  fontWeight: on ? 700 : 600,
                  textDecoration: "none",
                }}
              >
                <child.Glyph size={18} />
                <span style={{ fontSize: "var(--fs-13)" }}>{child.label}</span>
              </Link>
            );
          }
          const item = row.item;
          const active = isActive(item.href, pathname);
          return (
            <Link
              key={item.href}
              href={item.href}
              {...intentPrefetch(item.href)}
              className={`hz-nav-item${active ? " hz-nav-active" : ""}`}
              style={{
                display: "flex",
                alignItems: "center",
                gap: 12,
                padding: "11px 12px",
                // 활성 항목의 바탕·글자색은 여기 없다 — globals.css 의 .hz-nav-active 가 든다
                // (옅은 하늘색 타일 + 파란 잉크, 2026-09-04 리디자인. 8월 목업의 꽉 찬 파랑
                // 알약은 그 칸만 무거워 배너처럼 읽혔다).
                // 비활성일 때 background 를 인라인으로 두면(예전 "transparent") 인라인이
                // 우선순위에서 이겨 .hz-nav-item:hover 회색 배경이 먹히지 않는다. 값을 아예
                // 빼서 호버는 CSS 가 담당하게 한다. 활성 색도 같은 이유로 인라인에 두지 않는다.
                color: active ? undefined : C.sub,
                fontWeight: active ? 700 : 600,
                // ⚠️ 활성 항목에 파란 그림자를 주지 않는다. 0 10px 20px 이라 **바로 아래
                // 항목 위로 번져서**, 그 항목만 호버 배경이 푸르스름하게 도드라졌다
                // (카더라는 볼록하고 MDD 는 평평해 보이던 원인, 2026-08-03).
                borderRadius: R.nav,
                textDecoration: "none",
              }}
            >
              <NavGlyph item={item} size={20} />
              <span style={{ fontSize: "var(--fs-14)" }}>{item.label}</span>
            </Link>
          );
        })}
      </nav>
      {/* 예고 항목에 대하여(위 map 의 kind === "soon" 가지).
          <a href> 가 아니라 <div> 인 게 "못 누른다"의 유일한 보장이다 — href 만 뺀 <a> 는
          클릭은 안 먹어도 브라우저·확장 프로그램에 따라 링크로 취급되는 변형이 남는다.
          div 는 포커스 순서에도 안 들어간다.

          색은 C.sub(다른 항목) 보다 한 단 흐린 C.disabled 로. 호버해 보기 전에도
          "지금은 아닌 것"이 보여야 한다. */}

      <div style={{ flex: 1 }} />

      <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
        {/* 라벨은 바뀌었어도 data-ga-cta 는 "community" 그대로 둔다 — 값을 같이 바꾸면
            이름 변경 전후의 클릭수를 한 줄로 비교할 수 없다. 탭바·푸터도 같은 값이다. */}
        <a
          href={TELEGRAM.href}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={TELEGRAM.aria}
          data-ga="cta_click"
          data-ga-cta="community"
          data-ga-surface="sidebar"
          // 바탕·글자색은 .hz-tg-cta 가 쥔다. 평소엔 카드 아이콘 타일과 같은 옅은 하늘색이고
          // (꽉 찬 파랑을 늘 깔면 사이드바에서 혼자 튀어 광고 배너처럼 읽혔다, 2026-08-03),
          // 호버·포커스에서만 현재 페이지 알약과 같은 파랑으로 꽉 찬다.
          // ⚠️ 두 값을 여기 인라인으로 되돌리면 인라인이 :hover 를 이겨 호버가 죽는다.
          //
          // `hz-tip` 은 이 화면의 공용 말풍선이다. 라벨("오늘 뭐래?")이 무엇을 주는
          // 채널인지 말하지 않아서, 올려 보면 한 줄로 알려 준다.
          // ⚠️ `.hz-tip` 이 커서를 default 로 못박으므로 `.hz-tg-cta` 가 pointer 를
          //    되찾는다(globals.css). 누르면 나가는 링크인데 화살표면 거짓말이 된다.
          className="hz-tip hz-tg-cta"
          data-tip="텔레그램에서 매일 시장 요약을 해 드립니다"
          style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            gap: 8,
            // 높이 44 · 모서리 12 — 카더라 히어로의 '미장 카더라 보기' 버튼과 같은 눈금이다.
            padding: 12,
            borderRadius: R.nav,
            fontSize: "var(--fs-14)",
            fontWeight: 700,
            textDecoration: "none",
          }}
        >
          <Icon name={TELEGRAM.icon} style={{ fontSize: "var(--fs-19)" }} />
          {TELEGRAM.label}
        </a>
      </div>
    </aside>
  );
}

// 모바일 메뉴(햄버거). 예전엔 하단 탭바였는데 탑바 오른쪽으로 올렸다.
//
// 탭바를 접은 이유는 그때 적어 둔 근거가 뒤집혔기 때문이다. "항목이 4개뿐이라 한 줄에
// 다 들어간다"가 전제였는데, 서학개미 장부가 붙어 5개가 되면서 칸마다 라벨이 눌린다.
// 게다가 예고 항목은 탭 칸에 두면 눌러도 아무 일이 없어 고장으로 보인다 — 탭바가 뜨는
// 폭은 터치라 hover 가 없어서 "준비 중" 툴팁이 안 뜬다. 세로 목록은 배지를 그 자리에
// 늘 띄워 두므로 눌러 보기 전에 이유가 보인다.
//
// 열려 있는 동안에만 DOM 에 올린다. 데스크톱에서는 여는 버튼 자체가 없지만, 열어 둔
// 채로 창을 넓히는 경우가 있어 패널·백드롭의 display 는 미디어쿼리가 최종적으로 막는다.
function MobileMenu({ onClose }: { onClose: () => void }) {
  const env = useShellEnv();
  const intentPrefetch = useIntentPrefetch();
  const pathname = usePathname() ?? "/";

  const rowStyle = (active: boolean): React.CSSProperties => ({
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "13px 14px",
    borderRadius: R.nav,
    color: active ? C.blueInk : C.sub,
    fontWeight: active ? 700 : 600,
    // 사이드바와 같은 이유로 비활성 background 는 인라인에서 뺀다 — 값을 두면
    // .hz-nav-item:hover 를 인라인이 이겨버린다.
    background: active ? "var(--c-blue-tint)" : undefined,
    textDecoration: "none",
  });

  return (
    <>
      <div className="hz-menu-backdrop" onClick={onClose} />
      <nav className="hz-menu-panel" id="hz-mobile-menu" aria-label="주요 메뉴">
        {/* 예고 항목은 사이드바와 같은 이유로 <div> 다(링크가 아니고 포커스도 안 받는다).
            다만 배지는 위첨자가 아니라 라벨 옆에 나란히 둔다. 여기는 폭이 사이드바처럼
            210px 로 묶여 있지 않아 자리가 남고, 툴팁이 안 뜨는 화면이라 배지가 유일한
            설명이므로 겹쳐 두지 않고 또렷하게 보여야 한다. */}
        {sidebarItems(env).map((row) => {
          if (row.kind === "soon") {
            const soon = row.item;
            return (
              <div
                key={soon.label}
                className="hz-tip"
                data-tip={soon.tip}
                style={{ ...rowStyle(false), color: C.faint }}
              >
                <NavGlyph item={soon} size={20} />
                <span style={{ fontSize: "var(--fs-15)" }}>{soon.label}</span>
                <span
                  style={{
                    fontSize: "var(--fs-9)",
                    fontWeight: 700,
                    lineHeight: 1.4,
                    whiteSpace: "nowrap",
                    color: C.faint,
                    background: "var(--c-hover)",
                    border: `1px solid ${C.line}`,
                    padding: "2px 6px",
                    borderRadius: 999,
                  }}
                >
                  {soon.badge}
                </span>
              </div>
            );
          }
          if (row.kind === "child") {
            const child = row.item;
            const on = childActive(row.siblings, child, pathname);
            // 들여쓰기는 사이드바와 같은 뜻(부모 라벨 자리에 서브 아이콘이 선다)이지만
            // 여기는 행 높이·글자 크기가 달라서 값을 그대로 못 쓴다. rowStyle 위에 얹는다.
            const indented = { ...rowStyle(false), paddingLeft: 40, gap: 10 };
            if (!child.href) {
              return (
                <div
                  key={child.label}
                  className="hz-tip"
                  data-tip={child.tip}
                  style={{ ...indented, color: C.faint }}
                >
                  <child.Glyph size={18} dimmed />
                  <span style={{ fontSize: "var(--fs-14)" }}>{child.label}</span>
                  <span
                    style={{
                      fontSize: "var(--fs-9)",
                      fontWeight: 700,
                      lineHeight: 1.4,
                      whiteSpace: "nowrap",
                      color: C.faint,
                      background: "var(--c-hover)",
                      border: `1px solid ${C.line}`,
                      padding: "2px 6px",
                      borderRadius: 999,
                    }}
                  >
                    {child.badge}
                  </span>
                </div>
              );
            }
            return (
              <Link
                key={child.label}
                href={child.href}
                {...intentPrefetch(child.href)}
                className="hz-nav-item"
                style={{ ...indented, color: on ? C.blueInk : C.sub, fontWeight: on ? 700 : 600 }}
              >
                <child.Glyph size={18} />
                <span style={{ fontSize: "var(--fs-14)" }}>{child.label}</span>
              </Link>
            );
          }
          const item = row.item;
          const active = isActive(item.href, pathname);
          return (
            <Link
              key={item.href}
              href={item.href}
              {...intentPrefetch(item.href)}
              className={`hz-nav-item${active ? " hz-nav-active" : ""}`}
              aria-current={active ? "page" : undefined}
              style={rowStyle(active)}
            >
              <NavGlyph item={item} size={20} />
              <span style={{ fontSize: "var(--fs-15)" }}>{item.label}</span>
            </Link>
          );
        })}
        <span style={{ height: 1, background: C.line, margin: "4px 8px" }} />
        {/* 라벨은 바뀌었어도 data-ga-cta 는 "community" 그대로 둔다 — 값을 같이 바꾸면
            이름 변경 전후의 클릭수를 한 줄로 비교할 수 없다. surface 만 tabbar → menu 로
            바꾼다. 그 자리는 실제로 없어졌으니 계속 tabbar 로 적으면 거짓이 된다. */}
        <a
          href={TELEGRAM.href}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={TELEGRAM.aria}
          className="hz-nav-item"
          data-ga="cta_click"
          data-ga-cta="community"
          data-ga-surface="menu"
          style={rowStyle(false)}
        >
          <Icon name={TELEGRAM.icon} style={{ fontSize: "var(--fs-20)" }} />
          <span style={{ fontSize: "var(--fs-15)" }}>{TELEGRAM.label}</span>
        </a>
      </nav>
    </>
  );
}

/**
 * 이용자의 선택을 1년짜리 쿠키로 남긴다(테마 · 통화).
 *
 * ⚠️ 컴포넌트 **밖**에 둔다. 리액트 컴파일러 규칙이 컴포넌트 안에서 바깥 값(여기서는
 * `document.cookie`)에 대입하는 것을 막는다 — 함수로 감싸면 그 대입이 이 모듈의 일이
 * 되어 통과한다. 테마 토글이 먼저 쓰던 줄도 여기로 모았다.
 * ⚠️ 쿠키가 없으면 새로고침마다 기본값으로 돌아간다. 다음 방문 때는 layout.tsx 의
 * PREF_SCRIPT 가 이 쿠키를 읽어 페인트 전에 <html> 에 붙인다(서버는 쿠키를 안 읽는다).
 */
function remember(name: string, value: string) {
  document.cookie = `${name}=${value}; path=/; max-age=31536000; SameSite=Lax`;
}

/**
 * 통화 스위치 — **달러 금액을 내는 화면에서만** 뜬다(서학개미 장부 · 내부자 리포트).
 *
 * 서학개미는 예탁원 결제(달러)와 KRX·한국은행(원)을 함께 다뤄서, 원천이 준 통화를
 * 그대로 내면 한 페이지에 두 통화가 섞인다. 내부자 리포트는 전부 달러라 한국 독자가
 * 크기를 가늠하기 어렵다. 둘 다 기본을 원화로 두고 이 스위치로 갈아 끼운다.
 *
 * ## ⭐ 테마 토글과 같은 수다
 *
 * 뿌리 요소의 `data-cur` 하나를 바꾸면 globals.css 가 숨길 쪽을 고른다. 금액은 카드가
 * **두 벌 다 그려 놓았다**(`app/seohak/money.tsx`) — 그 화면의 카드가 거의 다 서버
 * 컴포넌트라, 통화를 리액트 상태로 두면 그 전부를 클라이언트로 끌어와야 한다.
 *
 * ⚠️ 환율(FRED)을 못 받은 날에는 카드가 달러만 낸다(`Money` 가 그렇게 떨어진다).
 * 그때 이 스위치는 눌러도 화면이 안 바뀐다. **셸은 그 사정을 모른다** — 레이아웃이
 * 페이지의 로더 결과를 볼 수 없어서다. 드문 고장 경로이고, 그날은 페이지가 어차피
 * 성한 모습이 아니라 여기서 가리지 않는다.
 */
const CUR_EVENT = "hz-cur-change";

/**
 * 지금 고른 통화. 뿌리의 `data-cur` 가 유일한 정의라(layout.tsx 의 PREF_SCRIPT 가 페인트
 * 전에 쿠키 값을 붙인다) 리액트 상태를 따로 두지 않고 그 속성을 저장소로 읽는다.
 * PcHint 와 같은 수(useSyncExternalStore) — 서버 스냅샷은 null(안 고름)이라 SSR 과
 * 하이드레이션이 어긋나지 않고, 깨어난 직후 진짜 값으로 한 번 다시 그린다.
 */
const curStore = {
  subscribe(cb: () => void) {
    window.addEventListener(CUR_EVENT, cb);
    return () => window.removeEventListener(CUR_EVENT, cb);
  },
  getSnapshot(): "krw" | "usd" | null {
    const v = document.documentElement.getAttribute("data-cur");
    return v === "krw" || v === "usd" ? v : null;
  },
};

function CurrencyToggle({ fallback }: { fallback: "krw" | "usd" }) {
  // 뿌리 속성이 없으면(아직 안 고름) 화면의 기본값이 눌린 칸이다. 이 값은 aria-pressed
  // 에만 쓴다 — **눌린 칸의 모양은 CSS 가 뿌리 속성을 보고 고른다**(mobile.css
  // .hz-cur-switch). 모양까지 리액트가 그리면 쿠키가 기본값과 다른 사람에게 첫 페인트에서
  // 엉뚱한 칸이 눌려 보였다가 하이드레이션 뒤에 옮겨 가며 깜빡인다.
  const picked = useSyncExternalStore(curStore.subscribe, curStore.getSnapshot, () => null);
  const cur = picked ?? fallback;
  const pick = (next: "krw" | "usd") => {
    if (next === cur) return;
    track("currency_toggle", { to: next });
    // ⚠️ 원화도 **값을 적는다**(예전엔 속성을 지웠다). 지우면 "안 고름"과 같아져서,
    //    달러가 기본인 화면(내부자 리포트)에서 ₩ 를 눌러도 달러로 돌아간다.
    document.documentElement.setAttribute("data-cur", next);
    remember("hz-cur", next);
    window.dispatchEvent(new Event(CUR_EVENT));
  };
  return (
    // data-cur-default: 쿠키로 고른 게 없을 때 CSS 가 눌린 칸으로 그릴 쪽. 화면 본문의
    // `[data-cur-default]`(insider 페이지들)와 같은 값이어야 눌린 칸과 보이는 금액이 맞는다.
    <span className="hz-cur-switch" role="group" aria-label="통화 바꾸기" data-cur-default={fallback}>
      {([["krw", "₩"], ["usd", "$"]] as const).map(([k, glyph]) => (
        <button key={k} type="button" onClick={() => pick(k)} aria-pressed={cur === k} data-cur={k}
                aria-label={k === "krw" ? "원화로 보기" : "달러로 보기"}>
          {glyph}
        </button>
      ))}
    </span>
  );
}

/**
 * 통화 스위치가 뜨는 화면과 그 화면의 **기본 통화**.
 *
 * ⚠️ 여기 없는 화면에 스위치를 두면 눌러도 아무것도 안 바뀌는 단추가 된다.
 * ⚠️ 기본값은 CSS 에도 적혀 있다(`[data-cur-default]`, globals.css). **두 곳이 같아야
 *    한다** — 갈리면 서버가 그린 화면과 스위치의 눌린 칸이 어긋난다.
 */
const CURRENCY_PAGES: { prefix: string; fallback: "krw" | "usd" }[] = [
  { prefix: "/seohak", fallback: "krw" },
  // 재료가 전부 미국 공시라 달러가 원본이고, 원화는 크기를 가늠하라고 얹은 것이다.
  { prefix: "/insider", fallback: "usd" },
];

/**
 * 채널 등록 신청. 국장 카더라 히어로의 큰 버튼이었는데 탑바로 옮겼다 — 그 자리에는
 * 미장으로 건너가는 통로가 서는 편이 낫다는 판단이다(kadera/page.tsx 주석 참고).
 *
 * ⚠️ 카더라 두 화면에서만 뜬다. 채널을 늘리는 일은 그 두 화면의 재료를 늘리는 것이라,
 *    브리핑이나 내부자에서 권하면 맥락이 없다.
 * ⚠️ **좁은 화면에서는 글자가 빠지고 아이콘만 남는다**(globals.css 의 .hz-topbar-cta).
 *    글자를 단 채로 두면 320px 탑바에 90px 이 더 붙어 햄버거가 도로 밀려난다 —
 *    그 잘림을 오늘 고쳤다.
 */
function ChannelRequest() {
  return (
    <a
      href="https://forms.gle/PRapNH9rz8YuF2zu9"
      target="_blank"
      rel="noopener noreferrer"
      className="hz-btn-soft hz-topbar-cta"
      title="채널 등록 신청"
      data-ga="cta_click"
      data-ga-cta="register_channel"
      data-ga-surface="topbar"
    >
      <Icon name="add_circle" style={{ fontSize: "var(--fs-15)" }} />
      <span className="hz-topbar-cta-label">채널 등록 신청</span>
    </a>
  );
}

/**
 * 머리 오른쪽 도구 묶음. 통화 스위치와 채널 등록 신청이 **테마 단추 왼쪽**이다.
 *
 * ⚠️ 통화는 **달러 금액을 내는 화면에만** 있는 개념이라 경로로 가린다. 국장 화면에
 * 두면 눌러도 아무것도 안 바뀌는 단추가 된다.
 */
function PageTools() {
  const pathname = usePathname() ?? "/";
  return (
    <>
      {/* 쿠키로 고른 게 없으면 그 화면의 기본값을 눌린 칸으로 쓴다. */}
      {(() => {
        const page = CURRENCY_PAGES.find((p) => pathname.startsWith(p.prefix));
        return page ? <CurrencyToggle key={page.prefix} fallback={page.fallback} /> : null;
      })()}
      {pathname.startsWith("/kadera") && <ChannelRequest />}
      <ThemeToggle />
    </>
  );
}

function ThemeToggle() {
  // 상태를 두지 않는다. 지금 테마는 뿌리의 data-theme 이 유일한 정의고(layout.tsx 의
  // PREF_SCRIPT 가 페인트 전에 붙인다), 아이콘은 CSS 가 그 속성을 보고 고른다
  // (mobile.css .hz-if-light/.hz-if-dark). 리액트 상태로 아이콘을 고르면 서버가 쿠키를
  // 몰라 라이트 아이콘을 내보내고, 다크 이용자에게 하이드레이션 뒤 아이콘이 바뀌어
  // 보인다. 속성을 직접 읽으면 그 깜빡임이 없다.
  const toggle = () => {
    const root = document.documentElement;
    const next = root.getAttribute("data-theme") === "dark" ? "light" : "dark";
    // 기본이 라이트라, 이 이벤트는 "다크로 바꾼 사람"이 얼마나 되는지를 재는 쪽이
    // 주된 쓸모다. 전환 방향(to)이 있어야 양쪽이 구분된다.
    track("theme_toggle", { to: next });
    // 라이트는 속성을 **지운다**(값 "light" 를 적지 않는다). CSS 가 `:root` = 라이트,
    // `[data-theme="dark"]` = 다크로 갈라 둬서 둘 다 라이트로 보이지만, PREF_SCRIPT 도
    // 라이트면 안 붙이므로 "쿠키 없음"과 "라이트 고름"의 DOM 을 같게 둔다.
    if (next === "dark") root.setAttribute("data-theme", "dark");
    else root.removeAttribute("data-theme");
    remember("hz-theme", next);
    // 모바일 주소창 색도 같이 돌린다. 이 메타는 layout.tsx 가 라이트 값으로 내보내고
    // PREF_SCRIPT 가 다크 쿠키면 바꾸므로, 여기서 안 고치면 다음 페이지 로드까지
    // 주소창만 이전 테마로 남는다.
    // 값을 또 적지 않고 방금 바뀐 data-theme 의 --c-bg 를 읽어 globals.css 를 따라간다.
    const bg = getComputedStyle(root).getPropertyValue("--c-bg").trim();
    if (bg) document.querySelector('meta[name="theme-color"]')?.setAttribute("content", bg);
  };

  return (
    <button
      onClick={toggle}
      aria-label="다크 모드 전환"
      style={{
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        width: 38,
        height: 38,
        borderRadius: R.control,
        border: `1px solid ${C.line}`,
        background: C.bg,
        color: C.sub,
        cursor: "pointer",
        flexShrink: 0,
      }}
    >
      {/* 둘 다 그려 두고 CSS 가 테마에 맞는 하나만 보인다(display:none 이라 접근성
          트리에도 하나만 남는다). 라이트에서는 달(다크로 가기), 다크에서는 해. */}
      <Icon name="dark_mode" className="hz-if-light" style={{ fontSize: "var(--fs-20)" }} />
      <Icon name="light_mode" className="hz-if-dark" style={{ fontSize: "var(--fs-20)" }} />
    </button>
  );
}


/**
 * 본문 헤더 — 페이지 제목·부제 + 테마 토글.
 *
 * 부제는 사이드바 로고 밑에 있던 문장을 옮겨 온 것이다(NAV.sub). 페이지마다 달라지므로
 * 세 화면이 공유하는 사이드바보다 여기가 맞는 자리다.
 *
 * ⚠️ 여기가 그리는 h1 이 그 페이지의 **유일한** h1 이다. 화면 쪽에 제목을 또 두면
 * 같은 글자가 두 번 뜨고 SEO 상으로도 h1 이 둘이 된다.
 *
 * NAV 에 없는 경로(개인정보처리방침·이용약관)에서는 제목 칸을 통째로 비운다. 예전엔
 * `?? NAV[0]` 로 떨어져서 법률 문서 위에 "시장 브리핑"이라는 **틀린 제목**이 붙어
 * 있었다(중복보다 나쁘다 — 문서 제목과 나란히 서서 둘 다 h1 이었다). 그 문서들은
 * 자기 제목(legal.tsx 의 DocTitle)을 갖고 있으니 여기서 보탤 것이 없다. 도구 묶음은
 * 남긴다 — 테마 토글은 어느 화면에서나 같은 자리에 있어야 한다.
 */
/** 경로의 각 조각을 encodeURIComponent 꼴로 맞춘다(이미 부호화된 조각은 한 번 풀고 다시 묶는다). */
function encodedPath(pathname: string): string {
  return pathname
    .split("/")
    .map((seg) => {
      try {
        return encodeURIComponent(decodeURIComponent(seg));
      } catch {
        return seg;
      }
    })
    .join("/");
}

function PageHeader() {
  const pathname = usePathname() ?? "/";
  const page = navFor(useShellEnv()).find((n) => isActive(n.href, pathname));
  // 서브 페이지에서는 서브의 이름을 h1 으로 쓴다. 부모(구역)의 이름을 그대로 두면
  // /kadera 와 /kadera/us 두 페이지가 **같은 h1** 을 갖는다.
  //
  // 부모와 주소가 같은 서브(국장 → /kadera)도 서브의 이름을 쓴다. 한동안 여기만
  // 비껴가 있었는데(이미 색인이 끝난 제목을 바꿀 이유가 없다는 판단), 그러면 화면에는
  // "카더라 리포트", 사이드바에는 "국장 카더라"로 같은 페이지가 두 이름을 갖는다.
  // 주소는 그대로라 색인이 끊기지 않고 제목만 갱신된다.
  const child = page?.children?.find((c) => c.href === pathname);
  // 사이드바에 안 나오는 하위 페이지(전체보기)도 자기 제목을 쓴다. 안 그러면 부모와
  // 같은 h1 이 된다.
  // 날짜별 글(/daily/2026-09-05)은 안 연 동안 NAV 에 부모가 없어 /daily 의 것을 빌린다.
  // 열고 나면 NAV 의 /daily 가 startsWith 로 잡으므로 이 가지는 안 탄다.
  // 테마 주소(/theme/반도체)는 브라우저에 따라 퍼센트 부호화된 채로도 온다. DEEP_PAGES 의 키는
  // themeHref 가 부호화한 꼴이라, 들어온 경로를 같은 꼴로 맞춘 뒤 찾는다.
  const deepKey = encodedPath(pathname);
  const deep = DEEP_PAGES[deepKey] ?? DEEP_PAGES[pathname] ?? (pathname.startsWith(`${NOTE_PAGE.href}/`) ? DEEP_PAGES[NOTE_PAGE.href] : undefined);
  const title = deep?.label ?? child?.label ?? page?.label;
  const sub = deep?.sub ?? child?.sub ?? page?.sub;
  // 배지는 부모 것이다. 서브가 물려받으면 "25개 지표" 같은 남의 표찰이 따라 붙는다.
  // 배지는 부모 것이라 서브는 물려받지 않는다. 다만 DEEP_PAGES 가 자기 배지를 들고
  // 있으면 그건 자기 것이다(아직 안 연 화면의 '준비 중').
  const badge = deep ? deep.badge : child ? undefined : page?.badge;
  /**
   * 이 화면이 무엇인지 검색엔진에 말한다(구조화 데이터).
   *
   * **왜 여기냐.** 화면 파일마다 손으로 붙이면 새 화면을 열 때 또 빠뜨린다 — 사이트맵과
   * 공유 카드를 두 번 빠뜨린 것과 같은 자리다(app/sitemap.ts 머리말). 제목·부제를 이미
   * NAV 와 DEEP_PAGES 에서 뽑고 있으니, **그 한 곳에서 같이 낸다.** 새 화면을 NAV 에
   * 넣는 순간 구조화 데이터도 저절로 따라온다.
   *
   * 클라이언트 컴포넌트지만 서버에서도 렌더되므로 첫 HTML 에 그대로 들어간다(크롤러가
   * 자바스크립트를 안 돌려도 읽는다). 실제로 그런지는 배포 뒤 `curl | grep ld+json` 으로 볼 것.
   *
   * ⛔ **아직 안 연 화면('준비 중' 배지)에는 내지 않는다.** 그 화면들은 noindex 라
   *    색인 대상이 아닌데 구조화 데이터만 내면 서로 어긋난 신호가 된다.
   * ⛔ 제목이 없는 경로(법률 문서)에도 내지 않는다. 저 화면들은 자기 h1 을 따로 갖고
   *    있고 검색 대상도 아니다.
   */
  const parent = page && page.href !== pathname ? { name: page.label, path: page.href } : null;
  /**
   * ⛔ **이 셸이 이름을 아는 화면에만 낸다.**
   *
   * `/insider/stock/SPCX` 처럼 NAV·DEEP_PAGES 어디에도 없는 주소는 부모(`/insider`)를
   * 집어 와서 제목이 "내부자 리포트" 가 된다. 그대로 구조화 데이터를 내면 두 가지가
   * 어긋난다 — 이동 경로의 마지막이 부모와 같은 이름으로 겹치고(`홈 › 내부자 리포트 ›
   * 내부자 리포트`), 화면의 `<title>`("스페이스X(SPCX) 내부자 공시")과도 다른 이름을
   * 말한다. 구조화 데이터는 그 화면을 정확히 설명해야 하므로, 이름을 모르면 **안 내는
   * 편이 맞다**(루트의 WebSite·Organization 은 그대로 남는다).
   *
   * 종목·투자자 상세는 아직 사이트맵에도 없다. 종목별 실주소 작업에서 자기 제목을
   * 갖게 되면 그때 여기 조건을 풀면 된다.
   */
  const named = Boolean(deep) || child?.href === pathname || page?.href === pathname;
  return (
    <header className="hz-page-head">
      {named && title && sub && !badge && (
        <PageJsonLd
          title={title}
          description={sub}
          path={pathname}
          trail={parent ? [parent] : []}
          kind={deep || pathname.startsWith("/kadera") || pathname === "/insider" ? "CollectionPage" : "WebPage"}
        />
      )}
{/* #267 의 가드: NAV 에 없는 경로(법률 문서)에서는 제목 칸을 통째로 비운다.
          그 안에 콘솔 리디자인의 배지를 넣는다 — 둘은 서로 독립이다. */}
      {/* ⚠️ `page` 만으로는 부족하다 — NAV 에 없고 DEEP_PAGES 에만 있는 화면
          (아직 안 연 /preview)이 제목 칸을 통째로 비운다. */}
      {(page || deep) && (
        <div style={{ display: "flex", flexDirection: "column", gap: 3, minWidth: 0 }}>
          {/* 배지는 제목과 같은 줄, 세로 가운데 정렬. baseline 으로 두면 알약의 **글자**
              밑선이 제목 밑선에 맞아, 알약 자체는 그만큼(패딩+테두리) 아래로 내려앉는다 —
              옆에 붙은 라벨이 아니라 매달린 것처럼 보였다. */}
          <div style={{ display: "flex", alignItems: "center", gap: 9, flexWrap: "wrap", minWidth: 0 }}>
            {/* 크기는 .hz-page-title(globals.css)이 든다 — 폰에서 줄여야 하는데 인라인은
                미디어쿼리를 이긴다. 2026-09-04 리디자인: 23 → 30. 구간 제목(21)·시트 머리(17)
                보다 커야 "이 화면의 이름"으로 읽힌다(23 이던 땐 구간 제목 24 보다 작았다). */}
            <h1 className="hz-page-title" style={{ margin: 0, fontWeight: 800, letterSpacing: "-.03em", color: C.ink }}>
              {title}
            </h1>
            {badge && (
              <span
                style={{
                  fontSize: "var(--fs-11)",
                  fontWeight: 700,
                  color: C.sub,
                  /* 회색 알약이니 --c-chip 이다. --c-track(막대의 빈 트랙)을 쓰고 있었는데,
                     라이트에서는 둘이 네 단위 차이라 티가 안 났지만 다크에서는 track 이
                     chip 보다 한 칸 밝아서(#3c3c47 vs #35353f) 같은 글자(--c-sub)가
                     4.08 로 떨어졌다 — 라이트의 4.98 보다 눈에 띄게 낮다. chip 위에서는
                     4.55 다. 칩 배경을 뜻하는 값이 따로 있으면 그걸 쓰는 게 맞다. */
                  background: C.chip,
                  borderRadius: R.pill,
                  padding: "3px 8px",
                  whiteSpace: "nowrap",
                }}
              >
                {badge}
              </span>
            )}
          </div>
          <p style={{ margin: 0, fontSize: "var(--fs-14)", lineHeight: 1.5, color: C.sub, wordBreak: "keep-all" }}>{sub}</p>
        </div>
      )}
      {/* 제목이 없을 때 도구가 왼쪽으로 붙지 않도록. justify-content:space-between 은
          자식이 하나면 그 하나를 왼쪽 끝에 둔다. */}
      {!page && <div style={{ flex: 1 }} />}
      {/* 오른쪽 도구는 테마 토글 하나다. 검색창·알림 벨·프로필 칩은 걷었다 —
          셋 다 기능이 없어 모양만 있는 자리였고(2026-08-03), 로그인이 없는
          서비스라 프로필 칩은 앞으로도 가리킬 대상이 없다. */}
      <div className="hz-page-tools">
        <PageTools />
      </div>
    </header>
  );
}

function TopBar({
  scrolledDown,
  menuOpen,
  onMenuToggle,
}: {
  scrolledDown: boolean;
  menuOpen: boolean;
  onMenuToggle: () => void;
}) {
  // 2026-08 리디자인: 흐르는 티커가 여기서 빠졌다. 카더라 집계는 본문 맨 위 카드 4장
  // (TickerCards)으로, 햇쩨 지수는 히어로로 갔다. 그래서 이 탑바에 남은 건 **모바일에서
  // 사이드바가 숨을 때 필요한 것들뿐**이다 — 로고·테마 토글·햄버거.
  // 데스크톱에서는 globals.css 가 통째로 display:none 한다.

  // 메뉴가 열려 있으면 감추지 않는다 — 패널이 탑바 바로 아래에 붙어 있어서, 탑바만
  // 올라가면 패널이 허공에 뜬다.
  const hidden = scrolledDown && !menuOpen;

  return (
    <header
      className={`hz-topbar${hidden ? " hz-topbar-hidden" : ""}`}
      style={{
        height: 54,
        flexShrink: 0,
        background: C.card,
        borderBottom: `1px solid ${C.line}`,
        // ⚠️ display·정렬을 인라인에 두지 않는다. 이 탑바는 데스크톱에서 통째로 숨는데,
        // 인라인 display 는 미디어쿼리를 이겨서 .hz-topbar{display:none} 이 안 먹는다
        // (여백에서 같은 함정을 이미 네 번 밟았다). 전부 globals.css 의 .hz-topbar 로.
        // 좌우 여백은 .hz-topbar 가 정한다(데스크톱 24 · 모바일 16). 인라인에 두면
        // 미디어쿼리가 못 이겨서, 폰에서 탑바 양끝이 카드(16)와 안 맞고 24 로 남는다.
      }}
    >
      {/* 모바일 전용 로고. 사이드바가 숨는 폭에서는 브랜드가 화면 어디에도 없었다.
          display 를 인라인으로 안 주는 이유는 아래 미디어쿼리가 이겨야 하기 때문이다.

          배지를 링크 **밖**에 두는 건 사이드바와 같은 이유다 — 링크에 aria-label 이
          걸려 있어서, 안에 넣으면 라벨이 내용을 덮어 스크린리더가 '베타'를 못 읽는다. */}
      <span className="hz-topbar-logo">
        <Link href="/" aria-label="hatzze 홈" className="hz-logo-link" style={{ display: "inline-flex" }}>
          <LogoLockup symbolSize={22} wordmarkSize={23} gap={6} />
        </Link>
        <BetaBadge logoSize={23} />
      </span>
      <div style={{ flex: 1 }} />
      {/* 오른쪽 묶음: 테마 토글 + 햄버거(모바일 전용). 햄버거가 화면 맨 오른쪽 끝이다.
          데스크톱에서는 햄버거가 display:none 이라 flex 에서 아예 빠지고, 남는 건
          예전과 같은 토글 하나다 — 순서를 바꿔도 데스크톱은 그대로다. */}
      <div style={{ display: "flex", alignItems: "center", gap: 8, flexShrink: 0 }}>
        <PageTools />
        <button
          type="button"
          className="hz-menu-btn"
          onClick={onMenuToggle}
          aria-label={menuOpen ? "메뉴 닫기" : "메뉴 열기"}
          aria-expanded={menuOpen}
          aria-controls="hz-mobile-menu"
          style={{
            alignItems: "center",
            justifyContent: "center",
            width: 38,
            height: 38,
            borderRadius: R.control,
            border: `1px solid ${C.line}`,
            background: C.bg,
            color: C.sub,
            cursor: "pointer",
            flexShrink: 0,
          }}
        >
          <Icon name={menuOpen ? "close" : "menu"} style={{ fontSize: "var(--fs-20)" }} />
        </button>
      </div>
    </header>
  );
}

/**
 * 새 화면을 알리는 띠. 본문 맨 위(제목 위)에 한 줄로 눕고, X 를 누르면 사라진다.
 *
 * 자리를 **탑바가 아니라 본문 흐름**으로 잡았다. 탑바(.hz-topbar)는 좁은 화면에서만
 * 그려져서 거기 넣으면 데스크톱 방문자가 이 소식을 영영 못 본다. 본문 맨 위에 두면
 * 두 폭 모두에서 보이고, 카드·헤더와 같은 상자 안이라 왼쪽 선도 저절로 맞는다.
 * 스크롤하면 같이 올라가 사라지는 것도 의도다 — 소식은 도착했을 때 한 번 보이면 된다.
 *
 * ⭐ **이미 그 화면에 있으면 안 그린다.** 보고 있는 페이지를 보러 가라고 권하는 띠는
 * 알림이 아니라 잡음이다.
 *
 * ⚠️ **문구에 숫자를 넣지 말 것.** 종목 수·채널 수는 매일 바뀌는데 이 띠는 한 번 닫으면
 * 다시 안 보여서, 틀린 값이 조용히 남는다(아래 PcHint 가 같은 이유로 숫자를 뺐다).
 *
 * 여닫이 기계는 PcHint 와 같다(useSyncExternalStore + localStorage). 이유는 그쪽 주석에.
 * ⭐ 키에 **버전을 박아 둔다** — 다음에 다른 소식으로 이 띠를 되쓸 때 키만 바꾸면
 * 예전에 닫은 사람에게도 새로 뜬다. 키를 재사용하면 그 사람들은 새 소식을 못 본다.
 */
/* ⭐ 키를 갈아 끼우는 것이 곧 **띠를 되쓰는 것**이다. 소식은 하나뿐이라 아래 여섯만 바꾸면
   이전 소식은 사라지고 새 소식만 뜬다. 예전 소식을 닫아 둔 사람도 키가 달라져 다시 본다
   (닫힌 표시는 옛 키에 남아 있을 뿐 새 키를 막지 않는다).
   ⚠️ 옛 키(`hz-news-us-kadera`)를 되쓰지 말 것 — 미장을 닫았던 사람은 새 소식을 못 본다. */
const NEWS_EVENT = "hz-news-change";

/* 문구를 이름과 꼬리로 나눈 건 **이름에만 밑줄을 긋기** 위해서다 — 띠 전체가 이미
   링크지만, 눌러서 가는 곳이 **어디인지**는 이름이 말해야 한다.

   ⚠️ **아이콘과 GA 라벨도 소식마다 갈아야 한다.** 예전엔 이 둘이 본문에 박혀 있어서
   문구만 바꾸고 넘어갔고, 내부자 소식에 미장 카더라의 자유의 여신상이 그대로 붙어
   있었다(2026-08-26). 갈아 끼울 것을 한 덩이로 모아 둔다.
   ⭐ 아이콘은 **사이드바 NAV 의 그 화면 아이콘과 같은 것**을 쓴다. 띠를 눌러 가면
     사이드바에서 방금 본 그림이 그 자리에 켜져 있어야 같은 곳이라고 읽힌다.

   ⭐ 키를 갈아 끼우는 것이 곧 **띠를 되쓰는 것**이다. 소식은 하나뿐이라 이 덩이만
   바꾸면 이전 소식은 사라지고 새 소식만 뜬다. 예전 소식을 닫아 둔 사람도 키가 달라져
   다시 본다(닫힌 표시는 옛 키에 남아 있을 뿐 새 키를 막지 않는다).
   ⚠️ 옛 키를 되쓰지 말 것 — 그 소식을 닫았던 사람은 새 소식을 못 본다.

   ⛔⛔ **국장 미리보기 띠는 화면을 여는 날 함께 켜진다.** 이 띠에는 푸터 바로가기 같은
   조건부가 없어서, 띠만 먼저 넣으면 프로덕션에서 눌러 404 로 간다. 그래서 목적지가
   열려 있을 때만 걸리도록 플래그로 가른다 — `app/screen-flags.ts` 한 줄을 true 로
   바꾸는 순간 아래가 통째로 갈린다. 문구는 이미 정해 뒀다(2026-09-04, 48자). */
/* 2026-09-16 · 배당으로 살기 오픈 소식. 목적지가 우리 화면이라 같은 탭이고, 그 화면 안에서는 띠를 안 그린다(NewsStrip).
   ⚠️ 문구에 숫자를 넣지 않는다(위 주석). 이전 소식들(내부자 → 국장 미리보기 → 데일리 노트 → 텔레그램 2판)은
   git 이력에 있다 — 되살릴 땐 키를 새로 딴다.
   ⛔ 목적지가 열려 있을 때만 건다 — DIVIDEND_PUBLIC 이 false 면 텔레그램 2판 소식이 그대로 선다(띠만 먼저 나가 404 로
   가는 일을 막는다, 국장 미리보기 때의 규칙). */
const NEWS = DIVIDEND_PUBLIC
  ? {
      key: "hz-news-dividend",
      // 머지·배포 직후부터. 화면이 같은 배포에 실려 나가니 기다릴 글이 없다.
      from: "2026-09-16T00:00:00+09:00",
      href: DIVIDEND_PAGE.href,
      name: DIVIDEND_PAGE.label,
      tail: "를 열었습니다. 배당주로 파이어 준비하는 분들을 위한 월 배당 달력입니다.",
      // 사이드바 NAV 의 그 화면 아이콘과 같은 것(paid) — 띠를 눌러 가면 사이드바에서 같은 그림이 켜진다.
      icon: DIVIDEND_PAGE.icon,
      ga: "news-dividend",
    }
  : {
      key: "hz-news-telegram-v2",
      // 이 시각 전에는 띠를 안 그린다. 새 형식의 첫 글(9/11 아침, 08:45~09:20 도착)이 나간 뒤에
      // 떠야 눌러 들어간 사람이 새 글을 본다 — 머지 당일 저녁에 뜨면 옛 글이 맨 위에 있다.
      from: "2026-09-11T09:30:00+09:00",
      href: TELEGRAM.href,
      name: "텔레그램 채널",
      tail: " 글의 퀄리티가 향상되었습니다. 더 종합적이고 더 자세하게 정리합니다.",
      // 사이드바의 채널 아이콘(send)이 아니라 '올라갔다'는 그림이다 — '새 화면'이 아니라 '나아졌다'는
      // 소식이라서. 전구(lightbulb)는 MDD 본문이 이미 써서 못 쓴다(한 화면에 같은 아이콘 두 번 금지).
      icon: "upgrade",
      ga: "news-telegram",
    };

// 모듈이 읽힐 때 한 번만 본다(렌더 안에서 Date.now() 를 부르면 React 컴파일러 린트가 막는다).
// 서버는 어차피 안 그리고(getServerSnapshot 이 false), 클라이언트는 페이지를 열 때마다 새로 읽는다.
// 개발 서버에서는 시각과 무관하게 띄운다 — 문구·아이콘을 로컬에서 보려면 날짜를 기다릴 수 없다.
const NEWS_LIVE = process.env.NODE_ENV !== "production" || Date.now() >= Date.parse(NEWS.from);

const newsStore = {
  subscribe(cb: () => void) {
    window.addEventListener(NEWS_EVENT, cb);
    return () => window.removeEventListener(NEWS_EVENT, cb);
  },
  getSnapshot() {
    if (!NEWS_LIVE) return false;
    try {
      return localStorage.getItem(NEWS.key) === null;
    } catch {
      // 사생활 보호 모드 등에서 접근이 던진다. 닫은 걸 기억 못 하면 갈 때마다 다시
      // 뜨므로, 그때는 아예 안 띄운다(PcHint 와 같은 판단).
      return false;
    }
  },
};

function NewsStrip() {
  const pathname = usePathname() ?? "/";
  const show = useSyncExternalStore(newsStore.subscribe, newsStore.getSnapshot, () => false);

  if (!show || pathname.startsWith(NEWS.href)) return null;

  const dismiss = () => {
    try {
      localStorage.setItem(NEWS.key, "1");
    } catch {}
    window.dispatchEvent(new Event(NEWS_EVENT));
  };

  return (
    <div className="hz-news" role="status">
      {/* 링크와 닫기 버튼은 **형제**로 둔다. 버튼을 링크 안에 넣으면 유효하지 않은
          마크업이고, 닫으려다 페이지가 넘어간다.

          ⭐ 눌러서 들어가도 닫은 것으로 친다. 소식을 확인한 사람에게 그 소식을 다시
          보여 줄 이유가 없다 — X 를 눌러야만 사라지면, 링크로 들어갔다 돌아온 사람은
          이미 본 띠를 또 만난다.
          ⚠️ dismiss 안의 dispatchEvent 는 동기지만 React 는 이벤트 핸들러에서 나온
          상태 변경을 핸들러가 끝난 뒤로 미룬다. 그래서 이 줄이 링크를 먼저 언마운트해
          이동을 막지 않는다(브라우저에서 눌러 확인했다). */}
      <Link
        href={NEWS.href}
        className="hz-news-link"
        data-ga-cta={NEWS.ga}
        onClick={dismiss}
        // 바깥 주소(t.me)면 새 탭. 사이드바의 채널 링크와 같은 규칙이다.
        {...(NEWS.href.startsWith("http") ? { target: "_blank", rel: "noopener noreferrer" } : {})}
      >
        <Icon name={NEWS.icon} style={{ fontSize: "var(--fs-17)", flexShrink: 0 }} />
        <span className="hz-news-text">
          {/* ⚠️ <a> 안에 <a> 를 넣을 수 없다. 바깥 링크가 이미 같은 곳으로 가므로
              여기서는 **밑줄만** 긋는다 — 눌리는 건 띠 전체다. */}
          <span className="hz-news-em">{NEWS.name}</span>
          {NEWS.tail}
        </span>
        <span className="hz-news-go">
          <span className="hz-news-go-label">보러 가기</span>
          <Icon name="arrow_forward" style={{ fontSize: "var(--fs-15)" }} />
        </span>
      </Link>
      <button type="button" onClick={dismiss} aria-label="알림 닫기" className="hz-news-x">
        <Icon name="close" style={{ fontSize: "var(--fs-17)" }} />
      </button>
    </div>
  );
}

/**
 * 첫 방문자에게 한 번 뜨는 PC 권유 토스트(모바일 전용).
 *
 * 문구는 "최적화되지 않았습니다" 류를 피했다. 모바일은 고장난 게 아니라 카드가 한 줄로
 * 늘어서서 한눈에 못 견줄 뿐인데, 첫 화면에서 사과부터 읽으면 남은 화면까지 의심하게
 * 된다. 지표 개수 같은 숫자도 일부러 안 넣었다 — 개수가 바뀌면 문구가 조용히 거짓이
 * 되는데, 이 배너는 아무도 다시 안 보는 자리라 틀린 채로 남는다.
 *
 * useEffect + setState 가 아니라 useSyncExternalStore 를 쓴다. localStorage 는 React
 * 바깥의 저장소라 이게 정석이고, 서버 스냅샷을 false 로 두면 SSR 은 아무것도 안 그려
 * 하이드레이션이 어긋나지 않는다. effect 로 하면 eslint(set-state-in-effect)에도 걸린다.
 *
 * 표시 여부는 CSS(.hz-pc-hint)가 최종 결정한다. 데스크톱에서는 DOM 에 있어도 안 보이고,
 * 그래서 데스크톱 방문자는 '봤음' 표시를 남기지 않는다 — 나중에 폰으로 들어오면 그때
 * 제대로 한 번 뜬다.
 */
const PC_HINT_KEY = "hz-pc-hint-seen";
const PC_HINT_EVENT = "hz-pc-hint-change";

const pcHintStore = {
  subscribe(cb: () => void) {
    window.addEventListener(PC_HINT_EVENT, cb);
    return () => window.removeEventListener(PC_HINT_EVENT, cb);
  },
  // 사파리 사생활 보호 모드 등에서 localStorage 접근이 던진다. 그때는 안 띄운다 —
  // 껐다는 걸 기억할 수 없으니, 띄우면 갈 때마다 다시 뜬다.
  getSnapshot() {
    try {
      return localStorage.getItem(PC_HINT_KEY) === null;
    } catch {
      return false;
    }
  },
};

function PcHint() {
  const show = useSyncExternalStore(
    pcHintStore.subscribe,
    pcHintStore.getSnapshot,
    () => false,
  );

  if (!show) return null;

  const dismiss = () => {
    try {
      localStorage.setItem(PC_HINT_KEY, "1");
    } catch {}
    window.dispatchEvent(new Event(PC_HINT_EVENT));
  };

  return (
    <div className="hz-pc-hint" role="status">
      <Icon name="desktop_windows" style={{ fontSize: "var(--fs-18)", flexShrink: 0 }} />
      <span style={{ flex: 1, wordBreak: "keep-all" }}>지표를 한눈에 보시려면 PC를 권해 드립니다.</span>
      <button
        type="button"
        onClick={dismiss}
        aria-label="닫기"
        style={{
          display: "inline-flex",
          alignItems: "center",
          justifyContent: "center",
          flexShrink: 0,
          width: 26,
          height: 26,
          borderRadius: R.control,
          border: "none",
          background: "transparent",
          color: "inherit",
          opacity: 0.7,
          cursor: "pointer",
        }}
      >
        <Icon name="close" style={{ fontSize: "var(--fs-17)" }} />
      </button>
    </div>
  );
}

/**
 * 스크롤을 내리면 true, 올리면 false. 모바일 탑바를 감췄다 꺼내는 데 쓴다.
 *
 * window 가 아니라 인자로 받은 요소를 듣는다. 이 셸은 바깥(document)이 스크롤되지
 * 않는다 — 루트가 overflow:hidden 이고 main.hz-scroll 이 자기 안에서 굴린다.
 * window 에 붙이면 이벤트가 한 번도 안 온다.
 *
 * 6px 문턱을 둔 이유는 관성 스크롤이 끝날 때 1~2px 씩 방향이 튀어서, 문턱이 없으면
 * 탑바가 깜빡이기 때문이다. 최상단(10px 이내)에서는 방향과 무관하게 늘 보여 준다.
 *
 * 데스크톱에서도 이 훅은 돌지만 보이는 변화는 없다 — 감추는 건 transform 이고,
 * 그 규칙은 560px 미디어쿼리 안에만 있다.
 */
function useScrolledDown(ref: React.RefObject<HTMLElement | null>) {
  const [scrolledDown, setScrolledDown] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    let last = el.scrollTop;
    const onScroll = () => {
      const y = el.scrollTop;
      const dy = y - last;
      if (Math.abs(dy) < 6) return;
      last = y;
      setScrolledDown(y > 10 && dy > 0);
    };
    el.addEventListener("scroll", onScroll, { passive: true });
    return () => el.removeEventListener("scroll", onScroll);
  }, [ref]);

  return scrolledDown;
}

/**
 * 한 화면 넘게 내려왔는지. '맨 위로' 버튼을 띄우는 데 쓴다.
 *
 * useScrolledDown 과 같은 이유로 window 가 아니라 인자로 받은 요소를 듣는다.
 *
 * 문턱을 px 상수가 아니라 **clientHeight(한 화면)** 로 둔다. "한 화면 넘게 내려왔으면
 * 되돌아가기가 번거롭다"가 이 버튼을 띄우는 이유인데, 그 번거로움은 절대 픽셀이 아니라
 * 화면 몇 개분인지로 정해진다 — 상수로 박으면 작은 폰에서는 늦고 태블릿에서는 이르다.
 *
 * 끄는 문턱만 80px 낮춰 둔다(히스테리시스). 같은 값으로 켜고 끄면 경계에 멈춰 선 채
 * 손가락이 떨릴 때 버튼이 깜빡인다. 켤 때는 한 화면, 끌 때는 한 화면 −80.
 *
 * 스크롤마다 setState 를 부르지만 값이 그대로면 React 가 되돌려 보내므로(bail out)
 * 실제 렌더는 경계를 넘는 순간에만 일어난다.
 */
function useScrolledPastFold(ref: React.RefObject<HTMLElement | null>) {
  const [past, setPast] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const onScroll = () => {
      const fold = el.clientHeight;
      setPast((prev) => (prev ? el.scrollTop > fold - 80 : el.scrollTop > fold));
    };
    el.addEventListener("scroll", onScroll, { passive: true });
    return () => el.removeEventListener("scroll", onScroll);
  }, [ref]);

  return past;
}

/**
 * '맨 위로' 버튼. 모바일에서 한 화면 넘게 내려가면 오른쪽 아래에 나타난다.
 *
 * **자리는 고정이다.** 같은 구석을 쓰는 PC 권유 띠(PcHint)에 맞춰 버튼을 밀어 올리지
 * 않는다 — 띠는 대개 금방 닫히는 것이라, 그것 때문에 버튼이 오르내리면 늘 있는 쪽이
 * 늘 없는 쪽에 맞춰 흔들린다. 대신 띠보다 한 층 아래에 눕혀서 겹치는 동안만 가려지게
 * 한다(자세한 근거와 실측은 globals.css 의 .hz-totop 주석).
 *
 * 표시 여부는 PcHint 와 같은 방식으로 **CSS 가 최종 결정한다**(.hz-totop 은 560px
 * 미디어쿼리 안에서만 display 가 켜진다). 여기서는 .is-on 만 붙였다 뗀다 — 마운트를
 * 껐다 켜면 나타나고 사라지는 전환을 걸 수 없다.
 */
function ToTop({
  scroller,
  show,
}: {
  scroller: React.RefObject<HTMLElement | null>;
  show: boolean;
}) {
  return (
    <button
      type="button"
      className={show ? "hz-totop is-on" : "hz-totop"}
      aria-label="맨 위로"
      onClick={() => scroller.current?.scrollTo({ top: 0 })}
    >
      <Icon name="arrow_upward" style={{ fontSize: "var(--fs-20)" }} />
    </button>
  );
}

export default function AppShell({ children, themeNav = THEME_PUBLIC }: { children: React.ReactNode; themeNav?: boolean }) {
  const env: ShellEnv = { themeNav };
  const mainRef = useRef<HTMLElement>(null);
  const scrolledDown = useScrolledDown(mainRef);
  const pastFold = useScrolledPastFold(mainRef);
  const [menuOpen, setMenuOpen] = useState(false);
  const pathname = usePathname() ?? "/";

  // 페이지를 옮기면 닫는다. 패널은 셸에 얹혀 있어 라우팅만으로는 사라지지 않는다.
  //
  // useEffect 가 아니라 렌더 중에 맞춘다. effect 로 하면 이미 그린 뒤에 다시 그리는
  // 셈이라 메뉴가 한 프레임 남고(cascading render), eslint 도 막는다. 링크마다
  // onClick 으로 닫는 방법도 있지만 그러면 뒤로가기·앞으로가기가 빠진다 — pathname 을
  // 보면 어떤 경로로 바뀌든 다 걸린다.
  const [menuPath, setMenuPath] = useState(pathname);
  if (menuPath !== pathname) {
    setMenuPath(pathname);
    setMenuOpen(false);
  }

  useEffect(() => {
    if (!menuOpen) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") setMenuOpen(false);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [menuOpen]);

  return (
    <ShellEnvContext.Provider value={env}>
    <div
      className="hz-shell"
      style={{
        // 목업은 흰 셸이 옅은 파랑(--c-page) 위에 떠 있는 구조다. 바깥 한 겹이 페이지
        // 바탕이고, 안쪽 둥근 흰 판이 사이트 전체를 담는다.
        //
        // ⚠️ **스크롤은 여전히 main 안쪽이다.** 목업의 바깥 div 는 min-height:100vh 라
        // 페이지가 통째로 스크롤되는 모양인데, 그러면 사이드바가 같이 밀려 올라간다 —
        // 사이드바 아래쪽(오늘 뭐래? · 다크모드 · 버전)을 spacer 로 바닥에 붙여 둔 설계와
        // 어긋난다. 셸을 화면 높이에 못 박고 main 만 굴리면 목업과 첫 화면이 같으면서
        // 사이드바가 고정된다. 탑바 접힘(useScrolledDown)·PcHint 도 이 전제 위에 있다.
        display: "flex",
        background: C.page,
        color: C.ink,
        // 사이트 전체를 Pretendard 하나로 통일한다. 예전엔 Plus Jakarta Sans가 앞에 있어서
        // 라틴·숫자만 그 서체로 렌더되고 한글만 Pretendard로 떨어졌다 — 한 줄 안에서 서체가
        // 갈렸다. Pretendard는 next/font/local로 자체 호스팅하므로 mac·윈도우 모두 동일하다.
        fontFamily: "var(--font-pretendard), sans-serif",
        WebkitFontSmoothing: "antialiased",
        overflow: "hidden",
      }}
    >
      {/* 위임 리스너(클릭·툴팁·스크롤). 렌더 결과가 없으므로 어디에 두어도 되지만,
          셸 최상단에 두어 "모든 페이지에 걸린다"는 게 눈에 보이게 한다. */}
      <GaEvents />
      {/* 터치 기기에서 툴팁을 탭으로 여는 리스너. 호버가 되는 기기에서는 아무것도 안 건다. */}
      <TipTap />
      <div
        className="hz-frame"
        style={{
          // 화면을 꽉 채운다. 목업이 흰 판을 여백 위에 띄운 건 **캔버스 안에서 화면을
          // 보여 주려던 액자**이지 서비스의 모양이 아니다(2026-08-03).
          // 실제 사이트에서는 그 액자가 없어야 브라우저 창 전체가 서비스가 된다.
          flex: 1,
          display: "flex",
          minWidth: 0,
          background: C.card,
          overflow: "hidden",
        }}
      >
      <Sidebar />
      <div style={{ flex: 1, display: "flex", flexDirection: "column", minWidth: 0, background: C.bg }}>
        <TopBar
          scrolledDown={scrolledDown}
          menuOpen={menuOpen}
          onMenuToggle={() => setMenuOpen((v) => !v)}
        />
        {/* 메뉴는 탑바 '안'이 아니라 형제로 둔다. 안에 두면 백드롭이 탑바의 자식이 되어
            로고·햄버거까지 덮어 버려서, 정작 닫기 버튼을 누를 수 없다. */}
        {menuOpen && <MobileMenu onClose={() => setMenuOpen(false)} />}
        {/* 여백은 globals.css 의 --hz-main-pad 가 폭에 따라 정한다(40 → 24 → 16).
            인라인이 아니라 .hz-main 클래스로 준다 — 모바일에서 탑바가 fixed 로 떠서
            흐름에서 빠지는데, 그만큼을 padding-top 으로 되메워야 첫 카드가 안 가린다.
            인라인 padding 은 미디어쿼리의 padding-top 을 이겨서 그게 안 먹는다. */}
        {/* 헤더·푸터는 main **안**에 둔다. 목업이 그 둘을 본문 흐름의 일부로 그렸고
            (스크롤하면 같이 올라간다), 그래야 좌우 여백이 카드와 한 선에 맞는다.

            폭 제한은 **안쪽 상자**가 건다. main 자체에 걸면 스크롤바가 콘텐츠를 따라
            안쪽으로 들어와 창 오른쪽 끝에서 떨어진다. 그리고 헤더·카드·푸터가 같은
            상자 안에 있어야 셋의 왼쪽 선이 어긋나지 않는다 — 카드에만 걸면 헤더와
            푸터가 화면 끝까지 늘어나 격자와 안 맞는다.

            세로 flex + gap 20 도 여기 둔다(목업 main 의 값). */}
        <main ref={mainRef} className="hz-scroll hz-main" style={{ flex: 1, overflowY: "auto" }}>
          <div
            style={{
              width: "100%",
              // 목업의 본문 폭이다(셸 1600 − 사이드바 214 − 여백 48 = 1338).
              // ⚠️ 1180 이었는데 그 값으로는 **시트가 4열이 될 수 없다.** 최소 칸 304 ×
              // 4 = 1216 이라, 화면이 아무리 넓어도 3열에서 멈춘다(목업은 넓은 화면에서
              // 4열이고 히어로도 그때 [지수][분포][브리핑 span2] = 4칸으로 딱 맞는다).
              // 좁은 화면에서는 어차피 이 상한에 안 걸리므로 3열·2열 거동은 그대로다.
              maxWidth: 1340,
              margin: "0 auto",
              display: "flex",
              flexDirection: "column",
              gap: 20,
            }}
          >
            <NewsStrip />
            <PageHeader />
            {children}
            <Footer />
          </div>
        </main>
        {/* 둘 다 오른쪽 아래 구석을 쓴다. 층은 DOM 순서가 아니라 z-index 로 못박아
            두었으므로(버튼 33 · 띠 34) 여기 순서는 읽기 좋은 대로 둔다. */}
        <ToTop scroller={mainRef} show={pastFold} />
        <PcHint />
      </div>
      </div>
    </div>
    </ShellEnvContext.Provider>
  );
}
