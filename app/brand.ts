/**
 * 브랜드 태그라인. **한 곳에 두고 나머지는 전부 여기서 가져다 쓴다.**
 *
 * 예전엔 같은 문자열이 여섯 군데(홈 제목·사이드바·og:image alt·폴백 카드·푸터·README)에
 * 손으로 복사돼 있었다. 문구를 바꿀 때 한 곳만 빠뜨려도 **아무 데도 안 깨지고 조용히
 * 어긋난다** — 사이드바는 새 문구인데 공유 카드만 옛 문구로 남는 식이다.
 *
 * 왜 app/seo.ts 가 아니라 별도 파일이냐면, **사이드바(AppShell)가 클라이언트 컴포넌트**라서다.
 * seo.ts 는 lib/data(=`server-only`)를 끌고 오므로 클라이언트에서 import 하면 빌드가 깨진다.
 * 그래서 의존이 하나도 없는 파일로 둔다(app/og-copy.ts 와 같은 이유·같은 규칙).
 *
 * README 는 코드가 아니라 이 상수를 못 따라온다. 문구를 바꿀 때 첫 줄을 같이 고칠 것.
 */
export const SLOGAN = "데이터와 여론으로 읽는 시장";
