/**
 * 테마 리포트(/theme)의 이름과 문구 **한 벌**.
 *
 * 사이드바(AppShell)·푸터·페이지 메타데이터가 같은 라벨을 쓴다(app/daily/copy.ts 와 같은 이유).
 *
 * ⚠️ 이 파일은 `server-only` 가 아니다 — 셸(클라이언트 컴포넌트)이 읽는다. DB 를 읽는
 *    것은 lib/theme-page.ts 에 있다.
 */
export const THEME_PAGE = {
  href: "/theme",
  label: "테마 리포트",
  /** 본문 머리(PageHeader)의 부제. 합쇼체 한 문장. */
  sub: "테마마다 채널에서 무슨 얘기가 도는지 봅니다",
  /** 검색 결과·공유 카드의 설명. 목록 화면(/theme)이 쓴다. 테마별 화면은 자기 문장을 쓴다. */
  description:
    "반도체·로봇·원전 같은 테마마다 주식 텔레그램 채널에서 무슨 얘기가 도는지 봅니다. 말 많은 종목과 그 까닭, 앞으로의 일정을 테마 단위로 읽습니다.",
  /** 아직 안 연 동안 사이드바 예고 항목에 붙는 툴팁. */
  tip: "테마마다 채널에서 무슨 얘기가 도는지, 어느 종목이 말 많은지 봅니다. 준비 중입니다.",
  /** Material Symbols 이름. 여러 종목을 하나로 묶은 모양이라 '테마'가 한눈에 선다. */
  icon: "category",
} as const;
