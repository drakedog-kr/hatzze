/**
 * 배당 계산기(/dividend)의 이름과 문구 **한 벌**.
 *
 * 사이드바(AppShell)·푸터·페이지 메타데이터·사이트맵이 같은 라벨을 쓴다. 데일리 노트
 * (app/daily/copy.ts)와 같은 이유로 한 곳에 둔다 — 슬로건이 여섯 곳에 손으로 복사돼
 * 있다가 어긋난 이력(app/brand.ts 머리말).
 *
 * ⚠️ 이 파일은 `server-only` 가 아니다 — 셸(클라이언트 컴포넌트)이 읽는다. DB 를 읽는
 *    것은 lib/dividend.ts 에 있다.
 *
 * ## 이름 — '배당으로 살기' (2026-09-11 확정)
 *
 * 처음엔 '배당 계산기'였다. 후보 열 개(배당 월급·배당 통장·배당 입금표·배당 주판 …) 중에서
 * 이걸 골랐다 — 파이어족이 묻는 질문("배당으로 살 수 있나")을 그대로 이름으로 삼은 것이라
 * 뜻이 가장 또렷하다. 화면이 하는 일(종목과 주수 → 1년에 얼마)은 부제가 말한다.
 *
 * ⚠️ 사이드바 14px/600 에서 76px 다(실측). '준비 중' 배지까지 한 줄에 서는 것도 확인했다 —
 *    배지 오른끝이 항목 안쪽 169px 중 165px 에서 끝나 4px 남는다. 여기서 한 글자만 더 길어지면
 *    배지가 아랫줄로 내려간다. 열면 배지가 빠져 125px 까지 여유가 생긴다.
 */
export const DIVIDEND_PAGE = {
  href: "/dividend",
  label: "배당으로 살기",
  /** 본문 머리(PageHeader)의 부제. 합쇼체 한 문장. */
  sub: "종목과 주수를 넣으면 1년에 얼마 받는지 바로 계산합니다",
  /** 검색 결과·공유 카드의 설명. */
  description:
    "가진 종목과 주수를 넣으면 1년에 배당을 얼마 받는지, 어느 달에 들어오는지 바로 계산합니다. 성향에 맞춘 배당주 바스켓도 그대로 담아 볼 수 있습니다. 예탁결제원 배당 기록과 KRX 종가를 씁니다.",
  /** 아직 안 연 동안 사이드바 예고 항목에 붙는 툴팁. */
  tip: "종목과 주수를 넣으면 1년에 배당을 얼마 받는지 바로 계산합니다. 준비 중입니다.",
  /** Material Symbols 이름. 동전 더미 — '받는 돈'이 한눈에 선다. */
  icon: "paid",
} as const;
