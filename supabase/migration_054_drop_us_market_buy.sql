-- Hatzze — 마이그레이션 054: 발굴 표(us_market_buy)를 떨군다
--
-- ## 왜 지우나
--
-- 이 표를 읽던 "채팅방 밖에서 임원이 산 종목" 카드를 2026-08-21 에 뺐다. 그 뒤로
-- **읽는 코드가 하나도 없고** 수집기도 워크플로에 붙어 있지 않아, 94행이 2026-08-12 에
-- 멈춘 채 남아 있었다. 읽는 곳 없는 표가 남아 있으면 다음 사람이 "이건 어디서 왔나"를
-- 다시 판다.
--
-- ## ⚠️ 이 표를 만든 마이그레이션(048)은 저장소에 없다
--
-- 048 은 커밋되기 전에 이 정리와 함께 지웠다. 그래서 새 DB 에는 애초에 이 표가 없고,
-- 이 파일은 **이미 만들어져 있는 운영 DB 만** 정리한다. `if exists` 인 이유가 그것이다.
--
-- ## ⭐ 되살릴 근거는 남아 있다
--
-- 실측(2026-08-18 하루치 Form 4 접수 2,056건 전수):
--
--   장내 매수(P) 221건 · 54종목
--   그중 카더라에 오른 종목 **0건**
--   카더라 종목의 매수 비율 0.7% vs 시장 전체 17%
--
-- 의미 있는 임원 매수는 거의 전부 우리 코퍼스 밖에 있다. 발굴 축을 다시 세울 이유가
-- 생기면 이 숫자가 근거다. 같은 내용이 lib/insider-data.ts 머리말에도 있다.

drop table if exists public.us_market_buy;
