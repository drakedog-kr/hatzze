"""텔레그램 종목추출용 사전 보조 데이터 — 별칭표 + 오탐 위험 종목 목록.

하이브리드 추출의 '결정적' 층에서 쓴다. KRX 상장사명(stocks 테이블)이 base이고,
여기에 (1) 은어/축약 별칭, (2) 일반 단어와 겹쳐 오탐 위험이 큰 종목명을 더한다.
실측 오탐/누락을 보고 계속 키워갈 대상이라 코드가 아니라 데이터로 분리해 둔다.

## 이 목록은 근본 수가 아니다

2026-07-30 하루에 두 번 새어 나갔다. 목록이 낡아서 한 번(하이브←하이브리드가 언급을
2배로 부풀리고 있었다), 유령을 찾는 스캔 자체에 길이 제한이 걸려 있어서 또 한 번
(미래산업←하나증권 미래산업팀). 손으로 잇는 목록은 수렴하지 않는다.

**뿌리는 매칭에 뜻이 없다는 것이다.** `미래산업팀`과 `미래산업(025560)`, `카스피해`와
`카스 +5%` 를 가르는 건 규칙이 아니라 문장의 의미다. 경계 규칙은 그 근사일 뿐이라,
일반명사와 겹치는 이름에서는 어디까지 가도 절반쯤에서 멈춘다.

문맥으로 가르는 안(언급 자리 ±60자에 시세·공시·수주 같은 '주식 이야기의 흔적'이 있나)도
재 봤는데 **폐기했다** — 유령은 76.5%가 흔적 0개인데 진짜도 26.8%가 0개라, 문턱을 걸면
진짜 언급이 27% 죽는다(흔적 1개 이상 인정 기준). 게다가 그 27% 는 낙관적인 수치다:
'진짜' 라벨을 종목코드 병기로 잡았는데 코드를 찍는 건 공시 봇·특징주라 원래 흔적이 많다.
"삼전 오늘 어때요" 같은 평범한 언급은 흔적이 0개다.

**남은 수는 이미 도는 LLM 에게 묻는 것이다.** scripts/analyze_telegram_messages.py 가
메시지 본문 600자를 배치로 Haiku 에 보내 톤·화제어를 받고 있는데, 그 프롬프트에
"종목명·기업명은 절대 넣지 마세요(종목은 따로 집계합니다)"가 박혀 있다. **뜻을 읽는 쪽이
종목만 못 보게 막혀 있는 셈이다.** 매칭이 찾은 후보를 같이 넘겨 "이 메시지가 실제로 어느
회사 얘기냐"를 받으면 사전 2,660개 전부에 걸리고, 후보 이름은 메시지당 평균 1개 미만이라
비용은 사실상 안 는다(본문을 다시 보내지 않으므로 별도 패스보다 싸다).

지금 안 넣은 이유는 하나다 — 그 스크립트가 감성 지표의 유일한 공급원인데 같은 입력으로
두 번 돌리면 낙관도가 36%↔48% 로 흔들려서, 프롬프트를 건드리고 "톤이 안 밀렸다"를 보이기가
어렵다. 잴 때는 집계 % 가 아니라 **같은 메시지를 짝지어 라벨 뒤집힘률**로 볼 것. 그래야
집계 잡음이 상쇄된다.

## 목록으로 풀 문제와 문맥으로 풀 문제는 다르다

2026-07-31 에 상장 증권사 이름이 종목으로 세어지는 걸 찾았다(SK증권이 /kadera 공개
순위 4위였는데 표본 25건 중 24건이 리서치 발행처 표기였다). 이건 **여기 있는 어느
목록으로도 풀 문제가 아니다.** 이름 자체는 멀쩡하고, 같은 이름이 같은 코퍼스에서
'발행처'와 '종목' 두 뜻으로 다 쓰인다 — 유령이 아니라 **동음이의**다.

  · 오탐: `[SK증권 반도체 한동희]` `기관 : SK증권[이동주]` `… 목표가 11,000원 - SK증권`
  · 진짜: `(유가)삼성증권 - 투자설명서` `[잠정실적]현대차증권, 2Q 매출 1조344억`

그래서 이름을 죽이는 대신 **자리를 본다**(extract_telegram_stocks.publisher_context).
증권사 27곳을 통째로 EXCLUDE_NAMES 에 넣는 안을 먼저 재 봤는데, 무작위 표본 100건에서
진짜 종목 언급이 18건이었고 그중 13건이 DART 공시였다. 증권사도 실적을 내고 공시를 하는
상장사라 그 안은 못 쓴다.

**대상 이름을 손으로 적지 않은 게 요점이다** — `이름이 '증권'으로 끝나는 상장사`라는
조건이라 새 증권사가 상장해도 따라온다. 위 목록들이 낡는 이유가 손으로 잇기 때문인데,
문맥으로 가르는 쪽은 이름을 셀 필요가 없다.

⚠️ 이건 문두에서 폐기한 '±60자 흔적' 안과 다르다. 그건 사전 2,660개 **전체**에 문턱을
걸어 진짜 언급 27%를 죽이는 안이었다. 이건 발행처로 쓰이는 게 다수인 20개 이름에만,
흔적의 유무가 아니라 **표기의 자리**(대괄호 머리·괄호 귀속·줄 끝 귀속)로 가른다.

## 오탐엔 결이 있고, 결마다 봐야 하는 자리가 다르다

여기까지 찾은 결이 일곱이다. 새 유령을 만나면 **어느 결인지부터 가릴 것** — 결을 잘못
읽으면 맞는 목록에 넣고도 안 걸린다.

  ① 유령 부분문자열  이름이 더 긴 낱말의 **앞**부분    태양←태양광    → 뒤 경계(AMBIGUOUS_NAMES)
  ② 계열 접두어      지주사가 계열사 이름의 앞부분      한화←한화세미텍  → 뒤 경계(같은 목록)
  ③ 동음이의        같은 이름이 두 뜻으로 다 쓰임      SK증권=발행처   → 표기의 자리(publisher_context)
  ④ 수식받는 머리명사 이름이 합성어의 **꼬리**로 등장   디바이스←뷰티 디바이스 → 앞 자리(HEAD_NOUN_NAMES)
  ⑤ 조사로 위장한 합성어 ①인데 이어지는 글자가 조사꼴  하이브←하이브로자임 → 뒤 꼬리 전체(JOSA)
  ⑥ 미국 티커 동형      국내 종목명과 글자가 같은 티커    STX=Seagate    → 메시지 전체(US_TICKER_COLLISION)
  ⑦ 원천이 끼워 넣은 태그 채널이 본문에 종목코드를 박아 넣음 아스트(067390)로노바 → 주석을 건너뛰고 다시(boundary_ok)

④ 를 2026-08-02 에 찾았다. ①②를 찾은 스캔이 **뒤 글자만** 셌기 때문에 ④ 는 후보에도
못 올랐다(`디바이스`는 /kadera 급부상 2위였는데 3일 창 안의 50회 중 진짜가 1건이었다).
스캔은 자기가 세는 자리만 본다 — 새 결을 찾으려면 **세는 자리를 바꿔야** 한다.

⑤ 는 2026-08-05 에 찾았다. ① 과 같은 결인데 ① 의 그물이 **한 글자만** 봐서 빠져나간
것이다. `하이브`는 ① 로 이미 AMBIGUOUS_NAMES 에 있는데도 `하이브로자임`(알테오젠의 SC
제형변경 플랫폼)이 통과했다 — 뒤 글자 `로` 가 조사라서다. 뒤 글자만 보는 한 조사와
합성어의 첫 음절을 못 가른다. JOSA 주석에 그 수단을 적었다.

⑦ 은 2026-09-07 에 찾았다. **원천 텍스트가 이미 종목 태그를 달고 있고, 그게 우리 것과
같은 오탐이라 그물을 무력화하는** 결이다. kwtok 채널이 글 끝 키워드 줄에 자기네 종목코드를
박는데(`아스트(067390)로노바` `SK(034730)YQ`), 끼어든 `(067390)` 가 이름과 뒷말 사이를
갈라 놓아 뒤 경계 검사가 '괄호가 왔다'고 보고 통과시킨다. 위 다섯 결과 결정적으로 다른
점은 **오탐의 원인이 우리 코퍼스 밖에 있다**는 것이다 — 이름 목록으로도, 문맥으로도 못
막고, 끼워 넣은 자국을 걷어 낸 뒤 원래 잣대를 다시 대야 한다. 전량 재현으로 327자리이고
(오탐위험 이름 30가지) 그중 진짜 언급은 0건이었다.
"""

from __future__ import annotations

# 은어/축약 → KRX 정식 종목명. stocks에 그 이름이 있으면 코드로 해석된다.
# (2026-07-20 코스닥 일별매매 API 승인 → stocks에 코스닥 1,821종목이 적재됐다.)
ALIASES: dict[str, str] = {
    "삼전": "삼성전자",
    "하닉": "SK하이닉스",
    "하이닉스": "SK하이닉스",
    "삼바": "삼성바이오로직스",
    "현차": "현대차",
    "기아차": "기아",
    "네이버": "NAVER",
    "셀트": "셀트리온",
    "포스코": "POSCO홀딩스",
    "포홀": "POSCO홀딩스",
    "엘지엔솔": "LG에너지솔루션",
    "엘엔솔": "LG에너지솔루션",
    "카뱅": "카카오뱅크",
    "한전": "한국전력",
    # 계열사 줄임말. 이게 없으면 앞부분만 걸려 **지주사 언급으로 잘못 센다** —
    # "한화에어로 +6.8%"가 한화(000880) 한 건이 됐다(전량 스캔 97건).
    # 뒤 글자가 조사 '에'라 AMBIGUOUS_NAMES 경계 규칙으로는 못 거른다. 애초에 오탐이
    # 아니라 계열사를 줄여 부른 진짜 언급이니, 버리지 말고 제자리에 붙이는 게 맞다.
    "한화에어로": "한화에어로스페이스",
    # ── 2026-08-06: 결 ⑤ 를 막고 나서 드러난 같은 부류 셋 ──────────────────────
    #
    # 위 `한화에어로` 와 똑같은 결인데, JOSA_TAIL_RE(결 ⑤) 가 잡아내기 전까지는
    # **지주사 언급으로 잘못 세어져** 눈에 안 띄었다. 결 ⑤ 를 막자 이번엔 그냥
    # 사라졌다 — 잘못된 태그에서 태그 없음이 됐을 뿐 제자리를 찾은 건 아니다.
    # 셋 다 **상장사**의 잘린 이름이라 붙일 코드가 있다.
    #
    #   SK이노      46자리  SK이노베이션(096770)      "SK이노, 2Q 영업익 3.5조 서프라이즈"
    #   HD현대에너지   5자리  HD현대에너지솔루션(322000)  "신재생에너지 (SK이터닉스,HD현대에너지 ,OCI홀딩스)"
    #   LG에너지솔    4자리  LG에너지솔루션(373220)     "LG에너지솔 (373220.KS) ESS·테슬라향 …"
    #
    # 매칭될 자리를 **전수(55개) 손으로 읽었다.** 다른 회사를 가리키는 자리는 0건이다.
    # 사전에 이 접두어로 시작하는 이름이 각각 하나뿐이라(위 정식명) 겨눌 곳도 하나다.
    # 긴 이름 우선 매칭이라 정식명이 통째로 적힌 자리는 별칭이 타지 않는다.
    #
    # ⚠️ **AMBIGUOUS_NAMES 에는 넣지 않았다.** 넣으면 뒤 경계 규칙이 걸려 `LG에너지솔션`
    #    (오타 난 진짜 언급)이 죽는다. `한화에어로` 도 같은 이유로 안 들어가 있다.
    #    실측 꼬리는 조사와 구두점뿐이라(SK이노: 없음 36·에 6·가 2·는 2) 위험이 없다.
    #
    # ❌ `LS에코`(→LS에코에너지 229640)도 후보였는데 **뺐다.** 4자리뿐인 데다 3이 같은 날
    #    같은 통신사 기사 하나이고, 나머지 하나(`LS에코프로에너지`)는 **다른 채널의 LLM
    #    요약이 지어낸 사명**이다. 실재하는 법인이 아닌 표기에 기대어 별칭을 세우는 셈이라
    #    근거로 못 쓴다. 화면 효과도 +1행이었다. 다시 넣고 싶으면 **다른 날·다른 채널의
    #    자리가 쌓였는지부터 셀 것.**
    "SK이노": "SK이노베이션",
    "HD현대에너지": "HD현대에너지솔루션",
    "LG에너지솔": "LG에너지솔루션",
    # ── 2026-09-19 주간 유령 점검(scripts/scan_phantom_week.py 첫 회)에서 찾은 것 ──
    #
    # 사전 이름보다 긴 표기가 본문에 오면 앞부분만 걸려 **다른 종목**이 된다. 별칭으로
    # 제자리에 붙인다(회수는 09-13~19 한 주).
    #   디아이동일   6  stocks 는 영문 'DI동일'(001530)이라 한글 표기가 디아이(003160)로 갔다
    #   케이씨티시   6  stocks 는 'KCTC'(009070). 케이씨티(089150)로 갔다
    #   포스코인터  15  포스코인터내셔널의 줄임말이 별칭 '포스코'(→POSCO홀딩스)로 갔다
    #   포스코홀딩스  3  별칭 '포스코'를 AMBIGUOUS_NAMES 에 올리면 뒤가 '홀딩스'라 죽는다
    #   두산에너빌   4  두산(000150)으로 갔다
    #   한국전력기술  2  한국전력(015760)으로 갔다
    #   한국항공우주산업 3  정식명이 'KAI' 쪽. 아래에서 한국항공우주를 AMBIGUOUS 에 올리면서
    #                   '산업'이 죽지 않게 함께 둔다
    #   에스엠엔터(테인먼트) 에스엠을 AMBIGUOUS 에 올리면서 살려 둔다
    "디아이동일": "DI동일",
    "케이씨티시": "KCTC",
    "포스코인터": "포스코인터내셔널",
    "포스코홀딩스": "POSCO홀딩스",
    "두산에너빌": "두산에너빌리티",
    "한국전력기술": "한전기술",
    "한국항공우주산업": "한국항공우주",
    "에스엠엔터테인먼트": "에스엠",
    "에스엠엔터": "에스엠",
}

# 일반 단어/영문 약자와 겹쳐 오탐 위험이 큰 종목명. 이들은 '경계 규칙'을 통과할
# 때만(앞은 비한글·비영숫자, 뒤는 조사이거나 구두점/공백) 인정한다.
# 예) "대상으로"(X) vs "대상 20%↑"(O), "SKT"(X) vs "SK가"(O).
AMBIGUOUS_NAMES: set[str] = {
    # 일반 단어와 동일한 한글 종목명
    "대상", "동양", "동방", "동서", "신원", "대현", "서원", "서연", "방림",
    "전방", "진도", "대교", "남성", "무학", "우성", "조비", "백산", "벽산",
    "한창", "대창", "원림", "세방", "선진", "삼립", "후성", "화신", "경방",
    "대덕", "대동", "신흥", "영풍", "영흥", "조흥", "한독", "우진", "보령",
    "경농", "덕성", "혜인", "서흥",
    # 코스닥 적재(2026-07-20)로 사전이 944 → 2,660개가 되며 새로 들어온 짧은 이름들.
    # 아래는 전부 실측 오탐이다(회수는 추가 전 언급 수):
    #   테스←"테스터/테스트 장비"(31), 태양←"태양광"(16), 레이←"레이어/레이저"(11),
    #   배럴←"WTI 배럴당"(8), 러셀←"러셀2000"(7), 고영←"다올 반도체 고영민"(7),
    #   서남←"서남권"(5) — 진짜 언급("서남 (294630)")은 경계 규칙을 통과해 살아남는다.
    "테스", "태양", "레이", "배럴", "러셀", "고영", "서남",
    # ── 2026-09-19 주간 유령 점검 첫 회(09-13~19 한 주 전수) ──────────────────────
    #
    # 위험이름 목록 **밖**의 이름은 뒤 경계를 아예 안 본다. 그래서 더 긴 낱말·계열사의
    # 앞부분이 그대로 통과했다(회수는 그 주의 유령/언급). 죽는 자리를 전부 읽었고, 정식명이
    # 더 긴 것은 위 ALIASES 로 살렸다(한국항공우주산업·포스코홀딩스·에스엠엔터).
    #   한국항공우주  6/26  한국항공우주연구원(항우연)
    #   오스템      6/6   오스템임플란트(다른 회사)
    #   케이프      6/6   케이프사이즈(벌크선)·케이프투자증권·케이프커내버럴
    #   에스엠      6/14  에스엠하이플러스·에스엠인더스트리·에스엠국일(SM그룹, 엔터와 무관)
    #   NICE      12/15  NICE신용평가(비상장 계열)
    #   코오롱      5/14  코오롱스페이스웍스·코오롱그룹
    #   포스코(별칭) 30/59 포스코이앤씨·포스코경영연구원·포스코그룹
    #   에스텍      3/3   에스텍시스템(다른 회사)
    #   디오        3/5   디오션자산운용·디오고 알메이다
    #   HS효성      4/21  HS효성에너지솔루션코리아·HS효성그룹
    #   덴티스·프로텍·코아스·두올·디지아이·케이엠·종근당·카프로·머큐리  1~2건씩
    #                  (덴티스트리·프로텍터·코아스파이어·두올리타스·디지아이티엑스·케이엠에프·
    #                   종근당산업·카프로락탐·머큐리아)
    #   ⚠️ 삼성생명(공익재단 8/41)은 **안 넣었다** — 전량 재현에서 `삼성생명보험`(정식명) 5건과
    #      `삼성생명등 하락` 4건이 죽었다. 유령보다 진짜 손실이 컸다.
    "한국항공우주", "오스템", "케이프", "에스엠", "NICE", "코오롱", "포스코", "에스텍",
    "디오", "HS효성", "덴티스", "프로텍", "코아스", "두올", "디지아이",
    "케이엠", "종근당", "카프로", "머큐리", "디아이", "케이씨티", "상상인",
    # 같은 부류(일반어·인명과 겹치는 2~3글자 코스닥 종목명)라 예방적으로 함께 둔다.
    # 경계 규칙은 '조사/구두점이 붙을 때만 인정'으로 좁힐 뿐이라 진짜 언급은 안 잃는다.
    "태성", "태광", "태웅", "나노", "레몬", "성우", "진영", "삼일", "삼진",
    "삼기", "삼현", "상보", "서산", "서한", "선광", "세동", "세중", "승일",
    "오공", "오아", "우양", "원익", "원풍", "유신", "일승", "청보", "채비",
    "코디", "캐리", "캐프", "하림", "하스", "하츠", "한탑", "흥국", "희림",
    "힘스", "대원", "대모", "노을", "야스", "알트", "본느", "부방", "산돌",
    "광무", "국전", "넵튠", "로젠", "미코", "천보", "파루", "피노", "제닉",
    # ── 2026-07-30 전수 스캔으로 찾은 '유령' 이름들 ──────────────────────────
    #
    # 위 목록을 손으로 이어 오다 보니 계속 낡았다. 그래서 저장된 언급 52,191건을 전부
    # 되짚어, 이름이 나온 자리마다 **뒤 글자가 조사 아닌 한글인 비율**을 셌다. 비율이
    # 높으면 그 이름은 더 긴 낱말의 앞부분으로만 등장한다는 뜻이다. 아래는 메시지 수
    # (넣기 전 → 넣은 뒤)와 사라진 문맥이다.
    #
    #   하이브 345→150  하이브리드 본딩/차/판          ← 언급이 2배 넘게 부풀어 있었다(*)
    #   컨텍  109→  1  컨텍스트 윈도우/메모리
    #   카스   65→  7  카스피해·카스트로·카스피안      ← 진짜 언급이 0건이었다
    #   아스트  55→  3  아스트라제네카(44건)
    #   딜리   20→  0  딜리버리히어로·딜리셔스         ← 진짜 언급 0건
    #   이지스  20→  9  이지스밸류리츠·이지스레지던스   ← 별개 상장사
    #   휴온스  15→  6  휴온스메디텍·휴온스랩          ← 별개 법인
    #   사람인  14→  6  "사람인데"·"사람인지"
    #   유니온  14→  7  유니온툴·유니온제약
    #   케이씨  14→  5  케이씨씨·케이씨씨글라스        ← 별개 상장사
    #   웹스   13→  8  웹스터(인명·지명)
    #   링크드   9→  0  링크드인                     ← 진짜 언급 0건
    #   골프존   6→  4  골프존홀딩스                  ← 별개 상장사
    #   삼바  330→320  삼바노바(SambaNova)           ← 별칭도 이 목록이 걸린다
    #
    # 남는 쪽이 진짜 언급인 것을 문맥으로 확인했다("하이브(352820)"·"케이씨와의 판매"·
    # "아스트(067390)"). **별칭이면 정식명이 아니라 별칭을 넣어야 한다** — 이 집합은
    # 사전의 '매칭 키' 기준으로 판정하므로 `삼바`(→삼성바이오로직스)가 그 예다.
    #
    # (*) ⚠️ **이 목록에 넣는 것만으로는 끝이 아니다.** 여기 넣은 뒤에도 `하이브`는
    #     `하이브로자임`으로 계속 샜다 — 뒤 글자 `로` 가 조사라서다(3일 창 40건 중 32건).
    #     결 ⑤ 이고, JOSA 를 형태소 목록으로 고쳐 막았다. 같은 함정이 이 블록의 다른
    #     이름에도 있었다(`레이`←레이도스, `카스`←카스나비, `아스트`←아스트로노바).
    #
    # ⚠️ **짧은 이름 전부에 이 규칙을 강제하지는 말 것.** 같은 스캔으로 재 보면 언급
    # 2.6%(1,368건)가 사라지는데 그 안에 `현대차그룹` 165 · `삼전닉스` 266(삼성전자와
    # 하이닉스를 함께 부르는 은어라 진짜다) · `코오롱티슈진` · `카카오모빌리티` 처럼
    # 살려야 할 것이 섞여 있다. 계열사·그룹 지칭을 잃는 이름은 넣지 않았다(`케이카`는
    # 죽는 것이 전부 '케이카캐피탈'이라 뺐다).
    "하이브", "컨텍", "카스", "아스트", "딜리", "링크드", "이지스",
    "휴온스", "사람인", "유니온", "케이씨", "웹스", "골프존", "삼바",
    # ── 네 글자 이상 (2026-07-30, 화면에서 잡아 왔다) ──────────────────
    #
    # 위 스캔에 `len(name) <= 3` 을 걸어 뒀던 탓에 이 둘이 후보에조차 안 올랐다. 근거 없이
    # 정한 값이었다. 제한을 풀고 다시 돌린 결과가 아래다.
    #
    #   미래산업 112→89(─23)  '하나증권 **미래산업팀** 드림' 리서치 데스크 꼬리말이 대부분
    #   한국석유  20→14(─ 6)  **한국석유공사**(비상장 공기업) — 한국석유(004090)와 남남
    #
    # 둘 다 긴 표기가 사전에 없어(`미래산업팀`·`한국석유공사`) 긴 이름 우선 매칭으로는
    # 안 걸리고, 뒤 경계 규칙이 맞는 수단인 것을 확인했다.
    #
    # ⚠️ **길이 제한을 푼다고 목록을 늘리지는 말 것.** 네 글자 이상에서 걸린 9개 중 진짜
    # 유령은 이 둘뿐이었고, 나머지는 **그 회사의 더 긴 정식 상호**였다(화승인더→화승인더스트리,
    # 계룡건설→계룡건설산업, 롯데칠성→롯데칠성음료, 포스코→포스코홀딩스, 한국앤컴퍼니→
    # 한국앤컴퍼니그룹) · 우선주(소프트센우·한화갤러리아우) · 계사 오작동(에스바이오메딕스
    # **입니다** — '입'이 조사 목록에 없다). 짧은 이름은 남의 낱말에 삼켜지지만 긴 이름은
    # 자기 자신의 긴 표기에 삼켜져서, 길이가 늘수록 이 휴리스틱의 정밀도가 무너진다.
    #
    # ⚠️ **미래산업은 이걸로 절반도 안 잡힌다**(유령 61건 중 23건). `SK증권 미래산업/미드스몰캡`
    # `미래산업: 삼미금속…` `미래산업 콜라보` 는 `/`·`:`·공백이 와서 그대로 통과한다. 애초에
    # '미래산업'이 일반명사라 경계 규칙으로 갈 수 있는 데까지가 여기다. 이건 땜질이고,
    # 근본 수는 아래 주석에 적었다.
    #
    # ※ 저 `SK증권 미래산업/미드스몰캡` 예시를 2026-07-30 에는 **미래산업 쪽 오탐으로만**
    #   읽었다. 같은 줄에서 `SK증권` 자신도 종목으로 세어지고 있었는데 못 봤다(그쪽이
    #   662건으로 더 컸다). 유령을 찾을 때 **한 줄에 오탐이 하나뿐이라고 보지 말 것.**
    #   증권사 건은 목록이 아니라 문맥으로 갈랐다 — 문두 '목록으로 풀 문제와 문맥으로
    #   풀 문제는 다르다' 참고.
    "미래산업", "한국석유",
    # ── 2026-07-30 2차: 계열 접두어 ────────────────────────────────────────
    #
    # 위 두 스캔이 놓친 결이 있다. 이 둘은 짧지도 않고 일반 단어와 겹치지도 않아 눈에 안
    # 띄었는데, **지주사 이름이 계열사 이름의 앞부분**이라 계열사가 불릴 때마다 지주사가
    # 같이 세어졌다. 전량 재현(메시지 64,885건)으로 뺀 건수와 사라지는 문맥이다.
    #
    #   한화     151  한화필리조선소·한화세미텍·한화자산운용·한화그룹
    #   HD현대   112  HD현대重·HD현대삼호·HD현대일렉·HD현대오일뱅크
    #
    # ※ 같은 스캔이 `미래산업`(23건)도 짚었지만 **바로 위 블록이 먼저 넣었다.** 같은 날 두
    #   경로가 따로 찾아 같은 숫자에 닿은 것이고(23건), 그쪽이 '경계 규칙으로는 61건 중
    #   23건까지'라는 한계까지 적어 뒀으니 근거는 그쪽에서 볼 것.
    #
    # 위 ⚠️ 경고(짧은 이름 전부에 강제하지 말 것)와 어긋나지 않는다. 이 둘은 손으로 문맥을
    # 확인해 넣은 것이고, 빠지는 263건이 전부 오탐인 것을 눈으로 봤다. 진짜 언급은 하나도
    # 안 잃는다("한화(000880)"·"한화는 오늘"처럼 뒤가 조사·구두점이면 경계 규칙을 그대로
    # 통과한다).
    #
    # ※ `한화에어로`(97건)는 여기서 버리지 않고 ALIASES 로 한화에어로스페이스에 붙였다 —
    #   뒤 글자 '에'가 조사라 경계 규칙만으로는 어차피 안 걸리고, 무엇보다 그건 오탐이
    #   아니라 **계열사를 줄여 부른 진짜 언급**이라 버릴 것이 아니라 제자리를 찾아 줄 것이다.
    "한화", "HD현대",
    # ── 2026-09-17: 화면에서 잡아 온 유령 하나 ──────────────────────────────
    #
    #   유니크(011320)  유니크레딧·유니크레디트(이탈리아 은행 UniCredit)
    #
    # 유럽 증시 시황 복붙 글이 이 태그 하나 때문에 '국장 관련 글'로 분류돼 트렌딩 칩에
    # 코스닥 자동차부품 회사가 떴다(2026-09-09 전수조사). 저장된 태그 39건을 되짚으니
    # 11건이 전부 이 은행이고(뒤가 `레`), 진짜 언급("유니크 - "·"유니크(011320)"·"유니크의")
    # 28건은 뒤가 공백·괄호·조사라 경계 규칙을 그대로 통과한다(39→28, 2026-09-17 재현).
    "유니크",
    # ── 2026-09-25: 오로라(039830) ─────────────────────────────────────────────
    #
    # 이 목록 밖이라 뒤를 안 봤다. 붙여 쓴 남의 이름이 그대로 통과했다(전량 310,520건).
    #   오로라스(雙鴻, 대만 쿨링 부품) · 오로라매크로스트래티지스(Aurora Macro Strategies)
    #   오로라(039830)이노베이션 — kwtok 태거가 박은 자국(결 ⑦). 주석 건너뛰기는 이 목록에만 걸린다
    # 이 목록에 올려 인정→거부로 바뀌는 자리는 이 셋뿐이고 전부 유령이다. 사전에 '오로라'로 시작하는
    # 다른 이름이 없고 코퍼스에 `오로라월드`(옛 사명)가 0건이라 긴 정식명을 잃을 일도 없다.
    # 띄어 쓴 `오로라 이노베이션`은 여기서 못 막는다. HOMONYM_CUES 가 맡는다.
    "오로라",
    # 영문 약자(대문자로 흔히 등장)
    "DB", "SK", "LG", "KT", "GS", "LS", "DL", "CJ", "NC", "E1", "TP", "LF",
    # 코스닥 영문명. 실측 오탐: CS←"CSP(클라우드 서비스 제공업체)"(19),
    # NEW←"Bloomberg News"/"New Business Model"(12, 대소문자 무시 매칭 부작용),
    # SG←"SG&A". 경계 규칙이 사전 키 기준으로 판정하므로 대소문자 무시에도 적용된다.
    "CS", "NEW", "SG", "EG", "KD", "KX", "YW", "E8", "3S",
    # ── 2026-08-02: 합성어의 '꼬리'로 등장하는 이름 ────────────────────────────
    #
    # 앞의 두 스캔이 못 보던 결이다. `디바이스경험(DX)`·`디바이스솔루션(DS)`은 뒤가
    # 조사 아닌 한글이라 이 목록에 넣기만 하면 걸린다. 다만 이 이름의 유령은 대부분
    # **앞**에 붙으므로(`뷰티 디바이스`) 여기 넣는 것만으로는 128건 중 24건밖에 못
    # 줄인다. 나머지는 HEAD_NOUN_NAMES 가 맡는다. 두 목록에 같이 들어가야 하는
    # 첫 이름이라 적어 둔다.
    "디바이스",
}

# 합성명사의 **머리**로 쓰여, 앞에 띄어쓴 수식어가 붙는 이름.
# (extract_telegram_stocks.modifier_context 가 이 목록만 본다.)
#
# ## AMBIGUOUS_NAMES 와 무엇이 다른가
#
# 저 목록은 이름 **뒤**를 본다. 짧은 이름이 더 긴 낱말의 앞부분으로 삼켜지는 결
# (`태양`←태양광, `하이브`←하이브리드)을 잡으려고 만든 것이라, 이름이 합성어의
# **꼬리**로만 등장하면 아무것도 못 본다. 2026-07-30 전수 스캔이 `디바이스`를
# 후보에조차 올리지 못한 이유가 이것이다 — 그 스캔은 뒤 글자만 셌다.
#
#   뷰티 디바이스 · 아나로그 디바이스 · 메모리 디바이스 · 스마트 디바이스
#   투자자 서한 · 주주 서한 · 공개 서한 ·  성인 남성 · 50대 남성
#   MSCI 신흥 · MSCI 선진 · 엔 캐리 · 오픈소스 진영 · 최대 진도 · 샘 알트만
#
# 앞 글자가 공백이라 앞 경계를 통과하고, 뒤가 구두점·조사라 뒤 경계도 통과한다.
# 경계 규칙에 이 자리를 볼 장치가 아예 없었다.
#
# ## 왜 목록이 필요한가(규칙만으로는 안 되는 이유)
#
# "앞에 띄어쓴 낱말이 있으면 거부"를 AMBIGUOUS_NAMES 전체에 걸면 994건이 죽는데,
# 그 안에 진짜가 섞인다. `최태원 SK`(107) · `수주 따낸 한화`(30) · `공급 계약
# 맺은 하이브`(14)는 앞 낱말이 수식어가 아니라 문장의 다른 성분이다. 반대로 아래
# 이름들은 **앞에 무엇이 오든** 종목이 아니다 — 진짜 언급은 예외 없이 구두점이나
# 줄머리로 열린다(`(코스닥)진영` `기업명: 서한` `9) 알트 (29.91%)` `농심(+3.1%),
# 동서(+2.1%)`). 시세·공시·특징주 표기가 전부 구분자를 쓰기 때문이다.
#
# 그래서 문턱은 비율이 아니라 **손으로 읽은 결과**다. 앞 수식어 비율이 높다고
# 다 넣지 않았다. `뉴프렉스`(54.5%) `도우인시스`(60%) `시선AI`(50%)는 비율이
# 높은데도 죽는 쪽이 전부 진짜였다("투자와 **뉴프렉스**가 보유한", "스페이스프로와
# **도우인시스**의 동반 성장", "앞서 뛴 **시선AI**·유온로보틱스"). 비율은 후보를
# 고르는 데만 쓰고, 넣을지는 문맥을 봐야 한다.
#
# 아래는 메시지 75,480건 전량 재현으로 잰 (넣기 전 → 넣은 뒤) 언급 수와 사라지는
# 문맥이다. 합계 750건이 빠지고, **이 20개 밖의 종목은 한 건도 안 변한다.**
#
#   서한   196→ 20  투자자/주주/공개 서한, CEO에게 서한 발송
#   디바이스 128→ 13  뷰티·스마트·메모리·아나로그 디바이스 (AMBIGUOUS 몫과 합산)
#   진영   103→ 36  보수/좌파/AI/오픈소스 진영
#   신흥    89→ 26  MSCI 신흥 지수, 신흥 시장·산업
#   남성    65→ 16  성인/50대/미국 남성
#   진도    46→ 12  최대 진도 7 지진, 실행 진도
#   캐리    40→  7  엔 캐리 트레이드, 단기 캐리 수익률
#   신원    34→  2  신원 미상·확인·검증
#   레이    46→ 24  레이도스·레이먼드·레이 트레이싱·X-레이(사명·보통명사 조각)
#   DB     43→ 18  Graph DB, 퇴직연금 DB, "재판매 및 DB 금지", `목표가↑"-DB`
#   한창    21→  1  "증자에 한창이다", "논의도 한창"
#   미래산업  96→ 77  x 미래산업 콜라보, [SK증권 미래산업/미드스몰캡  ← 뒤 규칙이 놓친 몫
#   선진    22→  4  MSCI 선진, 선진 시장·의료시장
#   태양    18→  2  태양 전지(판), 중국의 태양
#   NEW    43→ 30  CORP NEW·What's New(미국 상장사 접미어)
#   알트    30→ 19  샘 알트만
#   동서    24→ 13  동서 송유관(East-West Pipeline)
#   채비    16→  5  상업화/출범/공급 채비
#   대원     8→  0  혁명수비대 대원, 해경국 대원 (진짜 언급 0건)
#   하스     7→  0  인명 Haas (진짜 언급 0건)
#
# 검증은 **규칙 설계에 안 쓴 표본**으로 했다. 죽는 쪽에서 새로 40건을 뽑아 손으로 읽어
# 40건 전부 오탐이었다(진짜 언급 손실 0). 남는 쪽 40건에는 오탐이 아직 섞여 있다 —
# 이 규칙은 한쪽으로만 작동해서, 구두점 뒤에 오는 오탐("· 신흥·성장주")은 그대로 산다.
# 디바이스로 보면 128건(진짜 6~8) → 13건(진짜 9)이라 **오탐 120건이 4건으로** 준다.
# 남는 4건은 쉼표·콜론·괄호 뒤인데, 그 자리엔 진짜가 더 많아 규칙으로는 못 가른다.
#
# ⚠️ **이 목록에 이름을 넣기 전에 죽는 쪽을 눈으로 읽을 것.** 규칙이 세서, 틀린
#    이름을 넣으면 그 종목의 진짜 언급이 통째로 사라진다. 비율만 보고 넣었다면
#    위 세 이름(뉴프렉스·도우인시스·시선AI)에서 진짜 언급 32건을 날렸을 것이다.
HEAD_NOUN_NAMES: set[str] = {
    "디바이스", "서한", "진영", "신흥", "남성", "진도", "캐리", "신원",
    "미래산업", "레이", "선진", "한창", "태양", "채비", "동서", "알트",
    "대원", "하스", "DB", "NEW",
    # 2026-09-07 추가. 유니온(000910)이 /kadera 급부상 1위였는데, 3일 창의 언급 5건이
    # 전부 한 미국 시황 요약의 복붙이었다("트랜스 유니온(-5.93%)" = TransUnion).
    # 앞 글자가 공백이라 뒤 경계 규칙도 앞 낱말 검사도 그냥 통과했다 — 결 ④ 다.
    # 전량 재현으로 죽는 12자리를 손으로 읽어 **전부 외국 회사**였다(트랜스 유니온 5 ·
    # 유니온 일렉트릭 2 · 퍼스트 유니온 · 유니온 파시픽 · 유니온 애비뉴 · 타이완 유니온 ·
    # 유니온 일렉트리카). 진짜 언급 손실 0건이다.
    #
    # ⚠️ 이 이름은 절반만 잡힌다. 쉼표·줄머리 뒤에 오는 `유니온 퍼시픽(UNP)`·`유니온
    #    카운티`·`유니온 반도체` 10여 건은 그대로 산다 — 위 주석이 경고한 그 자리다.
    "유니온",
    # 2026-09-19. 나노(187790)가 급부상 카드에 사흘 올랐는데 17건 중 11건이 일반어였다.
    # 앞 자리 둘(`미니·나노`·`바이오-나노`)은 이 규칙이 잡는다. 뒤 자리(`나노 멤브레인`·
    # `나노 약물전달체`, 띄어쓴 합성어)와 붙임표(`나노-MRI`)는 못 잡는다 — 뒤 경계는 붙은
    # 글자만 본다. 그 셋은 HOMONYM_CUES 의 단서(MRI·멤브레인·약물전달)로 막는다.
    "나노",
}

# 종목명이지만 텔레그램 주식 문맥에선 거의 항상 다른 뜻이라 아예 매칭에서 빼는 것.
# (경계 규칙으로도 못 거르는 관용 약자들. 실측하며 추가한다.)
#   TP = Target Price(목표가), 종목 TP(007980) 언급은 사실상 없음.
#   전방 = 전방산업/전방시장(downstream), 대상 = target/객체 — 리서치 문맥에서
#   해당 종목(전방 방직, 대상 식품)보다 일반어 쓰임이 압도적이라 v1에선 제외.
#   배럴 = 유가 단위("500만 배럴 이송"), 러셀 = 러셀2000 지수 + 인명(러셀 보트,
#   데이비드 러셀) — 뒤에 조사 아닌 공백이 와서 경계 규칙을 통과해버린다. 코스닥
#   적재 후 전수 확인 결과 두 이름 모두 진짜 종목 언급이 0건이라 뺀다.
EXCLUDE_NAMES: set[str] = {"TP", "전방", "대상", "배럴", "러셀"}

# ── 결 ⑥: 미국 티커와 글자가 같은 국내 종목 ─────────────────────────────────
#
# 2026-08-11 에 미장 카더라 작업을 하다 발견했다. 두 사전이 같은 코퍼스를 보기
# 전까지는 안 보이는 결이다 — 국내 사전만으로는 `STX` 가 씨게이트인지 STX(011810)
# 인지 알 방법이 없다.
#
#   씨게이트 테크놀로지(Seagate Technology, $STX)   → 국내 STX(011810) 로 태그돼 있었다
#   골드만삭스(GS) 센티멘트 지표는 0.6으로 …          → 국내 GS(078930) 로 태그돼 있었다
#
# ## 왜 목록이 둘뿐인가 (전수로 쟀다)
#
# 국내 상장사 중 **이름이 라틴 문자인 것이 90개**인데, 그중 미국 종목 사전
# (config/us_stock_extraction.py) 의 티커 178개와 **글자가 똑같은 것은 이 둘뿐**이다.
# 손으로 고른 게 아니라 교차해서 나온 전부다. 재는 법:
#
#     라틴 국내명 = stocks 에서 이름이 [A-Za-z0-9&\-\. ]+ 인 것
#     겹침        = 그 이름들 ∩ US_NAMES 의 값(티커)
#
# ⚠️ **새 종목이 상장하거나 미국 사전이 늘면 다시 재야 한다.** 손으로 잇는 목록이
#    낡는 그 문제 그대로다(문두 참고). 위 두 줄이면 다시 뽑힌다.
#
# ## 왜 이름을 죽이지 않고 '단서'로 가르나
#
# GS 는 305건 중 32건만 미국이다. 이름을 EXCLUDE 에 넣으면 진짜 GS 언급 273건이
# 같이 죽는다. 결 ③(동음이의)과 같은 처지라 **자리를 봐야 한다.**
#
# 다만 여기서 보는 자리는 표기의 위치가 아니라 **메시지 전체**다. 위치로 가르려
# 해 봤는데 둘 다 실패했다:
#   · `$STX` 의 달러 접두 → **이 코퍼스는 국내 종목에도 달러를 붙인다**
#     (`✔️ $NAVER $카카오`, `$삼성전자 $SK하이닉스`). 미국 표기의 신호가 아니다.
#   · `(STX)` 의 괄호 + 앞 낱말이 미국 종목명 → 앞 낱말을 부분문자열로 보면
#     `은닉 마르코프 모델(HMM)` 이 "모**델**"(Dell) 로 걸린다. 왼쪽 경계 문제 재발.
#   · `STX.US` 접미 → 맞는 신호이긴 한데 **단서 규칙이 이미 전부 잡는다**(추가 0건).
#     규칙을 둘 둘 이유가 없다.
#
# ## 값을 고를 때
#
# 값은 "같은 메시지에 이 낱말이 있으면 그 자리는 미국 종목이다"이다.
# 표기 변형을 다 적어야 한다 — 실측으로 `시게이트` 140건 · `씨게이트` 84건이라
# 하나만 적으면 절반을 놓친다.
#
# ## 실측 (2026-08-11, 코퍼스 136,512건)
#
#   STX  태그 106건 중 **64건**이 단서에 걸린다(60%)
#   GS   태그 305건 중 **32건**(10%)
#   ⭐ 걸린 것 중 **국내 계열사 낱말**(GS건설·GS리테일·GS칼텍스·STX엔진 …)이
#      같이 있는 메시지는 **0건**이다. 즉 이 규칙은 조이기만 한다.
#      규칙을 손볼 땐 그 0을 다시 확인할 것 — 0이 아니면 진짜를 죽이고 있다.
US_TICKER_COLLISION: dict[str, tuple[str, ...]] = {
    "STX": ("씨게이트", "시게이트", "Seagate"),
    # 2026-09-19: 골드만삭스 주최 컨퍼런스 표기(`GS 테크 컨퍼런스`·`GS 컨슈머/리테일
    # 컨퍼런스`)는 이름을 안 적는다. 한 주 3건. 미국 시황의 `MS -1.9%, GS -4.0%`·`$GS` 는
    # 단서가 없어 그대로 산다(4건) — `$`는 국내 종목에도 붙어 단서로 못 쓴다.
    # 2026-09-25 `(GS)`: 골드만삭스 리포트 요약을 퍼 나르는 채널 무리(Jstockclass 등)가 제목 줄 끝에
    # 출처를 `(GS)` 로 단다(`무라타 컨퍼런스콜 후기 : … Buy 유지 (GS)`). 그날 급부상 3위 GS 의 창 안
    # 언급 14건 중 11건이 이 제목 둘의 복붙이었다. 전량 311,880건에서 `(GS)` 가 든 GS 태그 메시지 78건을
    # 다 읽었다 — 골드만 77(제목 끝 75 · `Eric Sheridan(GS)` · `GS GIR` 도표 1) · `굿소프트웨어(GS)인증` 1.
    # 진짜 언급 0 이고, 국내 계열사 낱말·종목표기꼴(`078930`·`(유가)GS`·`기업명: GS`)이 같이 있는 글도 0 이다.
    # 2026-09-25 저녁: `(GS)` 뒤에도 남은 골드만 표기. GS 태그 전량 863건 중 위 단서가 78건을 막고, 남은
    # 785건의 태그 자리를 다 읽었다 — 골드만(미국 종목 GS 포함) 85 · 나머지 700. 아래로 65건을 더 막는다.
    #   `골드만`        22  `골드만 투자의견`·`골드만` 머리글처럼 삭스를 뺀 표기. 앞의 `골드만삭스` 둘을 흡수한다
    #   `GS의 추정치`   14  `※est는 GS의 추정치` 14채널 복붙
    #   `Conviction`     9  `GS 8월 APAC Conviction buy list` 8채널 · `(Conviction List)`
    #   `GSe`            8  골드만 추정치 약자(`vs. GSe`)
    #   `GS 전망치`      7  `GS DRAM 센티먼트 지표` 요약의 `컨센서스가 GS 전망치와 부합`
    #   `BofA, GS` 6 · `GS, 미즈호` 5 · `GS, JPM` 2 · `GS, Bloomberg` 2   IB·출처를 나란히 적은 자리
    # (한 글이 여러 단서에 걸려 합이 65 보다 크다.) 나머지 700건 중 걸리는 글 0 · 새로 막히는 65건에
    # 국내 계열사 낱말·종목표기꼴(`078930`·`(유가)GS`·`기업명: GS`·`GS건설`·`GS칼텍스`·`허태수` …)이 같이 있는 글 0.
    # ⚠️ IB 이름·미국 티커를 **혼자** 단서로 쓰지 않는다. `BofA` 는 진짜 7건, `IBKR` 는 진짜 1건을 죽인다 —
    #    국내 시황 다이제스트에 IB 전망(`뱅크오브아메리카(BofA)는 …`)이나 미국 신고가(`Interactive Brokers(IBKR)`)가
    #    같이 실린다. 그래서 GS 와 나란히 붙은 자리만 적었다. `GS 컨퍼런스` 도 `GS 컨퍼런스콜`(지주사 실적 발표)에
    #    걸려 못 쓴다.
    # ⚠️ 남는 골드만 20건은 그대로 둔다. `(GS: 14.2%~20.0%)` 4채널(월가 은행 기사 — `(GS:`·`월가 은행` 은 국내
    #    글에도 나올 꼴이다) · 미국 시황 속 GS 주가 8(위 09-19 주석의 `MS -1.9%, GS -4.0%` 꼴, 4건에서 늘었다) ·
    #    그 밖의 한 채널짜리 8. 카드를 띄우는 건 여러 채널 복붙인데, 그런 묶음 11개 중 남는 건 `(GS: …)` 하나다.
    "GS": (
        "골드만", "Goldman", "GS 테크 컨퍼런스", "GS 컨슈머", "(GS)",
        "GS의 추정치", "GS 전망치", "GSe", "Conviction",
        "BofA, GS", "GS, 미즈호", "GS, JPM", "GS, Bloomberg",
    ),
}

# 같은 장치를 국내 일반어에도 쓴다 — 이름이 **사람·사물을 가리키는 보통명사**와 글자가
# 같을 때, 그 뜻으로 쓰였음을 알려 주는 낱말이 메시지 어딘가에 있으면 종목이 아니다.
#
# ## 왜 자리로는 못 가르나
#
# `남성`(004270)은 2026-09-07 유령 셋을 걷어 내자 급부상 카드 6위로 올라왔는데, 언급
# 2건이 둘 다 유령이었다(룰루레몬 실적의 `부문별 매출: 남성:` · 기내 난동 기사의
# `변경. 남성은`). 앞이 콜론·마침표라 HEAD_NOUN_NAMES 규칙이 못 잡는 자리다.
# 그런데 **진짜 언급도 같은 자리를 쓴다**(`대원화성, 남성은 시가총액 300억원 미달`).
# HEAD_NOUN_NAMES 주석이 "그 자리엔 진짜가 더 많아 규칙으로는 못 가른다"고 적어 둔 그것이다.
#
# ⚠️ 그렇다고 EXCLUDE_NAMES 에 넣을 수도 없다. 태그된 62건 중 **절반이 진짜**다
# (DART 공시 11 · 실적속보 4 · 지수 편출입 3 · 관리종목 목록 3 · 동전주 목록 4 …).
# `배럴`·`러셀`은 진짜가 0건이라 뺐던 것이고, 이 이름은 그 잣대에 안 걸린다.
#
# ## 실측 — 메시지 239,936건, 태그된 62건 전량 (2026-09-07)
#
#   여성 없음 + 종목표기꼴 있음  30건  진짜(`(유가)남성 -` `△남성` `남성(004270)` `[잠정실적]남성`)
#   여성 포함 + 종목표기꼴 없음  18건  유령
#   ⭐ 여성 포함 + 종목표기꼴 있음  **0건**  ← 이 규칙은 조이기만 한다
#   여성 없음 + 종목표기꼴 없음  14건  유령 12 · 진짜 2
#
# 규칙을 손볼 땐 그 0 을 다시 확인할 것. 0 이 아니면 진짜를 죽이고 있다.
#
# ⚠️ **남은 유령 12건은 그대로 둔다.** `남성 탈모치료제`·`남성 노동자`·`남성 갱년기`
#    처럼 여성이 안 나오는 글이다. 단서를 하나씩 이어 붙이면 문두가 경고한 '손으로 잇는
#    목록'이 되고, 그 12건은 카드에 영향을 주지 않는다. 카드를 만든 두 자리는 둘 다
#    여성 단서에 걸린다.
#
# ## 동서 · 레이 (2026-09-15) — 급부상 카드에 나란히 올라온 유령 둘
#
# 둘 다 AMBIGUOUS_NAMES 에 있는데도 샜다. 뒤 경계 검사는 **붙어 있는** 글자만 보는데
# `동서 송유관`·`레이 달리오`는 띄어 쓴다 — 자리로는 못 가르는 꼴이라 남성과 같은 장치다.
# 그날 종목 요약이 커피 회사에 사우디 송유관 복구 지연을, 치과 장비 회사에 헤지펀드
# 매니저 이야기를 붙여 저장됐다.
#
# 실측 — 태그된 메시지 전량 (2026-09-15)
#
#   동서 46건   송유관·동서 파이프 단서 22건 · 그중 종목표기꼴 있음 **0건**
#              단서 없는 24건은 전부 DART 공시꼴(`(유가)동서 - 반기보고서`, `A026960`)
#   레이 125건  달리오 단서 53건 · 그중 종목표기꼴 있음 **0건**
#              (표기꼴 검사가 `인플레이션 2.34%` 의 '레이'를 3건 세었으나 경계를 보면 0)
#
# ⚠️ 레이는 이 단서로 끝나지 않는다 — 기아 레이(`모닝/레이 신규 모델`)·레이와(令和)·
#    레이 크록·레이 카 캐리어스가 남는다. 남성 주석의 그 경고다: 카드를 만든 자리
#    (달리오 · 뉴스봇 태그 줄)만 막고 나머지는 카드에 안 오르니 그대로 둔다.
HOMONYM_CUES: dict[str, tuple[str, ...]] = {
    "남성": ("여성",),
    # `파이프` 하나로 걸지 않는다 — 바이오 글의 '파이프라인'과 겹친다. 사우디 글은 전부
    # `동서 송유관`·`동서 파이프라인`·`동서파이프 라인` 꼴이라 이름에 붙여 건다.
    "동서": ("송유관", "동서 파이프", "동서파이프"),
    "레이": ("달리오",),
    # ── 2026-09-19 주간 유령 점검 첫 회 — 급부상 카드·요약에 오른 것부터 ────────────
    # OCI(456040)  급부상 09-13·14. 15건 중 7건이 오라클 클라우드(OCI)·OCIO·기타포괄손익.
    #              OCIO 는 boundary_ok 의 '라틴 뒤 라틴' 규칙이 잡고, 나머지는 단서로.
    "OCI": ("오라클", "Oracle", "클라우드 매출", "기타포괄손익"),
    # DB(012030)   요약 09-14. 10건 중 5건이 도이체방크·퇴직연금 DB형.
    "DB": ("도이체방크", "퇴직연금", "DC·IRP", "IRP"),
    # NC(036570)   17건 중 7건 — 현대모비스 리포트의 `C to NC`·KBO NC 다이노스.
    "NC": ("C to NC", "다이노스", "KBO"),
    # 하스(450330) 5건 전부 Arm CEO 르네 하스. HEAD_NOUN 이 `제이 하스`는 잡지만 `하스 CEO`는 못 잡는다.
    "하스": ("하스 CEO", "르네 하스", "Arm"),
    # 진도(088790) 8건 전부 지명(네이버 216MW 진도 태양광 7 · 진도 소삼도 1).
    "진도": ("진도 태양광", "진도군", "소삼도"),
    # 서산(079650) 3건 전부 SK온 서산 공장.
    "서산": ("SK온", "서산 공장", "서산공장"),
    # 캐리(313760) 5건 중 4건이 캐리 트레이드.
    "캐리": ("트레이드", "캐리 전략", "엔캐리"),
    # 알트(459550) — 샘 알트만. JOSA_HEAD 의 '만'이라 뒤 경계로는 못 막는다.
    "알트": ("알트만",),
    # 붙임표 합성어 둘. 붙임표 뒤를 거부하는 자리 규칙은 짝 표기(`한화-EDGE`·`본느-최대주주 변경`)를
    # 잃어 접었고(extract_telegram_stocks.boundary_ok 주석), 이 둘만 단서로 건다.
    # DL(000210)  `DL-메티오닌`(사료 첨가제 시세표) 전량 44건 — 매일 같은 표.
    # 나노(187790) `나노-MRI 조영제`·`나노 멤브레인`·`나노 약물전달체` — 급부상 카드에 오른 그 자리.
    "DL": ("메티오닌",),
    "나노": ("MRI", "멤브레인", "약물전달"),
    # 보령(003850) — 2026-09-22 급부상 6위, 이슈 #541. 창 안 10건 중 9건이 하나증권 LNG 리포트
    # 한 편의 복붙(`포스코인터내셔널(광양), SK E&S/GS에너지(보령), SK가스/KET(울산)`)이라 보령은
    # 회사가 아니라 GS에너지 LNG 터미널이 있는 충남 보령시다. 한 줄 요약이 제약사에 "글로벌 LNG
    # 가격 강세 시 이익 확대"를 붙여 나갔다. 전량 295,363건에서 이름이 통과한 81자리 중 진짜 68 ·
    # 지명 13 이라 이름은 살리고 지명 꼴만 단서로 건다(아래 단서로 지명 12자리, 진짜 0 이 걸린다).
    # ⛔ `LNG` 하나로 걸지 말 것 — 뉴스 모음 글(09-16 balanceasset)에 진짜 언급과 LNG 소식이 같이 있다.
    "보령": ("에너지(보령)", "충청남도 보령", "충남 보령", "진주·보령", "보령·태안", "보령화력", "보령시"),
    # 케이프(064820) — 2026-09-25 아침 급부상 2위(5.0배)로 오를 참이었다. 창(09-22~24) 언급 3건이
    # 전부 띄어 쓴 외국 지명·선박명이다(`플로리다주 케이프 커내버럴 우주군 기지` 2 · `화물선 MV 케이프
    # 다오` 1). 붙여 쓴 `케이프커내버럴`·`케이프사이즈`는 AMBIGUOUS_NAMES 의 뒤 경계가 막지만 띄어
    # 쓰면 뒤가 공백이라 통과한다 — `동서 송유관`·`레이 달리오`와 같은 꼴이다.
    # 전량 310,520건에서 이름이 통과한 42자리를 다 읽었다. 뒤에 띄어 쓴 외국 지명·선박명은 이 넷뿐이다
    # (메시지 수: 커내버럴 8 · 스테이션 3(퍼보 에너지 지열 발전소 Cape Station) · 피어 1(Lower Cape
    # Fear) · 다오 1). 단서가 든 13건에 다른 케이프 자리·종목표기꼴은 0건이다.
    # ⛔ `커내버럴`·`스테이션`처럼 뒷말만 걸지 말고 이름을 붙여 건다. 띄어 쓴 뒷말이 있어도 진짜인
    #    자리(`케이프 등 조선·기자재주 강세` · `(코스닥)케이프 - 반기보고서` · `#케이프 #동성화인텍`)는
    #    그대로 산다. 발행처 목록(PUBLISHER_SHORT_NAMES)에 넣지 않은 이유는 그 목록 주석에 있다.
    "케이프": ("케이프 커내버럴", "케이프 스테이션", "케이프 피어", "케이프 다오"),
    # 오로라(039830) — 2026-09-25 급부상 7위(3.20배, 6위와 0.02배 차). 창(09-22~24) 언급 2건이 둘 다
    # 미국 자율주행 트럭 회사 오로라 이노베이션(AUR)이다(kwtok `오로라 이노베이션, '2030 비전' 발표` ·
    # insidertracking `오로라 이노베이션 (AUR) — 2030년까지 무인 트럭`). 띄어 써서 뒤 경계로는 못 막는다.
    # 전량 310,520건의 태그 54건을 인정된 자리로 다 읽었다. 진짜 22 · 유령 32 이고 유령 17건이 AUR 이다.
    #   오로라 이노베이션  6  kwtok·insidertracking 의 정식 표기
    #   [AI시그널] 오로라  4  이 연재(eMoney AI시그널프로)는 미국 종목만 다룬다. 코퍼스 7,200건이 전부
    #                        `market=US` 링크이거나 미국 상장사라 제목의 `오로라,`도 AUR 이다
    #   Aurora            8  영문 병기. 일리노이주 오로라(소재지) 2 · 오로라 비콘 LLC · 오로라 앳 그린 밸리 ·
    #                        오로라 우라늄 프로젝트 · 오로라 키나아제 · AUR 2
    # 단서가 든 태그 메시지 17건에 진짜 언급과 종목표기꼴은 0건이다(`오로라(039830)이노베이션`의 코드는
    # kwtok 태거가 박은 결 ⑦ 자국이다). 붙여 쓴 `오로라이노베이션`은 코퍼스에 없어 안 넣었다.
    # ⚠️ `Aurora` 는 이 회사의 영문 사명(Aurora World)과 겹친다. 코퍼스엔 0건이다. 나오면 좁힐 것.
    # ⛔ `자율주행` 하나로 걸지 말 것 — 특징주 정리(07-29 balanceasset `13. 오로라 (11.76%)`)에 자율주행
    #    테마가 같이 적혀 진짜 1건이 죽는다. 남는 유령은 13건이다. AUR 6건은 08-18 한 헤드라인의 복붙 4건
    #    (`美 자율주행 업체 '오로라'`)과 `코디악`(경쟁사)이 같이 나온 2건인데, 되풀이되는 표기가 아니라 안
    #    건다. 나머지 7건은 흩어진 일반어·고유명사다(콜로라도주 오로라 · GS건설 오로라 현장 · 오로라 인산화효소 …).
    "오로라": ("오로라 이노베이션", "[AI시그널] 오로라", "Aurora"),
    # 디바이스(187870) — 2026-09-25 저녁 급부상 2위(16.5배), 이슈 #572. 창(09-23~25) 언급 6건이 전부 골드만
    # 리포트 요약 한 편(`무라타 컨퍼런스콜 후기`)의 복붙이고, 잡힌 자리는 `• 디바이스/모듈 부문의` 다. 무라타
    # 제작소의 사업부 이름(Devices & Modules)이지 세정장비 회사가 아니다. 줄머리 글머리표 뒤라 앞 낱말 검사
    # (HEAD_NOUN_NAMES)를 통과하고, 뒤가 `/` 라 뒤 경계도 통과한다.
    # ⛔ 이름 뒤 `/` 를 자리 규칙으로 막지 말 것. 진짜가 같은 자리를 쓴다 — DART 요약 `디바이스/ 단일판매ㆍ
    #    공급계약/`·`미래산업/ 투신 매도` 는 빗금 뒤를 띄우고, 리포트 머리 `[나노/소재/대기오염방지]`·
    #    `[NEW/미디어·엔터]` 는 붙여 쓴다. HEAD_NOUN 이름 뒤 `/` 30자리가 진짜 22 · 유령 8(이 6건과
    #    리포트 분류 `[미래산업/미드스몰캡]` 2)이고, 붙여 쓴 것만 골라도 진짜 둘이 같이 죽는다.
    # 전량 311,880건에서 이름이 통과한 83자리를 다 읽었다. 무라타 사업부 표기는 둘이다(`디바이스/모듈` 6 ·
    # 08-01 JP모건 요약의 `디바이스 모듈은` 1). 두 단서가 든 7건에 다른 디바이스 자리·종목표기꼴은 0건이다.
    # ⛔ `모듈` 하나로 걸지 말 것 — 태그 메시지 13건에 걸리고 그중 진짜가 1건이다. `무라타` 도 안 건다
    #    (걸리는 8건이 다 유령이지만 회사 이름이라 간접 단서다. 오로라 주석이 `코디악`을 뺀 것과 같은 판단이다).
    # 태그 메시지 80 → 73(진짜 35). 남는 유령 38건은 `엣지 디바이스에서`·`디바이스 매출`·`디바이스 및 소비자`
    # 같은 흩어진 일반어라 안 건다(남성·레이 주석과 같은 판단이다).
    "디바이스": ("디바이스/모듈", "디바이스 모듈"),
}

# 종목명을 품고 있지만 **한 번도 그 종목을 뜻하지 않는 구절.** 매칭 전에 URL 처럼 가린다.
#
# `네이버증권`: 리서치 요약 봇의 꼬리말 `출처: 📍네이버증권 리서치` 다. 실측(2026-09-15,
# NAVER 태그 3,665건 전량): 합성어 자리에서만 잡힌 글이 1,359건(37%)이고, 합성어 자리
# 1,600여 곳 중 1,055곳이 이 구절이다. 그 1,359건에 NAVER 종목표기꼴이 있는 글은 3건.
# 이 구절을 가리면 태그를 잃는 글은 795건(다른 자리에도 네이버가 있는 글은 남는다). 네이버클라우드·네이버페이·
# 네이버웹툰(합쳐 360건)은 자회사 언급이라 카카오의 선례대로 그냥 둔다 — 그래서
# AMBIGUOUS_NAMES 에 올리는 대신 이 구절 하나만 가린다.
#
# ⚠️ 자리 규칙이 아니라 구절 목록이다. 낱말을 하나씩 잇지 말 것 — 여기 오를 자격은
#    "태그의 큰 몫이 한 구절에서 나온다"는 실측이다(위 1,055/3,665).
# 2026-09-19 추가 둘.
#   `(상보)`·`[상보]`: 연합인포맥스류 속보의 꼬리표(상세보도). 한 주 상보(027580) 16건 중 14건.
#   후원 안내: 두 채널의 꼬리말(`카카오페이를 통한 커피값 후원하기`·`카카오페이, 네이버페이를
#   이용한 음료 한 잔 후원하기`)이 한 주 카카오페이 39건 중 10건을 만들어 급부상 카드에 올렸다.
#   같은 채널이 매일 올려도 본문이 매번 달라 check_phantom_stocks 의 복붙 감시에 안 걸린다.
NOT_MENTION_PHRASES: tuple[str, ...] = (
    "네이버증권",
    "(상보)",
    "[상보]",
    "카카오페이를 통한 커피값 후원하기",
    "카카오페이, 네이버페이를 이용한 음료 한 잔 후원하기",
)

# 증권사 **줄임말**이 상장사 이름과 같은 경우. 발행처 표기 자리(extract_telegram_stocks.
# publisher_context)를 `…증권`으로 끝나는 이름에만 보는데, 이 둘은 그 꼴이 아니다.
#   흥국(010240)  09-13~19 13건 전부 흥국증권 리서치 `[흥국 지주/유통/상사 박종렬]`·`(흥국)`
#   상상인(038540) 5건 전부 `(상상인 엄우용)`·`상상인 자산전략 Weekly`·상상인저축은행
# 이름 자체는 살린다 — 흥국은 DART 공시가 진짜 언급으로 온다.
# ⚠️ 케이프(064820)는 **넣지 않는다.** 케이프투자증권은 AMBIGUOUS_NAMES 의 뒤 경계로 막히고,
#    여기 넣으면 publisher_context ⑤(뒤에 띄어쓴 한글)가 `케이프 등 조선·기자재주 강세` 같은
#    진짜 언급을 발행처로 읽는다(전량 재현 3건).
PUBLISHER_SHORT_NAMES: frozenset[str] = frozenset({"흥국", "상상인"})

# 종목명 뒤에 붙어도 '그 종목 언급'으로 인정하는 한국어 조사(경계 판정용).
#
# ## 한 글자가 아니라 형태소 목록인 이유
#
# 2026-08-05 까지 이건 한 글자 집합이었고, 경계 규칙도 **붙어 있는 한 글자**만 봤다.
# 그 글자가 조사면 통과였다. 그래서 이름 뒤에 조사꼴 음절로 시작하는 합성어가 붙으면
# 그대로 새어 나갔다 — `하이브`+`로자임`(알테오젠의 SC 제형변경 플랫폼)이 그것이다.
# `하이브`는 이미 AMBIGUOUS_NAMES 에 있었는데도 `로` 가 조사라 아무 소용이 없었다.
#
# 그래서 **한글 덩어리 전체**를 본다(extract_telegram_stocks.JOSA_TAIL_RE). 이어지는
# 한글을 이 목록의 조사들로 끝까지 설명할 수 있어야 인정한다.
#
#   하이브|로자임    로 다음이 `자` → 조사로 설명 안 됨 → 거부
#   SK|와의         와 + 의                        → 인정
#   SK하이닉스|로부터  로부터                          → 인정
#
# ⚠️ 이어 붙이는 건 아무 조사나가 아니다. 둘째 자리부터는 JOSA_TRAILING 만 쓴다 —
#    그 제한이 없으면 `아스트|로처럼`이 `로`+`처럼` 으로 설명돼 통과한다(2026-09-07).
#
# 다음 글자 하나로는 못 가른다. 조사를 아예 안 보면 `삼성전자와`·`SK하이닉스는` 이 전부
# 합성어로 걸려 유령률이 7.5%로 부풀고, 조사를 보면 `하이브로자임`의 `로`가 조사로
# 오인된다. **덩어리 전체**여야 둘이 갈린다.
#
# ## 이 목록은 위 목록들과 달리 낡지 않는다
#
# 문두에 "손으로 잇는 목록은 수렴하지 않는다"고 적어 뒀는데, 그건 종목명 이야기다.
# 조사는 **닫힌 집합**이라 새 회사가 상장해도, 사명이 바뀌어도 늘지 않는다. 새 낱말
# 목록을 하나 더 지는 게 아니라 이미 있던 목록을 형태소 단위로 고쳐 적은 것이다.
#
# ## 이 규칙은 조이기만 한다 — JOSA_HEAD 가 그걸 보장한다
#
# 목록을 형태소로 넓히면 **느슨해질 수 있다.** 옛 규칙은 첫 글자가 17자 안에 있어야
# 통과였으므로 `태양|으로부터`(해)와 `아스트|라고`(아스트라고: 씨이랩 제품명)는 `으`·`라`
# 에서 이미 막혀 있었는데, 조사를 문법대로 다 적으면 이 둘이 되살아난다. 실측으로
# 유령 16건이 새로 생겼다. 그래서 **첫 글자 관문은 예전 그대로 두고**(JOSA_HEAD)
# 그 뒤를 더 볼 뿐이다. 긴 조사는 이어 붙는 자리에서만 쓰인다(`만|으로`, `에서|부터`).
#
# 그 결과 지금 통과하는 것 중 새로 통과하는 것은 하나도 없다(전량 재현으로 확인:
# 늘어난 종목 0개). 영향 범위는 '지금 통과하면서 뒤 한글이 2자 이상인 자리'로 갇히고,
# 그건 메시지 95,200건 전량에서 245자리다. 저장 행은 **108건 빠진다**(74,258 → 74,150).
# 그 108건을 전부 손으로 읽어 107건이 오탐이었다. 잃은 진짜 언급은 한 건인데,
# 띄어쓰기가 빠진 원문이다(`'㈜KT의개인정보 유출사고' 제재처분 의결`).
#
# 목록은 오탐위험군 **밖** 이름들의 꼬리 분포에서 뽑았다. 그쪽은 뒤 경계 검사를 아예
# 안 받아 꼬리가 전부 통과하므로, 이 코퍼스에서 종목명 뒤에 실제로 무엇이 붙는지의
# 무편향 표본이 된다(15,771자리). 규칙을 설계한 표본과 검증한 표본을 그렇게 갈랐다.
JOSA: tuple[str, ...] = (
    # 격조사
    "은", "는", "이", "가", "을", "를", "의", "에", "와", "과", "로", "으로",
    "에서", "에게", "에게서", "한테", "한테서", "께", "께서",
    "로서", "으로서", "로써", "으로써", "로부터", "으로부터",
    # 보조사
    "도", "만", "나", "이나", "랑", "이랑", "하고", "부터", "까지",
    "조차", "마저", "밖에", "처럼", "만큼", "보다", "마다", "대로", "커녕",
    # 서술격조사(이다)의 활용형 — "케이뱅크이며", "파인엠텍이야"
    "이다", "이며", "이고", "이라", "이라는", "이라고", "이라도", "이야",
    "라는", "라고", "라도", "든", "든지", "이든", "이든지",
    # 옛 한 글자 목록에 있던 축약형("…에서"의 준말).
    "서",
)

# 꼬리의 **첫 글자** 관문 — 2026-08-05 이전의 JOSA 한 글자 집합 그대로다.
# 위 목록의 긴 조사 중 여기 없는 글자로 시작하는 것(으로·부터·까지·처럼·라고…)은
# 이어 붙는 자리에서만 쓰인다. 이 관문이 있어야 규칙이 조이기만 한다(JOSA 주석 참고).
JOSA_HEAD: str = "은는이가을를에의도와과로만나랑서께"

# 조사 **뒤에 이어 붙을 수 있는** 조사. 첫 자리는 위 JOSA 전체를 쓰고, 두 번째부터는
# 이 목록만 쓴다.
#
# ## 왜 나눴나 — `아스트로처럼`
#
# 2026-09-07 /kadera 급부상 카드에 `아스트`(067390)가 올라온 걸 화면에서 잡았다.
# OpenAI 의 아스트라 발표를 다룬 미국 시황 요약이 채널 넷에 복붙됐는데, 그 글에서
# 종목으로 세어진 자리가 하나뿐이었다.
#
#   …수익성에 대한 우려가 확대되고 있었지만 **아스트|로처럼** 기술 발전이 …
#
# 같은 글의 `아스트라로`·`아스트라를`·`아스트라는`은 이미 막혀 있었다. `로처럼`만
# 새어 나간 건 그게 조사 `로`+`처럼` 로 **끝까지** 쪼개져서다. 결 ⑤ 를 막은
# JOSA_TAIL_RE 는 '이어지는 한글 덩어리가 조사 연쇄로 전부 설명되는가'를 보는데,
# 여기서는 정말로 설명이 된다. 그물의 코가 굵은 게 아니라 **연쇄를 문법 없이
# 이어 붙인 것**이 문제였다(`로처럼`은 한국어에 없는 결합이다).
#
# ## 목록은 코퍼스가 정했다
#
# JOSA 자체를 뽑을 때 쓴 것과 같은 무편향 표본으로 셌다 — 오탐위험군 **밖** 이름들의
# 꼬리(그쪽은 뒤 경계 검사를 아예 안 받아 꼬리가 전부 통과하므로 편향이 없다).
# 메시지 239,936건에서 2형태소 이상 연쇄가 552자리 · 26가지였고, **두 번째 자리에
# 실제로 온 조사는 여섯뿐**이다.
#
#   와|의 271 · 과|의 84 · 에|는 46 · 에|도 40 · 로부터|의 23 · 에서|는 15
#   에서|도 12 · 만|의 8 · 로|의 6 · 에서|만 6 · 에게|도 5 · 에게|는 5 …
#
# `처럼`·`만큼`·`보다`가 두 번째로 온 자리는 **0건**이다. 아래 목록은 그 여섯에
# 같은 자리에 설 수 있는 격조사·보조사를 더한 것이고, JOSA 의 부분집합이라
# **조이기만 한다**(집합이 좁아질 뿐 넓어지지 않는다).
#
# ## 실측 — 전량 재현(2026-09-07)
#
# 오탐위험군 안에서 연쇄로 통과하던 111자리 중 이 규칙으로 죽는 건 **6자리뿐**이다:
# `아스트|로|처럼` 4 · `알트|만|과` 1 · `알트|만|과|의` 1(샘 알트만). 진짜 언급인
# `HD현대와의` 8 · `한화와의` 7 · `KT와의` 7 · `SK와의` 6 · `케이씨와의` 5 는
# 전부 그대로 산다. 새로 생기는 태그는 0건이다.
#
# ⚠️ `에서처럼`은 문법에 맞는데 이 규칙이 막는다(`처럼`이 두 번째 자리라서). 코퍼스
#    전체에 3건이고 종목명 뒤에 온 자리는 0건이라 감수했다. 종목명 뒤에서 실제로
#    쓰이는 게 확인되면 여기 `처럼`을 더하지 말고 **`에서처럼` 을 JOSA 에 한 덩어리로**
#    넣을 것 — 두 번째 자리를 열면 `로처럼`이 같이 살아난다.
JOSA_TRAILING: tuple[str, ...] = (
    "은", "는", "이", "가", "을", "를", "의", "도", "만",
    "부터", "까지", "나", "라도", "로", "으로", "서",
)

# 오탐 위험 종목명 뒤에 띄어쓰고 오면 '개별 종목'이 아니라 '그룹 전체' 지칭으로 보는 말.
# 붙여쓴 "SK그룹"은 조사 아닌 한글이라 이미 걸러지는데 띄어쓴 "SK 그룹"만 통과해
# 규칙이 어긋나던 걸 맞춘다.
GROUP_SUFFIXES: tuple[str, ...] = ("그룹", "계열", "그룹주", "일가")
